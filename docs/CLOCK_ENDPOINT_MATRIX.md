# Clock PMS+ Endpoint Matrix

Living document (Milestone 11, Task 14 — updated incrementally by Tasks 4-11, not written fresh at the end). Every row carries a verification status per `docs/source/clock-pms-integration.pdf` section 39: `CONFIRMED_BY_DOCS`, `CONFIRMED_IN_SANDBOX`, `CONFIRMED_BY_CLOCK_SUPPORT`, `ASSUMPTION`, or `NOT_SUPPORTED`. No `ASSUMPTION`-only row may be treated as production-ready (section 39's own rule).

Sandbox used for verification: HOTEL DEMO account (`support@must.al`), `sky-eu1.clock-software.com`, verified 2026-08-04.

| Domain | Use case | API family | Method | Endpoint | Parameters | Response schema | Filters | Pagination | Statuses | Webhook | Retry | Idempotency | Caching | Reconciliation | Verification | Last verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Auth | Digest challenge/response | both | any | `/{api}/{account_id}/{subscription_id}/...` | n/a | `WWW-Authenticate: Digest realm="API", qop="auth", algorithm=MD5, nonce, opaque` on 401 | n/a | n/a | 401 unauthenticated, 403 authenticated-but-lacking-right, 200/201 success | n/a | n/a | n/a | n/a | n/a | **CONFIRMED_IN_SANDBOX** | 2026-08-04 |
| Catalog | List room types | `pms_api` | GET | `/room_types` | none required | JSON array: `id, name, created_at, updated_at, location_id, operational_until, upgrade_index, beds_count, sort_order, city_tax_id, is_virtual, components_room_type_ids` | Not yet tested | Not yet tested (no `next`/`page` observed in a small result set) | 200 on success | n/a | Not yet tested | n/a | Not yet tested | n/a | **CONFIRMED_IN_SANDBOX** (shape); pagination/filters **ASSUMPTION** | 2026-08-04 |
| Catalog | List rooms | `pms_api` | GET | `/rooms` | none required | JSON array: `id, name, room_type_id, created_at, updated_at, description, ...` | Not yet tested | Not yet tested | 200 on success | n/a | Not yet tested | n/a | Not yet tested | n/a | **CONFIRMED_IN_SANDBOX** (shape); pagination/filters **ASSUMPTION** | 2026-08-04 |
| Availability | Rate/availability query | `pms_api` | GET (tested) | `/rates_availability` | Requires (per validation error observed) at least `from`, `to`, `rates` — exact param format (array/bracket vs. JSON body) **not yet confirmed** | Error body observed: `{"error":"RestApi::Interactions::RateAvailability::Contract\n..."}` on missing params; success shape not yet observed | Unknown | Unknown | 400 on missing required params | n/a | Unknown | n/a | Unknown | n/a | **ASSUMPTION** (endpoint exists and is reachable; exact request contract still open — source brief section 38 lists "availability endpoint" as an explicit open ADR item) | 2026-08-04 |
| Booking | Create booking | `pms_api` | Unknown | `/bookings/` (per legacy plugin changelog prose — not independently verified this session) | Unknown | Unknown | n/a | n/a | Unknown | n/a | Unknown | Unknown | n/a | n/a | **ASSUMPTION** (endpoint path only, from legacy WordPress plugin changelog, itself described by its own authors as not externally verified) | Not yet verified in sandbox |
| Guests | Guest search | `pms_api` | Unknown | `/guests/search` (per legacy plugin changelog prose) | Unknown | Unknown | Unknown | Unknown | Unknown | n/a | Unknown | Unknown | n/a | n/a | **ASSUMPTION** | Not yet verified in sandbox |
| Webhooks | Clock Message Channels | n/a | POST (inbound) | Not yet built (Task 11) | Amazon SNS envelope shape, per source brief section 20 | Unknown | n/a | n/a | Unknown | AWS SNS `SignatureVersion`/`SigningCertURL` verification (source brief section 27) | n/a | n/a | n/a | n/a | **CONFIRMED_BY_DOCS** (source brief); mechanics **ASSUMPTION** until Task 11 | Not yet built |

## Notes

- **Task 6**: `ClockPmsProvider.testConnection` and the tenant-facing "Test connection" button now both run the full stack for real — HTTP client, rate limiter, circuit breaker, error classification — against the real sandbox, not just the raw Digest probe done at kickoff. Confirmed: a valid connection succeeds end-to-end, and an invalid API key produces a clean, correctly-classified failure rather than a crash or a false success. Clock's 401-on-bad-credentials body is **plain text** (`HTTP Digest: Access denied.`), not JSON — `classifyClockHttpResponse` handles both shapes.
- `room_types` and `rooms` were confirmed reachable and correctly shaped with **real data already present in the sandbox account** (e.g. room type "Standard Rooms", room "Direct Pool") — not empty-state guesses.
- The account/subscription ID pair in the URL path (`172528`/`16307` for this sandbox) — which one is the "Clock account ID" vs. "subscription ID" per source brief section 8's terminology is not yet disambiguated; both are opaque path segments as far as the HTTP client (Task 4) is concerned, so this doesn't block implementation, only the deliverable docs' precision.
- `/rates_availability`, booking creation, and guest search remain the highest-priority endpoints to confirm for real (Tasks 7, 8, 10) — this table will be updated as each is actually exercised against the sandbox, not assumed from the legacy plugin's changelog prose.
