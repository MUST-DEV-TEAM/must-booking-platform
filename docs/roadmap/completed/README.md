# Completed milestones

Milestone files are moved here from `../milestones/` by Claude when every one of a milestone's 10 tasks is reviewed and marked Done — see the process in `../README.md`. Empty for now; Milestone 0 is still active.
