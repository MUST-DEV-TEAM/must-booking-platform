# Milestone 2: Self-Serve Signup & Free Plan Onboarding

Status: Done
Depends on: Milestone 1; ADR-0007 (illustrative plan shape, PMS connections capped not unlimited), ADR-0008 (self-serve, permanent Free plan — separate from any paid-plan trial)

## Goal

A new hotel can sign up self-serve, without a payment card, and land directly on the **permanent** Free plan (illustrative limits from `docs/BILLING.md` — real numbers confirmed at Milestone 8). Free has no expiry clock — signup does not start a trial. Done means: a stranger can complete signup end-to-end and reach an empty tenant dashboard on Free. The separate, optional paid-plan-trial start/expiry mechanism (ADR-0008) is built in Milestone 8 alongside the upgrade flow, not here.

## Draft task areas (not final — define the real 10 tasks at kickoff)

1. Signup flow API: create organization + first property + first admin user in one transaction, per Milestone 1's schema.
2. Signup UI (Next.js): organization name, first property basics, admin account creation.
3. Assign new tenants the Free plan (permanent, no `trial_ends_at` set at signup — ADR-0008).
4. Email verification requirement before full activation (using Milestone 1's primitive).
5. Welcome email (transactional email provider chosen and wired here).
6. Signup-abuse guardrails: rate limiting per email/IP, basic bot protection.
7. Free-plan limits enforced from day one (max 1 property / 3 staff, illustrative) using the plan/limit fields from task 9 — no trial-expiry job in this milestone.
8. "Upgrade to unlock more" prompts in the dashboard shell where a Free limit is hit (no trial countdown — Free doesn't expire).
9. Plan/limit fields (`max_properties`, `max_staff_seats`, `pms_enabled`) added to the organization record, sourced from the plan table.
10. E2E test: full signup → email verify → login → land on empty Free-plan dashboard.

## Kickoff decisions (2026-07-28)

- Plan schema: a real `plans` table from day one (not hardcoded constants on `organizations`), per ADR-0007's "extensible for future tiers" intent — seeded with a single Free row now; Milestone 8 adds further rows (and Stripe wiring) rather than reworking the schema.
- Email provider: **Resend**, wired behind a `MailProvider` interface mirroring `PmsProvider`'s pattern (`docs/ARCHITECTURE.md`) — chosen for its usable free tier and no sandbox/production-access approval step, unlike SES; Postmark's free tier is a one-time 100 emails and not workable ongoing.
- Email verification gating: **dashboard access is granted immediately after signup**, before verification — unverified accounts see a persistent banner and lose access to sensitive actions (e.g. staff invite) until verified. This also requires signup to auto-issue a session (Milestone 1's `signup` endpoint does not set a session cookie today — Task 2 adds that).
- Signup-abuse guardrails: **rate limiting only** (Redis-backed, per-IP and per-email) — no CAPTCHA for v1; add one later only if abuse is actually observed.
- First property fields at signup: **name, address, timezone** — enough for Milestone 3 to build on without a schema patch; currency and other property detail remain Milestone 3's job.

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | `plans` table: `max_properties`, `max_staff_seats`, `pms_enabled`, `max_pms_connections_per_property`; seed one Free row (illustrative numbers per `docs/BILLING.md`, labeled as such). Add `organizations.plan_id` FK, defaulting new rows to Free. | Migration applies cleanly; a fresh `organizations` row without an explicit `plan_id` resolves to the seeded Free plan. | Done | `20260728010000_plans` |
| 2 | Signup API: single transaction creating `organization` (Free plan) + first `property` (name/address/timezone) + first admin `user` + `tenant_membership` (Owner) + email-verification token issuance (reusing Milestone 1's `issueToken`), plus auto-login (issues a session/cookie same as `login`, since today's `signup` does not). | One request leaves the caller with a valid session and a fully-formed tenant/property/Owner membership, all four rows created atomically (no partial state on failure). | Done | `20260728020000_signup_property_details` |
| 3 | `MailProvider` interface (mirrors `PmsProvider`) + Resend implementation. Wire real sending of the verification email (token from Task 2) and a welcome email triggered on successful verification. **Two follow-ups found in review, both landed:** (1) `signup()`/`verifyEmail()` catch-and-log mail-send failures instead of unwinding an already-committed core action; (2) `WEB_APP_URL` is now boot-time required in `environment.ts` (matching `DATABASE_URL`/`REDIS_URL`), and verification-URL construction moved inside the same try/catch boundary as the mail send, so no future construction step can bypass it either. Both proven by dedicated tests, including one that deletes `process.env.WEB_APP_URL` mid-run and asserts signup still returns 201 with a session. | Signing up sends a real verification email via Resend; verifying triggers a welcome email; provider access is only through the interface, never a direct Resend call from domain code; a mail-provider failure — or a misconfigured mail-related env var — does not fail signup or verification once the underlying DB state has committed. | Done | (uncommitted locally) |
| 4 | Signup UI (Next.js): organization name, first property basics (name/address/timezone), admin account fields; submits to Task 2's API and redirects straight to the dashboard shell. | A stranger can complete the form and land on the dashboard in one flow, no separate login step. | Done | (uncommitted locally) |
| 5 | Unverified-account gating: persistent "verify your email" banner on the dashboard shell; guard blocking sensitive actions (e.g. staff invite from Milestone 1) while unverified; clicking the emailed link consumes the token and clears the gate. **Two required follow-ups found in review, both landed:** (1) `isEmailVerified()` now explicitly returns `false` for a nonexistent user instead of `undefined !== null` evaluating to `true`. (2) `@RequiresVerifiedEmail()` moved off the `AdminStaffController` class onto only the four mutating handlers, matching `invite`'s method-level placement; `listMemberships`/`listPropertyStaff` stay open to unverified accounts, proven by a test that shows GET succeeding, PATCH 403ing, then succeeding once verified. **Reviewer also fixed directly (stale test, not a product bug):** `audit-log.e2e.spec.ts`'s fixture never marked its owner verified, so the full suite (not run by Codex this round — only 3 focused files were) failed once the four mutating actions became gated; added `email_verified_at` to that fixture's insert, matching the pattern already used elsewhere. | An unverified tenant reaches the dashboard but cannot invite staff until verified; verifying removes the banner and unblocks the guarded actions without re-login; a nonexistent user is never treated as verified; read-only admin listings remain available to an unverified Owner. | Done | (uncommitted locally) |
| 6 | Signup rate limiting: Redis-backed limiter, per-IP and per-email caps on the signup endpoint. **Two follow-ups found in review, both landed:** (1) `clientIp()` no longer trusts the caller-supplied `X-Forwarded-For` header (no trusted-reverse-proxy config exists yet); it uses only `request.socket.remoteAddress`, proven by a unit test that sets a spoofed header and asserts the limiter is still called with the socket's real address. (2) The two e2e spec files that call the real `/auth/signup` endpoint (sharing one real loopback IP) now clear the `rate-limit:signup:*` Redis keyspace in their own `beforeAll`, via a shared `clearSignupRateLimits()` test helper. Confirmed live: ran the full suite three consecutive times, 15 files/32 tests passing every time (previously the second consecutive run failed with stale state). | Exceeding either cap returns 429 with a retry-after; legitimate signups under the cap are unaffected; the per-IP cap cannot be bypassed by a caller simply setting its own `X-Forwarded-For` header; the e2e suite passes on repeated runs within the same hour without manual Redis intervention. | Done | (uncommitted locally) |
| 7 | Free-plan hard-limit enforcement, **narrowed at review (scope conflict found by Codex, resolved with the owner 2026-07-28):** this milestone excludes property management beyond the signup-created property (Milestone 3) and has no PMS-connection endpoint at all (Milestone 9), so `max_properties` and `pms_enabled` have no real mutation point to guard yet — building throwaway stub endpoints just to gate them was rejected as wasted, scope-violating work. Task 7 now only enforces `max_staff_seats` on the existing staff-invite endpoint (`StaffInviteController.invite`). Property-count enforcement must be added as an explicit task/acceptance-criteria line at Milestone 3 kickoff (when property creation is built); PMS-connection gating must be added at Milestone 9 kickoff (or Milestone 8's plan-enforcement wiring, whichever builds that endpoint first) — Claude must carry this forward at those kickoffs, not drop it. **Ambiguity resolved with the owner:** `max_staff_seats` counts every `tenant_membership` row including the Owner's own (total roster size, not "invited staff beyond the Owner") — recorded in ADR-0007. Implementation correctly locks the `organizations` row (`FOR UPDATE`) inside the transaction that checks-and-inserts the membership at accept/activate time, so concurrent acceptances can't both slip past the cap; the invite-time check is advisory only (pending invitations don't reserve a seat), which is a disclosed, reasonable v1 trade-off. | Inviting a staff member beyond `plans.max_staff_seats` for the tenant's current plan is rejected at the API layer with a clear error, even if the UI prompt (Task 8) is bypassed. | Done | (uncommitted locally) |
| 8 | **Narrowed at review (scope conflict found by Codex, resolved with the owner 2026-07-28):** the dashboard has no staff-management (or property-management) UI at all yet — that's Milestone 7's ("Staff management UI") and Milestone 3's job respectively — so a one-off "Invite staff" control built now would be thrown away once those milestones build the real screens around it. Task 8 is narrowed to a read-only, tenant-scoped plan-usage API: `GET /tenants/:tenantId/plan-usage` (or similar) returning the tenant's plan limits alongside current usage (at minimum `maxStaffSeats`/current staff-membership count, from Task 7's counting logic). No dashboard UI ships in this milestone. The actual disabled-control/"Upgrade to unlock more" UI is deferred to Milestone 7 kickoff (staff) and Milestone 3 kickoff (property, alongside the enforcement note already there) — carry-forward notes already added to both milestone files. Implemented as `GET /tenants/:tenantId/plan-usage`, gated by the existing `@TenantScoped()` membership guard (cross-tenant request proven to 403 in the test), returning plan limits plus live property/staff-seat counts using the same counting logic as Task 7. | The plan-usage endpoint is tenant-scoped (RLS-consistent, no cross-tenant leak) and returns accurate current-vs-max staff-seat usage for the caller's own tenant; no UI claim is made for this milestone. | Done | (uncommitted locally) |
| 9 | Audit logging for signup: extend Milestone 1's audit log to cover tenant/property creation at signup (actor, action, target, tenant context). | A durable audit log entry exists for each signup's organization and property creation, queryable the same way as Milestone 1's auth-sensitive entries. | Done | (uncommitted locally) |
| 10 | E2E test: full signup → dashboard (unverified banner visible, staff-invite blocked) → verify → welcome email received → **confirm `GET /tenants/:tenantId/plan-usage` reports the correct usage once the staff-seat cap is hit** (replaces the deferred dashboard-prompt assertion, per Task 8's UI deferral to Milestones 3 and 7 — no such UI exists to test in this milestone). | The full flow passes as one automated test, not manually inspected steps. | Done | (uncommitted locally) |

## Explicitly not included

- Upgrade/payment flow (Milestone 8).
- Any property/room/rate management beyond the property created at signup (Milestone 3).
