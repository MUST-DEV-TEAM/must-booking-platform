# Milestone 4: Local Booking Domain & State Machine

Status: Done (reopened and re-closed 2026-07-31 for task 11)
Depends on: Milestone 3

## Goal

The provider-agnostic booking domain exists: the `PmsProvider` interface (`docs/ARCHITECTURE.md`), a `LocalPmsProvider` implementation, the booking state machine, and idempotent booking creation/update/cancellation — proven against local inventory before any PMS complexity is introduced. Done means: a booking can be created, updated, and cancelled idempotently, with double-booking provably prevented under concurrency.

## Draft task areas (not final — define the real 10 tasks at kickoff)

1. `PmsProvider` interface finalized in `packages/domain-contracts` per `docs/ARCHITECTURE.md`'s signature.
2. `LocalPmsProvider` implementation over Milestone 3's inventory model.
3. Booking state machine (adapted from `docs/source/clock-pms-integration.pdf` section 17 for the local-only path: DRAFT → QUOTED → INVENTORY_REVALIDATING → ... → CONFIRMED, plus failure states).
4. Quote snapshot mechanism (signed/session-bound, tamper/staleness detection per the source brief section 16).
5. Booking create/update/cancel API, idempotent via an `integration_operations`-style table (source brief section 18) even for the local provider, so the pattern is proven before Milestone 10 needs it for Clock.
6. Guest record model and guest-matching rule (source brief section 24 — not name-only matching).
7. Concurrency test: two simultaneous booking attempts for the last unit of inventory — exactly one must succeed.
8. Booking projection/read model for admin/dashboard consumption (source brief section 23).
9. Cancellation policy evaluation (basic local cancellation rules; refund handling is Milestone 5's concern).
10. Unit + integration test suite for every state transition, including failure/edge states (AVAILABILITY_FAILED, MANUAL_REVIEW equivalent for local).

## Explicitly not included

- Any external PMS provider (Milestone 10).
- Payment processing (Milestone 5) — bookings here are payment-agnostic at the state-machine level, with a placeholder payment-pending state.

## Kickoff decisions

Resolved at kickoff on 2026-07-30, before writing the task table below:

- **Inventory consumption**: [ADR-0013](../../decisions/ADR-0013-local-inventory-consumption-model.md) — separate `booked_units` counter on `inventory_units`; `available_units` stays staff-configured, sellable inventory is derived at read time.
- **State machine scope**: [ADR-0014](../../decisions/ADR-0014-booking-state-machine.md) — the full state list from the source brief (section 17) is built now, resolved synchronously by `LocalPmsProvider`; Milestone 10 swaps only the provider.
- **Guest matching**: [ADR-0015](../../decisions/ADR-0015-guest-matching-rule.md) — exact email match only; phone is stored but never auto-merges.
- **Cancellation policy scope**: per-rate-plan cutoff/free-vs-non-refundable flag, evaluated and recorded at cancel time; no monetary action (Milestone 5's concern).

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | Finalize `PmsProvider` interface and shared booking domain types in `packages/domain-contracts` | `unknown` placeholders in `PmsProvider` (`packages/domain-contracts/src/index.ts`) replaced with real types: `Booking`, `BookingStatus` (ADR-0014's full enum), `Result<T>`, `Page<T>`, `AvailabilityQuery`/`AvailabilityResult`, `CreateBookingCommand`/`UpdateBookingCommand`/`CancelBookingCommand`. No behavior change — types only, matching the signature already documented in `docs/ARCHITECTURE.md`. | Done | (uncommitted locally) |
| 2 | `bookings` table + state machine transition guard | New `bookings` table: `tenant_id`, `property_id`, `room_type_id`, `guest_id`, `status` (ADR-0014's enum), `starts_on`/`ends_on`, `rate_plan_id`, `total_amount` (NUMERIC), `created_at`/`updated_at`, optimistic `version` column. A `BookingStateMachine` (or equivalent service method) holds the full transition table from ADR-0014 and rejects any transition not in it — covered by a unit test enumerating every valid and several invalid transitions. Required follow-up (recovery transitions for `MANUAL_REVIEW`/`PMS_UNKNOWN_RESULT`/`PMS_REJECTED`, found in review) landed and verified. | Done | (uncommitted locally) |
| 3 | Inventory consumption (ADR-0013) | Migration adds `booked_units INTEGER NOT NULL DEFAULT 0` + `CHECK (booked_units >= 0)` + `CHECK (booked_units <= available_units)` to `inventory_units`. `AvailabilityService.getAvailability` aggregate changes to `available_units - booked_units`. A new internal method reserves/releases `booked_units` across a date range inside a caller-supplied transaction (used by tasks 4 and 9), taking a `pg_advisory_xact_lock` scoped to `(tenant_id, property_id, room_type_id)` before checking/updating the range. | Done | (uncommitted locally) |
| 4 | `LocalPmsProvider` implementation | Implements all 8 `PmsProvider` methods over Milestone 3's catalog + task 3's inventory: `testConnection` is a trivial success, `syncCatalog` returns local room types/rates as a `Page`, `getAvailability` delegates to `AvailabilityService`, `createBooking`/`updateBooking`/`cancelBooking` drive the state machine (task 2) through `PMS_CREATION_PENDING`/`PMS_CONFIRMATION_PENDING` synchronously in the same transaction. Registered as the only `PmsProvider` implementation for now (no admin-facing provider selection yet — that's Milestone 10). Required follow-up (pre-reservation cancellation crash; missing `booking.created`/`booking.cancelled` audit entries) landed and verified — both reproduced independently before the fix, and re-verified after. Open design note carried to a standalone follow-up (see below): the audit entries use `guestId` as `actorUserId`, which is a real value but a semantic mismatch — `PmsProviderContext` (my own Task 1 design) has no actor-identity field at all, so there was no correct value available; `AuditLogService`'s `actor_user_id` is `NOT NULL` and every other caller in the codebase populates it with a real `users.id`. | Done | (uncommitted locally) |
| 5 | Quote snapshot mechanism | A `POST .../quotes` (or equivalent) endpoint returns a signed, session-bound quote referencing room type, rate plan, dates, and price, with an expiry timestamp. `INVENTORY_REVALIDATING` re-checks the quote's signature and expiry before proceeding to booking creation; an expired or tampered quote is rejected into `AVAILABILITY_FAILED` with a specific error, not a generic 400. Covered by tests for valid, expired, and tampered-signature quotes. Also lands the standalone audit-actor fix (`audit_logs.actor_user_id` now nullable; guest-initiated actions record `actorUserId: null` + `details: { guestId }`). Open question carried forward, to resolve before Task 6: `QuoteController`/session-binding currently piggyback on the staff `must_session` cookie (`@TenantScoped`) — there is no anonymous/guest session concept yet, so nothing here is actually callable by an unauthenticated guest today. | Done | (uncommitted locally) |
| 6 | Idempotent booking create/update/cancel via `integration_operations`, exposed via a public booking API | New `integration_operations` table: `idempotency_key`, `aggregate_id`, `external_reference`, `request_hash`, `status`, `attempts`, `external_entity_id`, tenant/property-scoped, unique on `(tenant_id, idempotency_key)`. Booking create/update/cancel endpoints require an idempotency key; a retried request with the same key and same `request_hash` returns the original result without re-executing side effects; same key with a different `request_hash` is rejected as a conflict. Same pattern applies to `LocalPmsProvider` now so Milestone 10's Clock adapter reuses it unchanged. Per ADR-0017, also lands the `must_guest_session` cookie + `@PublicTenantScoped` guard (retrofitting `QuoteController` off `@TenantScoped`, and `TenantContextGuard` bypassing for `@PublicTenantScoped` routes) and exposes booking creation/update/cancel through it, so an anonymous guest — not just staff — can actually complete a booking. | Done | (uncommitted locally) |
| 7 | Guest record model + matching (ADR-0015) | New `guests` table scoped `(tenant_id, ...)` with `email`, `phone` (nullable, informational only), unique index on `(tenant_id, lower(email))`. Booking creation looks up by exact-email match; no match creates a new guest record. No phone-based or name-based matching anywhere in the lookup path. `bookings_tenant_guest_fkey` fully validated (I found and cleaned up one leftover pre-Task-7 orphaned test row, then landed migration `20260730070000_validate_booking_guest_fkey` myself — mechanical, not sent back to Codex). Required follow-up (`resolveGuest` now returns `Result<string>` instead of throwing, matching the rest of the file) landed and verified. | Done | (uncommitted locally) |
| 8 | Concurrency test: last-unit double booking | A test that fires two simultaneous `createBooking` calls for a room type/date range where `available_units - booked_units = 1`. Exactly one must reach `CONFIRMED`; the other must fail cleanly into `AVAILABILITY_FAILED` (not a raw DB constraint error surfaced to the caller, not both succeeding). Proves task 3's locking actually serializes correctly under real concurrency, not just sequential calls. Codex found and fixed a genuine advisory-lock deadlock in the process (`createBooking` now takes the inventory lock before any other row locks) — I independently reproduced the deadlock by reverting the fix (6/6 failing runs) and confirmed the fix resolves it (6/6 passing runs, plus a clean full suite). | Done | (uncommitted locally) |
| 9 | Cancellation | `cancelBooking` evaluates each rate plan's cancellation cutoff (new field(s) on `rate_plans`: e.g. free-cancellation-until-N-hours-before-check-in, or non-refundable) and records which applied on the booking (fact only, no monetary action — Milestone 5's concern). Transitions to `CANCELLED`, releasing `booked_units` for every remaining night of the stay range via task 3's release path, in the same transaction. Idempotent per task 6's pattern. | Done | (uncommitted locally) |
| 10 | Booking projection/read model + full test suite | A read-optimized projection (view or denormalized table) joining booking + guest + room type + rate plan for dashboard/admin consumption, tenant/property-scoped. Full unit + integration test suite covering every state transition in ADR-0014's table, including failure/edge states (`AVAILABILITY_FAILED`, `PAYMENT_FAILED` placeholder path, `PMS_UNKNOWN_RESULT`, `PMS_REJECTED`, `MANUAL_REVIEW`, `EXPIRED`), run against real Postgres/Redis, not mocks. | Done | (uncommitted locally) |
| 11 | Store the guest's full billing address | **Reopened 2026-07-31** — while retrofitting the WordPress plugin (Milestone 6), guest-facing checkout collects street address, address line 2, city, county/state, and postcode, but `guests` (task 7 of this milestone) only has `email`/`phone` — there is nowhere for the API to store them, so the plugin currently collects them for nothing. Add nullable columns for these fields to `guests` (not a new table — same guest-matching row, informational like `phone`), accept them in `CreateBookingCommand`'s guest object, persist them on guest create/lookup (update-in-place on an existing exact-email match, matching ADR-0015's spirit — do not create a second guest row for an address-only change), and return them from any endpoint that already returns guest data to its owner (e.g. `GET public/bookings/:id`'s guest projection, if/when one exists). No change to the exact-email matching rule itself. Tests: creating a booking with full address fields persists and round-trips them; omitting them still works (all optional); an existing guest's address is updated by a later booking with different address fields. **Done, independently verified (2026-07-31).** Migration (`20260731150000_guest_billing_addresses`) adds 5 nullable columns to `guests`; `GuestDetails` (domain-contracts) and `booking.controller.ts`'s `createInput()` correctly parse them; `resolveGuest` (`local-pms.provider.ts`) correctly distinguishes "field omitted" (leave existing value) from "field provided" (update) via `!== undefined` `CASE WHEN` clauses on both the update and insert paths, both tenant-scoped (`WHERE ... AND tenant_id = ...`); staff booking projection exposes all 5 fields; WordPress's `confirmation-page.php` Review & Payment form now actually sends them instead of collecting them for nothing. **Found and fixed one real gap myself**: the controller's `createInput()` mapped guest address fields as `typeof x === 'string' ? x : undefined` — collapsing an explicit JSON `null` into `undefined`, so a guest submitting a blank address field could only ever add data, never clear a previously-stored value from an earlier booking. The domain-layer test (`local-pms-provider.e2e.spec.ts`) didn't catch this because it calls `provider.createBooking()` directly, bypassing the HTTP controller's body-parsing entirely. Fixed with a small `optionalNullableString()` helper (string → value, `null` → `null`, anything else → `undefined`), then independently verified against the real running API and database (not just re-reading the fix): created a booking with `county: "OriginalCounty"`, confirmed stored; a second booking for the same guest with `county` omitted entirely, confirmed unchanged; a third with `county: null` explicit, confirmed cleared to `NULL` — all three checked directly against the database, not just the HTTP response. Full suite re-run after the fix: 26/26 files, 50/50 tests, lint, format (fixed 9 files' pre-existing formatting drift with `prettier --write`, unrelated to this task but caught by the same `format:check` pass), build, and migration status all clean. PHP syntax-checked directly (`php -l`) since Codex had no PHP CLI available to verify their own change. | Done | (uncommitted locally) |
