# Milestone 3: Property, Room & Rate Management (Local)

Status: Done
Depends on: Milestone 2

## Goal

Tenant staff can configure their hotel's sellable inventory entirely locally — no PMS involved yet. Room types, physical rooms, rate plans, and availability exist and are manageable through the admin UI. Done means: staff can create a room type, set a rate, and see it reflected in an availability view, purely on local data.

## Draft task areas (not final — define the real 10 tasks at kickoff)

1. Schema: `room_types`, `rooms` (physical inventory units), `rate_plans`, `rate_rules`, `amenities` — independent of any PMS, per `docs/ARCHITECTURE.md`'s catalog model.
2. CRUD API for room types and physical rooms, tenant/property-scoped.
3. CRUD API for rate plans and date-based rate rules (seasonal/weekday overrides).
4. Admin UI: room type/room management screens.
5. Admin UI: rate plan management and a calendar-style rate override view.
6. Image/media upload for room types (object storage per `ARCHITECTURE.md`).
7. Amenities management (simple tagging on room types).
8. Local availability model: `inventory_units` and a query for "is room type X available for date range Y."
9. Validation rules (no overlapping rate rules, sane date ranges, currency handling per `docs/decisions/ADR-0009` — NUMERIC/minor units, no floats, per the source Clock brief's database-constraints guidance).
10. Test coverage for the availability query, including edge cases (single-night, date-range overlaps, zero inventory).

## Kickoff decisions (2026-07-28)

- Resolved the carried-forward Milestone 2 note: ADR-0006 requires onboarding to support adding a second/third property, not just the signup wizard — this milestone (the property-shape milestone) is the right home. A new Task 1 covers create/list + `max_properties` enforcement + the deferred "Upgrade to unlock more" prompt.
- To make room for it at 10 tasks: image/media upload for room types (originally its own draft area) is merged into the room-type CRUD task rather than dropped.
- Draft area 9's citation of `docs/decisions/ADR-0009` for currency handling is stale/incorrect — that ADR is about tenant data retention after cancellation, unrelated. Currency-as-`NUMERIC`/minor-units is already an existing convention in `AGENTS.md`'s database rules, not a separate ADR.

## Explicitly not included

- Any Clock/PMS-backed inventory (Milestone 10).
- The actual booking/checkout flow (Milestone 4-6).

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | Add-property API + create/list UI: create an additional property (name/address/timezone, same shape as signup) and list a tenant's properties; enforce `plans.max_properties` (409 at cap) with an "Upgrade to unlock more" prompt in the dashboard once reached. Resolves ADR-0006's onboarding requirement and the carried-forward Milestone 2 note. **Blocked in review by Codex (2026-07-28), resolved via new ADR-0010:** the dashboard had no tenant-selection model for a user who belongs to more than one tenant. Decision: tenant ID lives in the dashboard URL (`/dashboard/:tenantId/...`), not server-side session state — see `docs/decisions/ADR-0010-dashboard-tenant-routing.md`. This task's UI must live at that route shape; `/dashboard` itself becomes (at least minimally) a tenant picker linking into it. **Three required follow-ups found in review, must land before Done:** (1) No automated test coverage at all for the new `properties.controller.ts`/`properties.service.ts` or `/auth/memberships` — API test count is unchanged from before this task (16 files/34 tests); add tests proving `max_properties` enforcement (409 at cap), owner/admin-only + verified-email gating on create, tenant isolation on list/create, `property.created` audit logging, and the memberships endpoint. (2) The new `auth_list_user_tenants` SECURITY DEFINER function is missing `REVOKE ALL ... FROM PUBLIC` / `GRANT EXECUTE ... TO must_booking_app` (confirmed via `\df+` that PUBLIC retains default EXECUTE) — it takes an arbitrary `p_user_id` and deliberately bypasses RLS, so this must match the same-pattern hygiene every other SECURITY DEFINER function in this codebase already follows. (3) `pnpm format:check` fails across all 7 touched/new files (backend and frontend written as unformatted single-line code) — run `pnpm format` and confirm a clean re-check; report `lint`/`format:check` results going forward. All three landed and independently re-verified: `properties.e2e.spec.ts` proves the full flow (memberships, verified-email gating, owner/admin creation, tenant isolation, plan-cap 409, staff-role rejection, audit log) in 17 files/35 tests; `auth_list_user_tenants`'s privileges confirmed via `\df+` (PUBLIC revoked, only `must_booking`/`must_booking_app`); lint and format:check both clean. | A tenant can create a second property via the dashboard; the action is rejected at the API layer once `max_properties` is reached, with the UI showing the upgrade prompt instead of a silent failure or generic error; the property UI lives at `/dashboard/:tenantId/...` per ADR-0010; the new behavior is proven by automated tests, not just manual/build checks. | Done | (uncommitted locally) |
| 2 | Schema: `room_types`, `rooms` (physical inventory units), `rate_plans`, `rate_rules`, `amenities` — independent of any PMS, per `docs/ARCHITECTURE.md`'s catalog model. | Migration applies cleanly; every table carries `tenant_id`/`property_id` per `docs/TENANCY.md`. | Done | `20260728050000_local_catalog_schema` |
| 3 | CRUD API for room types and physical rooms, tenant/property-scoped, including image/media upload for room types. **Blocked in review by Codex (2026-07-28), resolved via new ADR-0011:** no object storage provider, interface, bucket/key convention, credentials, or access model existed. Decision: Cloudflare R2, public-read bucket, presigned-upload-only, behind a new `StorageProvider` interface (mirrors `MailProvider`) — see `docs/decisions/ADR-0011-object-storage-provider.md` for the full spec (key convention, size/type policy, env vars). **Second blocker resolved (2026-07-28):** R2 credentials are provider credentials like `RESEND_API_KEY`, not boot-required like `WEB_APP_URL` — `StorageProvider` fails per-call when unconfigured (mirroring `ResendMailProvider`), so the existing test/dev bootstrap is unaffected. `.env.example` placeholders added. Tests must mock `StorageProvider` (same pattern as `MAIL_PROVIDER`) — no real R2 credentials needed for the suite to pass, and none should be requested for that purpose. **Completed directly by Claude at the owner's request (2026-07-28)**, since Codex's scaffold (`room-types.controller.ts`) had drifted from the project's controller/service pattern (logic in the controller, a hardcoded `list()` stub, no persistence on upload, a bare `throw new Error` instead of an HTTP exception) and the owner asked for it to be finished directly rather than round-tripped further. Implemented: `RoomTypesService`/`RoomTypesController` (CRUD + image upload-authorization + image listing) and `RoomsService`/`RoomsController` (CRUD, nested under room type), both mirroring `PropertiesService`/`PropertiesController`'s established shape — tenant/property-scoped via `TenantScoped`, owner/admin + verified-email gated for mutations, reads open, unique-name and foreign-key-violation DB errors translated to clean 409s, every mutation audit-logged. Image upload validates content-type (`image/jpeg`/`png`/`webp`) and size (≤10MB) per ADR-0011 before requesting a presigned URL, then persists a `room_type_images` row; deleting a room type with existing rooms/images/rates correctly 409s via the FK-violation translation. New `room-types.e2e.spec.ts` and `rooms.e2e.spec.ts` cover unverified-blocked, RBAC (staff rejected), tenant isolation, duplicate-name conflict, room-type-not-found on a mismatched nested ID, image upload validation and persistence, delete-blocked-while-referenced, and audit logging — both using a mocked `StorageProvider`, no real R2 credentials. Full suite: 19 files/37 tests passing; lint, format, and workspace build all clean. | Room types and rooms can be created/read/updated/deleted, scoped to the caller's tenant and property; uploaded images are retrievable and tenant-scoped; upload requires a presigned URL issued only to an authorized (owner/admin, verified) caller for their own tenant/property, per ADR-0011; the test suite passes without any real R2 credentials. | Done | (uncommitted locally) |
| 4 | CRUD API for rate plans and date-based rate rules (seasonal/weekday overrides). | Rate plans and rules can be created/read/updated/deleted; overlapping rules for the same room type/date range are rejected (ties into Task 9's validation). **Note (2026-07-28):** the overlap check (`rejectOverlap`) gets a small additive update as part of Task 6, per ADR-0012 — a base rule (`NULL` dates) is never checked against dated overrides. This was correctly out of scope when Task 4 was originally built (no base-rate concept existed yet); it's an extension, not a defect being fixed. | Done | (uncommitted locally) |
| 5 | Admin UI: room type/room management screens (including image upload). | Staff can create a room type, upload an image, and manage physical rooms through the dashboard. | Done | (uncommitted locally) |
| 6 | Admin UI: rate plan management and a calendar-style rate override view. **Blocked in review by Codex (2026-07-28), resolved via new ADR-0012:** Task 2's `rate_rules` were always date-bounded and Task 4's overlap check assumed that; there was no way to express an ongoing "base rate" distinct from a seasonal override. Codex's own first proposal (a full-calendar-date-range rule standing in for "base rate") was correctly identified as broken — it would make Task 4's overlap check reject every real override, permanently. Decision: a `rate_rules` row with both `starts_on`/`ends_on` set to `NULL` is the base rate for its `(rate_plan, room_type)` pair (at most one, enforced by a partial unique index); a row with both dates set is a dated override; a base rule never conflicts with a dated override. Base rules must cover all 7 weekdays (no partial-week default). See `docs/decisions/ADR-0012-rate-plan-base-rate-model.md` for the full schema/logic spec. This task must land the additive migration (nullable date columns + relaxed check constraint + partial unique index) and the `rejectOverlap` update alongside its own UI work — **landed correctly**: migration, `rejectOverlap` update, and `rate-plans.e2e.spec.ts` all verified (base rate created, duplicate base rate 409s, partial-weekday base rate 400s, a dated override still creates successfully alongside an existing base rate). **Required follow-up found via live browser testing, must land before Done:** `rate-management.tsx`'s `submitRatePlan`, `submitBaseRate`, and `submitOverride` each call `event.currentTarget.reset()` *after* `await fetch(...)` — React nullifies a synthetic event's `currentTarget` once the handler yields past an await, so this throws `Cannot read properties of null (reading 'reset')` on every successful submission, confirmed live (the POST succeeds and the row lands in the DB, but the crash happens before the subsequent `loadPropertyData()`/`loadRules()` call, so the UI never reflects the change — no way to reach "Manage selected rate plan" after creating the first plan without a manual page refresh). Fix: capture `const form = event.currentTarget;` *before* the `await`, then call `form.reset()` afterward (matching how `room-management.tsx`'s equivalent handlers avoid this entirely by resetting controlled-input state instead of calling `.reset()` on the event target). **Fix landed and verified live (2026-07-30):** all three handlers now capture `form` before the `await`; confirmed in a real browser end-to-end — rate plan create (form resets, plan selected, "Manage selected rate plan" appears), base rate set (`Standard Room: 100.00` shown), and a dated override added afterward (both the base rate and the override coexist and display correctly), with no console/page errors at any step. | Staff can set a base rate and see/adjust date-specific overrides in a calendar view; a base rate coexists with any number of non-overlapping-with-each-other dated overrides for the same room type; creating a rate plan, base rate, or override does not crash the page. | Done | (uncommitted locally) |
| 7 | Amenities management (simple tagging on room types). | Staff can tag a room type with amenities and see them reflected on the room type. | Done | (uncommitted locally) |
| 8 | Local availability model: `inventory_units` and a query for "is room type X available for date range Y." | The availability query returns correct results against local inventory data, independent of any PMS. | Done | `20260728080000_local_availability` |
| 9 | Validation rules: no overlapping rate rules, sane date ranges, currency as `NUMERIC`/minor units (existing `AGENTS.md` convention, not ADR-0009 — see kickoff note above). **Confirmed via new tests, no new code needed:** invalid currency, backwards date ranges, half-bounded rules (ADR-0012), duplicate weekdays, over-precision decimals, and non-string JSON amounts were already rejected by Tasks 4/6's implementation — this task added the explicit test coverage locking that behavior in. | Overlapping rate rules and invalid date ranges are rejected at the API layer; monetary fields use `NUMERIC`/minor units, never floats. | Done | (uncommitted locally) |
| 10 | Test coverage for the availability query, including edge cases (single-night, date-range overlaps, zero inventory). | Automated tests cover the edge cases listed, not just the happy path. | Done | (uncommitted locally) |
