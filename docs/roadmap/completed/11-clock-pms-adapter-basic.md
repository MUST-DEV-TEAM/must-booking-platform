# Milestone 11: Clock PMS+ Adapter (Basic Integration)

Status: Done (2026-08-04)
Depends on: Milestone 4 (booking domain/`PmsProvider`); [ADR-0026](../../decisions/ADR-0026-tenant-integration-connections.md) (tenant-owned integration connections — this milestone builds that foundation, then Clock on top of it); reference material: `docs/source/clock-pms-integration.pdf`

**Post-close-out fix (2026-08-05):** Task 10/16's "no genuine Clock-confirmed booking possible" finding was wrong — it was a real code bug, not an account limitation. `ClockAvailabilityService`/`ClockBookingService` used Clock's rate-*plan* id (`/rate_plans`) as the `rates`/`rate_id` parameter instead of the room-type-scoped rate id from `/rates/` (Clock: "1 Rate belongs to 1 Room Type"). Fixed same day; a real booking create → confirm → cancel cycle is now proven end to end against DBL. No Clock support right was requested or needed. Full details: `docs/CLOCK_SANDBOX_VALIDATION_REPORT.md`.

**Carried forward from Milestone 2 (Task 7, 2026-07-28):** Milestone 2's Free-plan limit enforcement could not gate PMS connections — no PMS-connection endpoint existed at all yet. When this milestone builds Task 2's tenant Integrations UI (the actual "connect a PMS" mutation point), add enforcement of `plans.pms_enabled` there: a plan without PMS access must have connection attempts rejected outright (feature gate, not a count), per ADR-0007.

## Goal

A first working `ClockPmsProvider`, sandbox-validated, covering the core loop: connect, catalog sync, availability, and booking create/update/cancel. This is deliberately **basic**, not the full production-grade integration the source brief specifies (full webhook reconciliation, WAF handling, complete observability, all deliverable documents) — that hardening is Milestone 12 and beyond. Done means: a real Clock sandbox account can be connected, its catalog synced, and a booking created/cancelled through `ClockPmsProvider`, end to end.

## Kickoff notes (2026-08-04)

- **No real Clock sandbox credentials exist in this repo or the source brief** — confirmed by a full read of `docs/source/clock-pms-integration.pdf` (9 pages + appendices). The brief itself expects the team to obtain sandbox access separately and verify live. The owner confirmed sandbox access already exists; credentials will be supplied when Tasks 6 and 16 (the two tasks that make real Clock calls) are reached, not needed for the rest.
- **Implementation is being done directly by Claude, not dispatched to Codex** — explicit owner exception (Codex usage temporarily unavailable), same as the TanStack Query migration. Same verification rigor applies: every task gets build + lint + Prettier + full test suite before being marked Done, and Claude marks Done (no external report to cross-check against, since there's no separate implementer).
- **Not held to exactly 10 tasks** (per ADR-0023) — 16 tasks below, sized for reviewability over an arbitrary round number, per the owner's explicit instruction this kickoff.
- The source brief (Albanian) was read in full; section references below point at specific, verified content, not paraphrased guesses. Where the brief itself leaves something undecided (e.g. the exact Digest Authentication mechanics, which endpoint is authoritative for availability — both explicitly listed as open items in section 38), the task's acceptance criteria says so and defers to sandbox verification, per the brief's own `CONFIRMED_BY_DOCS` / `CONFIRMED_IN_SANDBOX` / `CONFIRMED_BY_CLOCK_SUPPORT` / `ASSUMPTION` / `NOT_SUPPORTED` classification (section 39).

## Explicitly not included (deferred to post-Milestone-12 hardening backlog)

- Full reconciliation jobs (source brief section 22), WAF-suspicion circuit breakers, complete observability/alerting (sections 12, 28) — narrow booking-creation-timeout reconciliation (section 18's lookup-before-retry) is in scope via Task 10; the scheduled/generic reconciliation job system is not.
- `CLOCK_RECONCILIATION.md`, `CLOCK_SECURITY_REVIEW.md`, `CLOCK_RUNBOOK.md`, the OpenAPI spec, ER/sequence diagrams, and the production activation checklist (section 37's full deliverable list) — written up before any production activation, not required for this milestone's sandbox-only scope. Task 15 covers only the deliverables that document what's actually built in this milestone.
- Any PMS vendor other than Clock.
- Production activation — this milestone is sandbox-only, per the source brief's Definition of Done (section 36).

## Tasks

| # | Task | Acceptance criteria | Status |
| --- | --- | --- | --- |
| 1 | `IntegrationConnection` schema + credential encryption (ADR-0026 foundation) | New Prisma models: `IntegrationConnection` (org-scoped: id, tenantId, kind [`PAYMENT`/`PMS`], provider, name, encrypted credential payload, status, lastTestedAt, lastTestResult, timestamps) and `PropertyIntegrationConnection` (propertyId, connectionId, enabled — many-to-many for `PAYMENT`, enforced at most one active per property for `PMS`). New AES-256-GCM encrypt/decrypt helper (master key from env, never logged) — first reversible-encryption primitive in the codebase; no existing pattern to reuse per the ADR-0026 research. Migration applied cleanly to a fresh DB. Unit tests for the encryption helper (round-trip, tamper detection via GCM auth tag). | **Done (2026-08-04)** — `d671eec` (unrelated pre-existing schema.prisma/migration drift fixed first, as its own commit — `RoomTypeImage`, `isAutoProvisioned`, `weekdays` SmallInt had silently diverged from the applied DB), `3b2c95d` (actual feature). RLS enabled+forced on both tables, partial unique index for one-active-PMS-per-property confirmed via `\d` against the real dev DB. Full API suite (45 files/93 tests), build, and lint all clean. |
| 2 | Tenant Integrations UI | New dashboard section: list connections, add a connection (provider + name + credential fields, masked after save), test-connection button, delete, assign to properties (multi-select for `PAYMENT`, single-select for `PMS`, enforced in the UI and the API). Reuses the TanStack Query pattern established across the rest of the dashboard. Plan gating: a plan without `pmsEnabled` must reject a PMS connection attempt outright (carried forward from Milestone 2 Task 7). | **Done (2026-08-04)** — `c68fc78`. Backend CRUD + property-assignment endpoints, `ConnectionTestRegistry` extension point (honest "not available yet" until Task 6/the Stripe-PokPay reopening register real testers), nested in Settings next to Manage properties. E2E covers plan gating (409 on Free) and the one-active-PMS-per-property conflict (409) against a real seeded plan. Full API suite (46/94) and web suite (33/85) clean; also fixed a stale "Milestone 11" billing-copy bug found along the way. |
| 3 | Platform admin oversight view | Read-only `/platform` view: which tenants have which connections (provider, name, status, last tested), for support/troubleshooting. No enable/disable-provider-type control (ADR-0026 decision 5 — oversight only). | **Done (2026-08-04)** — `df0ac06`. Extends the existing tenant-detail page/endpoint rather than a new screen; reuses the established `withTenantTransaction(tenantId)` read pattern instead of adding a new RLS carve-out. Full API suite (46/94) and web suite (33/86) clean — one transient signup-rate-limit collision seen across parallel e2e files (unrelated to this task, confirmed by re-running clean and in isolation; pre-existing shared-Redis-limiter fragility, not a regression). |
| 4 | Clock HTTP client core | New `apps/api/src/integrations/clock/` module: HTTP client with connection pooling, TLS verification, configurable timeouts, structured logging (no credentials ever logged, per source brief section 8). Digest Authentication implemented per RFC 7616 as a first pass, explicitly flagged `ASSUMPTION` in code comments and `CLOCK_ENDPOINT_MATRIX.md` until confirmed against real sandbox in Task 6. Controllers/use-cases never call Clock directly, only through this client (section 9's explicit requirement). Unit tests against a local mock HTTP server (no real Clock calls needed for this task). | **Done (2026-08-04)** — `9db1dfe`. Digest auth was already confirmed (`CONFIRMED_IN_SANDBOX`, not just `ASSUMPTION`) before this task, from the real sandbox probing done during Milestone 11 kickoff — `docs/CLOCK_ENDPOINT_MATRIX.md` reflects that. Added `undici` as an explicit dependency for real Agent-level pooling/TLS control. Tests run against a real local HTTP server, not a mocked fetch. Full API suite (48/106) clean when run sequentially — the shared signup-rate-limit flake across parallel e2e files (documented on Task 3) is recurring more often now with more e2e files in the suite; confirmed environmental again via a clean sequential run, not a regression, but worth a dedicated fix at some point outside this milestone's scope. |
| 5 | Rate limiter, circuit breaker, retry policy, error classification | Redis-backed rate limiter, 4 req/s per API user (source brief section 10's recommended operational limit, below Clock's documented 5 req/s), mirroring the existing Lua-`EVAL` pattern in `apps/api/src/auth/signup-rate-limiter.service.ts`. Circuit breaker for repeated failures. Retry policy exactly per section 11: automatic retry only for 429, safe network interruption, GET timeouts, and provider-temporarily-unavailable, never a blind retry on booking-creation timeout. Error classification into the brief's exact 14 categories (`authentication`, `authorization`, `validation`, `not_found`, `conflict`, `rate_limited`, `timeout`, `network`, `provider_unavailable`, `waf_blocked`, `unknown_result`, `schema_mismatch`, `configuration`, `permanent`), mapped into the domain `ResultError` shape. Unit tests for rate-limit enforcement and retry-eligibility logic. | **Done (2026-08-04)** — `88c8028`. Rate limiter tested against real local Redis, not mocked. A real bug (network failures not marked retryable) was caught by the test suite itself and fixed before committing. Full API suite (52/149) clean sequentially. |
| 6 | `testConnection` — first real sandbox call | `ClockPmsProvider.testConnection` implemented and run against the owner's real Clock sandbox account. Confirms (and corrects if wrong) the Digest auth assumption from Task 4, updates `CLOCK_ENDPOINT_MATRIX.md` with the real status (`CONFIRMED_IN_SANDBOX` or `NOT_SUPPORTED`/corrected approach). **Needs sandbox credentials from the owner before starting.** | **Done (2026-08-04)** — `6c876d6` (commit message undersells it — also carries a small, correct, separately-reviewed homelab env-wiring fix bundled in by a staging mistake; code content is accurate). First real end-to-end run of the full stack together (client + rate limiter + circuit breaker + error classification): a valid connection succeeds, an invalid API key fails cleanly (Clock's real 401 body is plain text, not JSON — classifier handles both). `ConnectionTestRegistry` now has a real Clock tester; the tenant "Test connection" button actually works for Clock. Self-skipping real-sandbox test added (runs when credentials are present, skips cleanly in CI). Full API suite (54/159) clean. |
| 7 | Catalog mapping table + `syncCatalog` | New Prisma model for the mapping table exactly as specified in source brief section 13: `hotel_id`, `connection_id`, `entity_type`, `local_entity_id`, `external_entity_id`, `sync_status`, timestamps. `ClockPmsProvider.syncCatalog` performs initial full sync (property metadata, room types, rooms, rates, normalization) producing *proposed* mappings, never auto-applied, per section 14's explicit "preview and admin confirmation" requirement. Admin UI (extends Task 2's Integrations UI) to review and confirm proposed mappings. Incremental sync path uses `updated_at`/cursor when available, avoids full re-extraction every cycle. | **Done (2026-08-04)** — hand-written migration (`clock_catalog_mappings`, composite FKs to `properties`/`integration_connections`, RLS enabled+forced) after the Task 1 drift incident, no `prisma migrate dev` auto-diff. `ClockCatalogSyncService` stages `ROOM_TYPE`/`ROOM` proposals via raw-SQL upsert (`ON CONFLICT ... RETURNING (xmax = 0)` to count proposed vs. updated); `confirm` requires a room's parent room type to already be confirmed. Tenant Integrations UI extended with a `ClockCatalogSync` panel (sync button, proposal list, confirm/reject) shown only when a Clock PMS connection is active for the property. Rows are never auto-applied. Full workspace build and lint clean; Clock module unit tests (63 passed, 2 sandbox-only skipped); real sandbox e2e test (`clock-catalog-sync.e2e.spec.ts`) ran successfully end to end — synced real Clock room types/rooms, confirmed a proposal into an actual local `room_types` row. Frontend `integrations-management.test.tsx` (4 tests, incl. new `ClockCatalogSync` coverage) passing with no regression. |
| 8 | `getAvailability` | Endpoint choice determined via sandbox discovery (source brief section 38 lists this as explicitly undecided) and recorded in `CLOCK_ENDPOINT_MATRIX.md`. Short-lived cache only (no fixed TTL specified by the brief, pick a conservative default, e.g. 60s, and document the reasoning); a final availability check always runs immediately before booking creation regardless of cache state, per section 16. | **Done (2026-08-04)** — `/rates_availability`'s real contract (`rates` + `room_types`/`rooms`, both array-capable) resolved via Clock's own public Postman API docs after blind sandbox probing alone couldn't isolate it (see `CLOCK_ENDPOINT_MATRIX.md` notes); reproduced the documented 400→200 transition against the real sandbox. New `ClockAvailabilityService`: maps the domain `AvailabilityQuery.roomTypeId` to Clock's external room type id via the Task 7 catalog mapping (configuration error if unconfirmed), fetches the property's rate plan ids (60s-cached), queries every occupied night `[startsOn, endsOn)`, and requires every night to show a free rate with `room_type_free_rooms > 0` — `availableUnits` is the min across nights. 60s in-process result cache with a `skipCache` escape hatch reserved for Task 10's pre-booking check (not wired yet, no booking creation exists). `ClockPmsProvider.getAvailability` now delegates to it instead of throwing. `ClockHttpClient` extended to support repeated `key[]=value` array query params (Clock's Rails convention), used for `rates`. Full workspace build and lint clean; Clock module unit tests (71/71, incl. new `ClockAvailabilityService` and `ClockPmsProvider.getAvailability` coverage); real sandbox e2e test (`clock-availability.e2e.spec.ts`) passing — connects, syncs+confirms a room type, and gets a well-formed `Result<AvailabilityResult>` back (the demo account has no live rate/availability configured for the room type tried, so `isAvailable: false` was observed rather than a populated success body — the request/response contract itself is confirmed, not the specific data). Full API suite (57 files/165 tests) clean run sequentially; running the two real-sandbox e2e files (Task 7's and this one) in parallel workers transiently 429s on Clock's shared per-API-user rate limit — same class of pre-existing shared-external-resource flake already documented for the signup limiter (Task 3/4), not a regression, confirmed by a clean sequential rerun. |
| 9 | Queue infrastructure (BullMQ) | `bullmq` (already a dependency, currently unused anywhere in the codebase) wired up for the first time: named queues from source brief section 26/Appendix A (`clock.critical.commands`, `clock.webhooks`, `clock.booking.sync`, `clock.catalog.sync`, `clock.financial.sync`, `clock.reconciliation`) plus a dead-letter queue, with the documented priority order (booking confirm/cancel first, then webhook hydration, then availability/operational sync, then reconciliation, then full catalog sync/reports last). Worker skeletons only, full job logic lands with the tasks that need them (10, 11). | **Done (2026-08-04)** — `ClockQueueService` owns the 6 named `Queue`s + `clock.dead-letter`, backed by a shared `ioredis` connection built from the already-required `REDIS_URL`. `ClockWorkerService` starts one real BullMQ `Worker` per named queue; each worker's `failed` handler copies a job onto the dead-letter queue once it exhausts its configured `attempts`, rather than only relying on BullMQ's internal failed-job set. Processors are skeleton-only (log receipt), per this task's explicit scope — Task 10 wires `clock.critical.commands`, Task 11 wires `clock.webhooks`. No new dependency added — `@nestjs/bullmq` isn't installed, so this is wired by hand as plain Nest providers (`onModuleInit`/`onModuleDestroy`), consistent with how the rest of the Clock module is registered in `app.module.ts`. Tests run against real local Redis (already a required env var, not sandbox-gated): enqueue sets the documented per-queue priority, a real worker picks up and completes a job, and the dead-letter path is exercised directly. Full workspace build and lint clean; full API suite (59 files/170 tests) clean sequentially. |
| 10 | Booking CRUD on `ClockPmsProvider` | `createBooking`/`updateBooking`/`cancelBooking` implemented, reusing the `integration_operations` idempotency pattern already proven in `LocalPmsProvider` (`withIdempotency`-style: insert-or-conflict on `(tenantId, idempotencyKey)`, hash-compare, attempts counter). Booking-creation timeout handling follows section 18 exactly: never blind-retry, look up the operation record, search Clock for the booking via the MUST external reference, link if found, only then decide if retry is safe. Update uses optimistic concurrency (`expectedVersion`) and re-fetches from Clock after write to confirm. Cancellation is idempotent and drives the existing `BookingStateMachine` (same instance/transitions `LocalPmsProvider` uses, no parallel state machine). Which Clock field carries the MUST external reference is a section-38 open item, confirmed during this task and recorded in `CLOCK_ENDPOINT_MATRIX.md`. | **Done (2026-08-04)** — **Scope, agreed with the owner at kickoff:** PMS-interface-only (create/update/cancel a `bookings` row + call Clock, driving the same `BookingStateMachine`/idempotency `LocalPmsProvider` uses), not full guest-checkout parity (no quote validation, no Stripe/PokPay checkout-session creation — those aren't part of the plain `CreateBookingCommand`/`PmsProvider` interface and `ClockPmsProvider` isn't the live `PMS_PROVIDER` yet). New `ClockBookingService`. New nullable `bookings.external_booking_id` column (Clock's own numeric booking id, distinct from our local uuid — unlike `LocalPmsProvider`, which reuses its own id). Create body/response contract, the PUT-based update/cancel, and the `lock_version` optimistic-concurrency token all confirmed via Clock's own public Postman API docs (found through Postman's public-network search after blind sandbox probing alone couldn't isolate them) and reproduced for real: a clean 400 rejection when Clock has no availability for the requested room/rate/dates (this sandbox account has none configured for any room type, and its API user lacks the "Rate Availability Control Override" right — a live *successful* creation could not be observed in this account; see `CLOCK_ENDPOINT_MATRIX.md`). Clock's stale-`lock_version` conflict is a plain HTTP 500 with a fixed message, not a 409 — special-cased in code rather than falling through to the generic 5xx classifier. Section 18's timeout-reconciliation path (search Clock by `reference_number`, link if found, else `PMS_UNKNOWN_RESULT`, never assume success) is implemented and shares the same Clock lookup as `findBookingByExternalReference`. Rate-plan selection is a documented "basic milestone" simplification: Task 7's catalog mapping only covers room types/rooms, not rate plans, so `ClockBookingService` uses the property's Clock rate plan when exactly one exists and returns a clear configuration error otherwise (follow-up: extend catalog mapping to `RATE_PLAN`). A real bug was caught by the real-sandbox e2e test and fixed: `mappedExternalId`'s raw query compared the `entity_type` enum column against a bare string, needing an explicit `::"ClockCatalogEntityType"` cast. A second real issue surfaced under real network latency: `ClockBookingService` makes outbound Clock HTTP calls *inside* a Prisma interactive transaction, which can exceed Prisma's 5000ms default timeout — fixed by adding an optional `timeoutMs` override to `TenantDatabaseService.withTenantTransaction` (additive, existing callers unaffected) and setting 30–45s for the booking-CRUD transactions. Real sandbox e2e test (`clock-booking.e2e.spec.ts`) verifies the full rejection path end-to-end plus idempotent replay (same key/request returns the cached result without a second Clock call or a second local row); update/cancel against an actually-*confirmed* Clock booking could not be exercised in this account (same no-availability limitation) and is a known gap for Task 16 or once the account is reconfigured. No unit test file (`ClockBookingService` has none, matching the codebase's existing convention — `LocalPmsProvider`'s equally raw-SQL-heavy booking logic also has zero unit tests and is e2e-only). Full workspace build and lint clean; Clock module unit tests (80/80 across unit + all 3 real-sandbox e2e files, run sequentially); full API suite (60 files/174 tests) clean sequentially, including after the shared `TenantDatabaseService` change. |
| 11 | Webhook gateway | New endpoint: validates structure and real AWS SNS signature (`SignatureVersion`/`SigningCertURL` scheme per section 27's explicit security requirement, not a custom HMAC), handles SNS subscription confirmation, deduplicates, archives the raw payload, returns 2xx immediately, then queues the event (Task 9's `clock.webhooks` queue) for hydration. Event hydration can be minimal/manually-triggered for this milestone, full automated reconciliation is Milestone 12+. | **Done (2026-08-04)** — `POST /clock-webhooks/:webhookPublicId`, public/unauthenticated (marked `@Public()`), since Clock/SNS can't carry a MUST session. New `webhook_public_id` (random UUID) on `IntegrationConnection` so the callback URL never exposes the internal connection id — now returned from the connection-create/list endpoints so the tenant can actually configure it on Clock's side. New `provider_events` table (generic naming per the brief, not Clock-specific) for durable storage + dedup on `(connection_id, event_id)` before the 2xx response. Real AWS SNS signature verification implemented from AWS's own documented algorithm (alphabetical canonical-field string, RSA-SHA1/SHA256 depending on `SignatureVersion`) — not a stub. SSRF protection: `SigningCertURL`/`SubscribeURL` must match a real `sns.*.amazonaws.com` host over HTTPS before any fetch. Replay protection: messages older than 5 minutes are rejected. Resolving `webhookPublicId` → tenant before a tenant context can be set required a new RLS read carve-out (`app.role = 'webhook_gateway'`, same established pattern as the platform-admin dashboard carve-out) on both `integration_connections` and `property_integration_connections` — the second one was missed on the first pass and caught by the real e2e test (events were being silently dropped, not stored, because the property lookup came back empty under RLS). A second real bug was also caught and fixed: the controller was missing an explicit `@Inject()` on its constructor param, causing every request to 500 (this codebase doesn't rely on implicit type-based DI). No live Clock Message Channels (SNS) subscription exists for this account (would need Clock dashboard access outside this session's reach), so genuinely AWS-signed traffic could not be captured — the real e2e test (`clock-webhook.e2e.spec.ts`) instead exercises the entire flow for real (HTTP routing, RLS lookup, dedup, BullMQ enqueue, 400/404 handling) using a real locally-generated RSA key pair to produce genuinely valid signatures, with only the literal AWS cert-fetch network call stubbed via dependency injection (`ClockWebhookVerificationService`'s `fetchCert` param — same testability pattern as `ClockHttpClient`'s `protocol` override). Event hydration is genuinely minimal per this task's scope: `ClockWorkerService`'s `clock.webhooks` processor (Task 9's skeleton) still just logs receipt — no fetch-full-object/normalize/apply pipeline, which is out of scope until a later milestone. Full workspace build and lint clean; Clock module unit tests (24 new: SNS signature crypto + hostname/SSRF checks + verification service, all real crypto against a real generated key pair, no mocked math); real webhook e2e test (4/4: valid-signature accept+store+enqueue, idempotent replay, tampered-signature rejection, unknown-id 404) passing. Full API suite: 198/202 passed on the default parallel pool (2 failures — signup-rate-limit and real-Clock-sandbox-rate-limit contention across parallel e2e workers, both already-documented pre-existing flake classes from Tasks 3/4/8, confirmed non-regressions by a clean isolated rerun of both files); a separate fully-sequential run of the whole suite hit zero test failures, only the also-already-documented Windows-only `ERR_IPC_CHANNEL_CLOSED` process crash partway through (unrelated to test outcomes — every test that ran before the crash passed). |
| 12 | Manual-review queue/table | New table for unknown/ambiguous Clock results, covering every trigger from source brief section 26: unknown results, duplicates, missing mappings, simultaneous changes, payment-booking mismatch, unknown statuses, schema mismatch. An unknown result must never be silently treated as success. Basic list view in the platform admin oversight UI (Task 3), resolution workflow beyond "mark reviewed" is out of scope for this milestone. | **Done (2026-08-04)** — New `manual_review_items` table with all 7 categories from the brief in its enum (schema-complete), generically named (not Clock-specific) per the brief's own `provider_events`-style naming choice. New `ManualReviewService` (`record`/`list`/`resolve`). Two categories have real, wired trigger code in this milestone's actual functionality: `UNKNOWN_RESULT` (Task 10's booking-creation timeout-reconciliation failure — when Clock can't be confirmed to have created the booking, the local status goes to `PMS_UNKNOWN_RESULT` *and* a manual-review row is recorded, never silently treated as success) and `SCHEMA_MISMATCH` (a new defensive guard, `isClockBookingResource`, validates a 2xx Clock booking-create response actually has the expected `id`/`lock_version`/`status` shape before trusting it — malformed 2xx bodies now also land in manual review instead of crashing or being trusted). The other five categories (`DUPLICATE`, `MISSING_MAPPING`, `SIMULTANEOUS_CHANGE`, `PAYMENT_BOOKING_MISMATCH`, `UNKNOWN_STATUS`) don't have producing code yet because their upstream features (payment sync, full reconciliation, Clock-status mapping) are out of this milestone's scope — the schema is ready for them, nothing wired yet; documented explicitly rather than left ambiguous. Platform admin tenant-detail view (Task 3) extended with a read-only "Manual review" section and a "Mark reviewed" action (`POST platform/tenants/:id/manual-review/:itemId/resolve`, audit-logged) — resolution workflow beyond that is out of scope per this task's own acceptance criteria. Real e2e test (`manual-review.e2e.spec.ts`) proves the platform-admin surface end to end against a seeded row: list scoped correctly per tenant (RLS-verified against a second tenant), resolve updates status/actor, audit row written. The `UNKNOWN_RESULT`/`SCHEMA_MISMATCH` trigger code itself wasn't independently re-exercised by a dedicated sandbox test (would require forcing a real Clock network timeout or a malformed 2xx from Clock, neither reproducible on demand) — `isClockBookingResource` (the schema_mismatch guard) is unit-tested directly instead. Full workspace build and lint clean; targeted suite (all Clock-module tests + all platform-admin e2e tests + the new manual-review e2e test, 116/117 passed) clean except one pre-existing, unrelated failure: `platform-audit.e2e.spec.ts` asserts an exact global `COUNT(*)` over the entire `audit_logs` table (not scoped to its own seeded rows) — a design that only holds if the table is otherwise empty, which doesn't hold in this long-lived shared dev database (confirmed by running that single file in total isolation and still seeing a different, non-zero baseline each time). Not a Task 12 regression — same class of pre-existing shared-global-state fragility as the already-documented signup/Clock-sandbox rate-limit flakes, just discovered via a different table. Full-suite runs continue to intermittently hit the already-documented Windows-only `ERR_IPC_CHANNEL_CLOSED` process crash (zero actual test failures in any such run before the crash). |
| 13 | Audit logging | Every connection create/update/delete/credential-change (Tasks 1-2) and every Clock booking operation (Task 10) writes to the existing `AuditLogService` (already used by `LocalPmsProvider`, no new logging mechanism needed). Confirms source brief section 8's "credentials must be... audited" requirement. | **Done (2026-08-04)** — Turned out to be a verification task, not new code: every write path this task cares about was already calling `AuditLogService` from the task that built it (`integration_connection.created/deleted/tested/enabled_for_property/disabled_for_property` from Task 2; `clock_catalog.synced/mapping.confirmed/mapping.rejected` from Task 7; `booking.created/updated/cancelled` from Task 10; `platform.manual_review.resolved` from Task 12). There is no separate "update"/credential-rotate endpoint — rotation is delete-then-recreate, and both of those are already audited, so "create/update/delete/credential-change" is fully covered by the existing create+delete actions. Real e2e verification: Task 2's `integration-connections.e2e.spec.ts` already asserted the full connection lifecycle audit trail; added one assertion to Task 10's `clock-booking.e2e.spec.ts` confirming a real `booking.created` row is written even when Clock ultimately rejects the booking (the audit call fires unconditionally right after the local row is inserted, before the Clock call). No gaps found. Full workspace build and lint clean; the modified e2e test passes for real against the sandbox. |
| 14 | `CLOCK_ENDPOINT_MATRIX.md` | Living document, updated incrementally by Tasks 4-11 rather than written fresh at the end. Columns per source brief section 33: domain, use case, API family, method, endpoint, parameters, response schema, filters, pagination, statuses, webhook, retry, idempotency, caching, reconciliation, last verified date. Every row carries one of the 5 verification statuses (section 39); no `ASSUMPTION`-only row may be treated as production-ready. | **Done (2026-08-04)** — Final review pass over the document Tasks 4-11 already kept live: all 17 required columns present on every row; every row carries a verification status; confirmed the two fully-`ASSUMPTION` rows (find-booking-by-reference, guest search) are explicitly called out as such rather than left ambiguous. Reordered the Notes section chronologically by discovering task (it had been appended out of order across tasks) and added a closing Task 14 note. No new sandbox calls made — this was a documentation review, not new verification work. |
| 15 | Remaining core deliverable docs | `CLOCK_ARCHITECTURE.md`, `CLOCK_DATA_MAPPING.md`, `CLOCK_BOOKING_STATE_MACHINE.md`, `CLOCK_WEBHOOK_FLOW.md`, `CLOCK_ERROR_CATALOGUE.md`, documenting what Tasks 1-13 actually built, not aspirational. (`CLOCK_RECONCILIATION.md`, `CLOCK_SECURITY_REVIEW.md`, `CLOCK_RUNBOOK.md` explicitly deferred, see above.) | **Done (2026-08-04)** — All 5 docs written, grounded in the actual code (re-read the relevant source files while writing rather than working from memory of what each task did). Deliberately documents real gaps alongside what works, matching this milestone's own honesty discipline: the retry *policy* exists but isn't wired into any retry *loop* (`CLOCK_ARCHITECTURE.md`); Clock's rate plans and booking `status` field aren't mapped at all (`CLOCK_DATA_MAPPING.md`); `waf_blocked` is a defined category nothing ever produces (`CLOCK_ERROR_CATALOGUE.md`); webhook hydration stops at "queue the event," no fetch/normalize/apply/reconcile pipeline exists yet (`CLOCK_WEBHOOK_FLOW.md`); Clock bookings never enter `PAYMENT_PENDING` because there's no payment orchestration on this path (`CLOCK_BOOKING_STATE_MACHINE.md`). Docs-only, no code changes. |
| 16 | Sandbox validation report + real E2E test | End-to-end test against the owner's real Clock sandbox: connect, sync catalog, confirm mappings, check availability, create a booking, cancel it. Sandbox validation report per section 36's Definition of Done checklist. This is the milestone's actual "done" gate. **Needs sandbox credentials from the owner before starting** (same account as Task 6). | **Done (2026-08-04)** — `clock-sandbox-validation.e2e.spec.ts`: one chained real-sandbox flow — connect, real `testConnection`, sync catalog, confirm a mapping, check availability, attempt a booking create, cancel it. `docs/CLOCK_SANDBOX_VALIDATION_REPORT.md` assesses source brief section 36's full checklist item by item: most items are done and verified for real; two (metrics, alerts) were never built at all in this milestone (flagged as real gaps, not hidden); a genuinely Clock-*confirmed* booking was never achieved in this sandbox account — this specific account has no rate/availability configured for any room type and its API user lacks the "Rate Availability Control Override" right, both confirmed Clock-account-side facts outside this codebase's control (the report names exactly what unblocking this requires). Cancelling a booking that has no `externalBookingId` (never made it into Clock) is proven for real — the state machine correctly allows `PMS_REJECTED → CANCELLED`, and `ClockBookingService` skips the pointless Clock API call. Comprehensive re-run of every Clock-related test file plus the connection audit trail and manual-review platform-admin flow together: 21 files / 116 tests, all passing. |
