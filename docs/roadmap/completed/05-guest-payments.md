# Milestone 5: Guest Payments

Status: Done (reopened 2026-07-31 for tasks 11-13, all now Done — re-closed 2026-07-31)
Depends on: Milestone 4

## Goal

Guests can pay for a local booking via Stripe Checkout, with server-side verified payment status, a payment ledger, and refund support — kept structurally and physically separate from platform billing (Milestone 11), per `docs/PROJECT_CONTEXT.md`'s domain-separation rule. Done means: a real (test-mode) Stripe payment can move a booking from payment-pending to confirmed, verified server-side, not by trusting the browser return.

## Draft task areas (not final — define the real 10 tasks at kickoff)

1. `PaymentProvider` interface (distinct from `BillingProvider`, per `docs/ARCHITECTURE.md`).
2. `StripePaymentProvider`: Checkout session creation for a booking.
3. Stripe webhook handling: signature verification, idempotent event processing, server-side payment confirmation (never trust browser return, per the source brief's booking/payment integrity principle).
4. Payment ledger schema (`payments` table), immutable/append-only for verified events, NUMERIC/minor-units for money (source brief section 34).
5. Linking payment confirmation to the booking state machine (Milestone 4) — payment-pending → confirmed transition.
6. Refund flow: staff-initiated refund, Stripe refund API call, ledger update.
7. Pay-at-hotel opt-in flow (offline payment, explicit opt-in, per source brief section 2/19 pattern) as a second payment mode.
8. Payment/refund confirmation emails to guests.
9. Idempotency tests: duplicate webhook delivery, retried payment confirmation — must not double-confirm or double-refund.
10. Security review pass on the payment flow specifically (server-side verification, webhook signature checks, no secrets in frontend).

## Explicitly not included

- ~~PokPay guest-payment integration (can be added later as a second `PaymentProvider` implementation, not scheduled in this milestone unless the owner requests it here).~~ **The owner requested it (2026-07-31) — see task 12.** PokPay is to become the primary gateway; Stripe stays available, tenant chooses per property (task 11).
- Platform/subscription billing (Milestone 11) — must not share code, tables, or ledger with this milestone's work.

## Kickoff decisions

Resolved at kickoff on 2026-07-30, before writing the task table below:

- **Refund policy**: [ADR-0018](../../decisions/ADR-0018-refund-policy-from-cancellation-snapshot.md) — `cancellation_is_free: true` (Milestone 4's Task 9 snapshot) triggers an automatic full refund; `false` triggers none automatically, with staff able to override manually either way.
- **Reservation timing vs. payment**: [ADR-0019](../../decisions/ADR-0019-reserve-then-expire-payment-pending.md) — inventory reserves immediately when a booking enters `PAYMENT_PENDING`; an expiry sweep releases abandoned `PAYMENT_PENDING` bookings into `EXPIRED` after a timeout.
- **Money representation**: the `payments` ledger uses `NUMERIC(12,2)` decimal-string amounts, matching `bookings.total_amount`/`rate_rules.amount` already established in Milestones 3–4, not integer minor-units — for consistency across the schema (the source brief's section 34 explicitly allows either).
- **Ledger shape**: append-only per the source brief's "immutable payment ledger" (section 34) — each event (charge succeeded, refund issued) is its own row, not a single mutable status field updated in place.

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | `PaymentProvider` interface in `packages/domain-contracts` | New interface distinct from `PmsProvider`/`BillingProvider`: `createCheckoutSession(context, command)`, `verifyWebhookEvent(context, rawBody, signature)`, `refund(context, command)`, `getPayment(context, paymentId)`. Reuses the existing `Money` type (`amount: string`, `currency`) for consistency with `Booking.total`. Types only for this task — no runtime implementation yet, matching how Task 1 of Milestone 4 was scoped. (Renamed `PaymentWebhookEvent.paymentId` → `externalPaymentId` myself for clarity — mechanical, nothing referenced it yet.) | Done | (uncommitted locally) |
| 2 | `payments` ledger table | Tenant/property/booking-scoped, append-only: `id`, `booking_id`, `kind` (`CHARGE`/`REFUND`), `provider` (`stripe`), `external_payment_id` (Stripe session/payment-intent/refund ID, unique per tenant for webhook dedup), `status`, `amount` `NUMERIC(12,2)`, `currency`, `created_at`. No `updated_at`/mutable status column — a correction is a new row, not an edit. RLS per the established tenant/property pattern, with only `SELECT`/`INSERT` policies (no `UPDATE`/`DELETE` policy at all) — I verified directly against the running DB that this makes `UPDATE`/`DELETE` silently affect 0 rows for the app's runtime role, enforcing append-only at the database level, not just convention. I also added `bookings` and `payments` to `tenant-rls.e2e.spec.ts`'s generic table list myself (both had been missing — a pre-existing gap from Milestone 4, not new to this task). | Done | (uncommitted locally) |
| 3 | `StripePaymentProvider.createCheckoutSession`, wired into `createBooking` per ADR-0019 | `createBooking` (`apps/api/src/booking/local-pms.provider.ts`) reserves inventory and transitions to `PAYMENT_PENDING` (replacing Milestone 4's synchronous `PAYMENT_NOT_REQUIRED` shortcut whenever payment is actually required), creates a Stripe test-mode Checkout session for the booking's total, and returns the booking plus a checkout URL. Booking creation no longer continues straight through to `CONFIRMED` in one call when payment is required. Fixed a lint failure myself (`no-unused-vars` on a destructured discard in the test file — used the codebase's existing `void x;` idiom). Required follow-up (`updateBooking` now rejects `total` changes with `UNSUPPORTED_UPDATE` once past `DRAFT`/`QUOTED`/`INVENTORY_REVALIDATING`; coverage restored against a real `PAYMENT_PENDING` booking) landed and verified. | Done | (uncommitted locally) |
| 4 | Stripe webhook endpoint | Public (unauthenticated, signature-verified — not `@PublicTenantScoped`'s guest-session model, since Stripe itself is the caller), verifies the `Stripe-Signature` header against the raw request body, never trusts the browser return. On `checkout.session.completed`/equivalent payment-succeeded event: records a `CHARGE` row in `payments`, then continues the booking through `PMS_CREATION_PENDING → PMS_CONFIRMATION_PENDING → CONFIRMED` using Task 4 of Milestone 4's existing transition helper. Deduplicates by Stripe event ID so redelivery doesn't double-process. Missing `apps/api/vitest.config.ts` fixed by me directly (test infra, not domain logic) — e2e tests were running on Vitest's bare 5000ms default and this task's test had genuinely grown past it; added `testTimeout`/`hookTimeout: 20_000`, verified it resolves the timeout without masking a real hang. Required follow-up (real end-to-end webhook test: signed payload via the actual `StripePaymentProvider`, posted to `/webhooks/stripe`, redelivery dedup) landed — and immediately caught a genuine bug: redelivery after confirmation 500'd because booking-state validation ran before duplicate recognition, since the booking is no longer `PAYMENT_PENDING` on the second delivery. Fixed by checking for an existing `external_payment_id` ledger row before rejecting on booking state. I also found and fixed a `pnpm format:check` failure across 3 files (not part of Codex's reported checks) with `prettier --write`. | Done | (uncommitted locally) |
| 5 | `PAYMENT_PENDING` expiry sweep (ADR-0019) | A scheduled job (BullMQ, per `docs/ARCHITECTURE.md`) finds bookings still in `PAYMENT_PENDING` past a timeout (30 minutes, swept every 1 minute), transitions them to `EXPIRED`, and releases their reservation via Milestone 4's existing `releaseBookedUnits` path. Also handles the edge case ADR-0019 flags: a late webhook arriving for an already-expired booking must not silently double-process — records a distinct `LATE_AFTER_EXPIRY` ledger row + audit entry, leaves the booking `EXPIRED`, does not re-confirm or re-reserve (remediation via refund is Task 6). Candidates found via a narrowly-scoped `SECURITY DEFINER` function (only 3 IDs returned, `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO must_booking_app`, matching the established pattern); each candidate is then re-verified under a row lock inside its own tenant-scoped transaction, closing the race between listing candidates and a concurrent webhook confirming the same booking. Verified independently: reverted-and-reproduced not needed here since the design was correct on inspection and all checks (26/26 files, 47/47 tests, lint, format, build, migration status) passed cleanly with no fixes required. | Done | (uncommitted locally) |
| 6 | Refund flow: automatic (ADR-0018) + staff-initiated | `cancelBooking` checks for an associated `CHARGE` payment and, when `cancellation_is_free` is true, automatically calls `PaymentProvider.refund` and records a `REFUND` row in the same transaction/idempotency pattern as the rest of cancellation. A separate staff-only endpoint (`@TenantScoped`, `@Roles`, `@RequiresVerifiedEmail`) allows a manual refund (full or partial, tracked against remaining balance) regardless of policy, for exceptions. Both paths are idempotent — retrying a refund request does not double-refund (verified via Stripe idempotency keys + `integration_operations`). Also remediates Task 5's flagged gap: a `LATE_AFTER_EXPIRY` charge now triggers an immediate automatic refund rather than sitting as an unresolved fact. I found and fixed one real bug myself: `Payment.bookingId` in the refund response was populated with the Stripe payment/session ID instead of the real booking ID (traceable to `RefundCommand` never carrying a `bookingId`, my own Task 1 design gap) — surfaced directly to the staff-facing endpoint's HTTP response. Fixed in `payment-refund.service.ts` where the correct id is actually known; verified with a rebuild, full test rerun (26/26 files, 48/48 tests single-forked), lint, format, and migration status. Noted, not blocking: Stripe API calls happen inside the DB transaction (same pattern as Task 3's checkout-session creation) — a real lock-holding/reconciliation tradeoff, mitigated by Stripe's own idempotency keys and this milestone's retry-on-failure pattern, worth revisiting in Task 10's security review. | Done | (uncommitted locally) |
| 7 | Pay-at-hotel opt-in | A second payment mode: guest explicitly opts in at booking time (no Stripe Checkout), booking transitions through `PAYMENT_NOT_REQUIRED` (Milestone 4's existing state) rather than `PAYMENT_PENDING`, and the booking/projection is tagged so staff can see payment is still owed in person. No new state — reuses `PAYMENT_NOT_REQUIRED`, just tagged with a payment-method marker (`STRIPE_CHECKOUT`/`PAY_AT_HOTEL`/`FREE`). Migration correctly backfills existing rows (zero-total → `FREE`, non-zero → `STRIPE_CHECKOUT`, preserving prior behavior) before adding the `NOT NULL` constraint. Verified independently: 26/26 files, 48/48 tests, lint, format, build, migration status all clean; no issues found. | Done | (uncommitted locally) |
| 8 | Payment/refund confirmation emails | Reuses the existing `MailProvider`/Resend integration (Milestones 2–3) to send a payment-confirmation email on `CONFIRMED` and a refund-confirmation email on a processed refund. Failure to send must not fail the underlying payment/refund operation (same safe-wrapper pattern Milestone 2 established for verification/welcome emails). Fixed a genuinely pre-existing (unrelated to this task) `Prisma` namespace import gap in `tenant-rls.e2e.spec.ts` myself — `tsc --noEmit` failed on it, mechanical one-line fix. Required follow-up (email sends moved to after each `withTenantTransaction(...)` resolves — `stripe-webhook.service.ts`, `payment-refund.service.ts`'s `refundCharge`/`manualRefund`, and `cancelBooking`'s automatic path all now capture a confirmation payload via closure and send afterward, mirroring `auth.service.ts`'s `signup()`) landed and verified. | Done | (uncommitted locally) |
| 9 | Idempotency tests: duplicate webhooks, retried confirmation | Tests proving a duplicate Stripe webhook delivery (same event ID) does not double-confirm a booking or double-insert a `CHARGE` row, and a retried refund request (same idempotency key) does not double-refund. Run against real Postgres/Redis. Precisely closed the three gaps identified in review (rather than adding redundant coverage): true concurrent (`Promise.all`) webhook delivery while still `PAYMENT_PENDING`, a retried automatic cancellation-refund asserted against an exact provider-invocation count, and duplicate `LATE_AFTER_EXPIRY` webhook delivery. Verified independently, including 3 repeated runs to rule out concurrency-test flakiness: 26/26 files, 49/49 tests, lint, format all clean. | Done | (uncommitted locally) |
| 10 | Security review pass on the payment flow | Dedicated review of: webhook signature verification (valid, missing, and tampered-signature cases all tested), no Stripe secret key or webhook secret ever reachable from the frontend/WordPress, no raw card data touches MUST's backend (Stripe Checkout's hosted page handles it), and the guest-facing payment endpoints are correctly scoped by `@PublicTenantScoped`/the public webhook route, not accidentally staff-gated or accidentally open to cross-tenant access. **Found and fixed a real IDOR-class authorization defect**: public booking update/cancel only checked tenant/property scope, not *who* created the booking — any guest who learned/guessed another booking's UUID could cancel or modify it. Fixed by persisting the creating session's UUID (`bookings.guest_session_id`, backfilled with an unguessable value for pre-existing rows) and requiring the same session on update/cancel, returning the same `BOOKING_NOT_FOUND` as a genuinely nonexistent booking (checked before the version-conflict check) so the response can't be used to enumerate valid booking IDs or leak ownership. Session ID is always derived server-side from the authenticated cookie via `PublicTenantScopedGuard`, never client-supplied. All other findings (webhook signatures, secrets, card data, refund endpoint's staff-only scoping, NUMERIC/BigInt money handling — no floating point anywhere) verified independently and confirmed sound. 26/26 files, 49/49 tests, lint, format, migration status all clean. | Done | (uncommitted locally) |
| 11 | Tenant-configurable payment gateways; no ungoverned `FREE` fallback | **Reopened 2026-07-31.** Today `paymentMethod` is decided per-booking by whatever the caller passes (`payAtHotel: true/false`), with no concept of which gateways a tenant has actually turned on — the WordPress plugin currently hardcodes a Stripe/pay-at-hotel choice with no backing configuration. **Resolved 2026-07-31: scoped per-property, not per-tenant** — consistent with every other configuration in this domain (`RatePlan`, `RoomType`, `Inventory`, `Property.publicWebsiteOrigin` from Milestone 6 task 8), and a tenant may run properties in different markets where PokPay/Stripe availability genuinely differs. Add a per-property setting for which gateways are active — `stripe`, `pokpay` (task 12), `pay_at_hotel` — staff-managed via a `@TenantScoped`/`@Roles`-gated endpoint, and exposed read-only via the existing public catalog/config surface so the guest-facing plugin can render only the enabled choices. `POST .../bookings` must reject a `paymentMethod`/`payAtHotel` selection that is not enabled for that property (a clear error, not a silent fallback). This does **not** touch the existing `FREE` marker's actual purpose (Milestone 5 task 7: a zero-total booking, e.g. a comp stay) — `FREE` stays exactly as-is for genuinely zero-total bookings; the fix is that a **non-zero-total** booking must never resolve to an unconfigured or default gateway. If a property has zero gateways enabled and pay-at-hotel is off, checkout must fail cleanly with a specific error, not silently pick something. Tests: booking against a disabled gateway is rejected; booking against an enabled gateway succeeds; a zero-total booking is unaffected and still resolves to `FREE` regardless of gateway configuration.

**Done, independently verified (2026-07-31).** Fix: the migration now backfills `stripe_enabled = true, pay_at_hotel_enabled = true` for every row present at migration time (`WHERE NOT stripe_enabled AND NOT pay_at_hotel_enabled AND NOT pokpay_enabled` — true for all pre-existing rows at the point the migration runs, since the columns are brand new), leaving `pokpay_enabled` false and leaving genuinely new properties on the all-disabled column defaults. Verified directly against the real dev database, not just re-reading the SQL: all 62 pre-existing properties now show `stripe_enabled: true, pay_at_hotel_enabled: true, pokpay_enabled: false`; re-ran the exact pay-at-hotel booking against Sanur Beach Resort (the property this milestone's WordPress verification runs against) that failed before the fix — now succeeds, `CONFIRMED`, and was cancelled afterward to clean up. Full suite re-run clean (26/26 files, 50/50 tests), build and migration status clean. Found and fixed one small thing myself, unrelated to Codex's work: my own leftover scratch verification script (`apps/api/scratch-check-property.cjs`, `scratch-verify-backfill.cjs`) was the actual cause of the repo-wide `pnpm lint` failure Codex correctly identified as not theirs — deleted both; `pnpm lint`/`format:check`/build all clean afterward (fixed 4 files' unrelated pre-existing formatting drift caught by the same `format:check` pass with `prettier --write`, same as task 11's original review). | Done | |
| 12 | PokPay `PaymentProvider` implementation | **Reopened 2026-07-31**, owner-requested — PokPay is intended as the primary gateway going forward (Stripe remains available; tenant chooses per task 11). **Design correction (2026-07-31), verified against the legacy plugin's actual production PokPay integration:** PokPay's current public docs ([docs.pokpay.io](https://docs.pokpay.io/), [payments.doc.pokpay.io](https://payments.doc.pokpay.io/)) document no webhook signature/verification scheme at all — confirmed directly, not assumed. Codex correctly blocked on this rather than inventing one. The legacy plugin (`MUST-DEV-TEAM/must-hotel-booking`, `src/Engine/PaymentEngine.php`) already solved this in production: its webhook handler (`handlePokPayWebhookRequest`) never trusts the webhook body's claimed status — it only extracts the `orderId` from it, confirms that order genuinely belongs to a local payment record (`reservationPaymentsMatchPokPayOrder`), then calls `getPokPaySdkOrder($orderId)` — an authenticated `GET` back to PokPay using the merchant's own bearer token — and only *that* response's status (`CAPTURED`/`PAID`/`COMPLETED`) is trusted, tagged literally `'authoritative_provider_reread'` in `finalizePokPayOrder()`. This is not a fallback design, it's how PokPay actually ran. Implement `PaymentProvider` (task 1's interface) for PokPay using this same pattern: `createCheckoutSession` creates an SDK order; there is no `verifyWebhookEvent` in the Stripe-signature sense — a webhook/return endpoint accepts an `orderId`, validates it's bound to a real local payment, then re-reads the order authoritatively from PokPay before ever confirming a booking; `refund`/`getPayment` per PokPay's documented REST endpoints. Reuse the existing `PAYMENT_PENDING` expiry-sweep job (task 5) to also poll PokPay for bookings that never return via redirect, rather than depending solely on the webhook arriving. No PokPay secret ever reachable from the frontend/WordPress. Selectable via task 11's gateway configuration. Tests: order-binding mismatch is rejected (the legacy `reservationPaymentsMatchPokPayOrder`/`validatePokPayOrderBinding` checks), authoritative re-read correctly confirms/rejects based on PokPay's real status, duplicate webhook/poll delivery doesn't double-confirm, refund idempotency — same rigor as tasks 4 and 9's Stripe coverage, against PokPay's sandbox/test mode.

**Review (2026-07-31) — changes requested, not Done.** Code inspected directly and is sound: the session binding (`payment_provider_sessions`) is written in the same transaction as checkout-session creation, before any callback could plausibly arrive; `processAuthoritativeOrder` cross-checks the re-read order's amount/currency against the local booking (not just its status); idempotency via `ON CONFLICT (tenant_id, external_payment_id) DO NOTHING` plus an explicit already-processed check when the booking is no longer `PAYMENT_PENDING`; `FOR UPDATE OF b` row lock closes the race between a webhook and the poll sweep hitting the same booking concurrently; the two new `SECURITY DEFINER` functions are correctly narrow (`REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE` to the app role only, RLS forced on the new table) — same discipline as task 5's expiry-sweep functions. `verifyWebhookEvent` correctly fails loudly (`POKPAY_AUTHORITATIVE_REREAD_REQUIRED`) rather than pretending to verify a signature that doesn't exist. Full suite re-run independently: 27/27 files, 52/52 tests, lint, build, migration status all clean (fixed 4 files' formatting drift with `prettier --write`, unrelated to the logic). **The one real gap**: zero test coverage of the actual orchestration path this task exists to secure. The only new test (`pokpay-payment.provider.spec.ts`) exercises the isolated HTTP-calling wrapper against a mocked `fetch` — it never calls `PokPayPaymentService.processAuthoritativeOrder` or `POST /webhooks/pokpay` at all. Confirmed directly: Stripe's webhook has 11 real touchpoints in `local-pms-provider.e2e.spec.ts` (signed payload, redelivery dedup, etc.); PokPay has 4, and all 4 are just gateway-config toggles (`pokpay: false`) — the binding-mismatch check, amount/currency cross-check, duplicate-webhook handling, and row-lock behavior have never actually run. This is exactly the "same rigor as tasks 4 and 9's Stripe coverage" this task's own acceptance criteria asked for and it's missing. Required before Done: real integration tests hitting `POST /webhooks/pokpay` (with the app's real `PokPayPaymentService`, `PaymentProvider` swapped for a test double the same way Stripe's tests override `PAYMENT_PROVIDER` — no real PokPay sandbox call needed to test the orchestration logic itself) covering: a booking with no session binding is rejected; a re-read amount/currency mismatch is rejected; duplicate delivery for an already-confirmed booking doesn't double-charge; the poll sweep picks up a bound order that never redirected back.

**Gap closed, Done (2026-07-31).** Real integration tests added to `local-pms-provider.e2e.spec.ts` against the actual running app (`PaymentProviderRegistry.pokpay` swapped for a realistic test double via both `.overrideProvider(PokPayPaymentProvider)` and a direct post-init assignment) — genuinely exercises `POST /webhooks/pokpay` end to end, not mocked at the HTTP-client level: an unbound `orderId` → `400`; a re-read amount mismatch → `400`, booking independently confirmed still `PAYMENT_PENDING`; duplicate endpoint delivery → both calls `200`, booking `CONFIRMED`, exactly one `payments` `CHARGE` row (checked directly against the DB, not just the HTTP response); `PaymentExpiryService.sweep()` independently confirms a bound order that never redirected back. Verified independently, not just re-read: full suite re-run twice (one run hit the same transient IPC worker-crash seen earlier in this session, reproduced clean on immediate retry — not a real failure), 27/27 files, 52/52 tests, lint, build, migration status all clean; fixed one file's formatting drift with `prettier --write` (caught by `format:check`, unrelated to the test logic itself). `PaymentProviderRegistry.pokpay`'s `readonly` → mutable change is test-only plumbing (lets tests swap the instance post-init), not a production behavior change. | Done | |
| 13 | Confirmation email completeness + match the old plugin's email design | **Reopened 2026-07-31**, surfaced while wiring Milestone 6's cancellation flow. Real gaps found: (1) `sendPaymentConfirmationEmailSafely` is currently only ever triggered from `stripe-webhook.service.ts` on payment success — a pay-at-hotel booking never sends any confirmation email at all, so that guest has no cancellation link, no booking record in their inbox, nothing. Trigger the same confirmation email directly from `createBooking` for `PAY_AT_HOTEL` bookings at the point they reach `CONFIRMED`, reusing the existing safe-wrapper pattern (task 8) so a send failure never fails the booking. (1b) **Added 2026-07-31, discovered after task 12 landed**: PokPay bookings confirm via `PokPayPaymentService.processAuthoritativeOrder`'s `continueAfterPayment` call (webhook or poll-sweep path), not `createBooking` — same gap, no confirmation email sent there either. Trigger it from the same place, once, covering both the webhook-delivered and poll-sweep-delivered confirmation paths without double-sending. **Scope note (2026-07-31): `FREE` is out of scope for this task** — the owner confirmed a genuinely zero-total booking will never occur in real hotel operation; leave the `FREE` payment method/schema exactly as-is (harmless, unused in practice), don't build or test an email specifically for it. (2) The cancellation link built in `cancellationUrl()` (`stripe-webhook.service.ts`) only appends `booking_id`/`cancellationToken` — it needs an additional marker so WordPress's confirmation page (`confirmation-page.php`) knows to show the cancellation-review UI immediately rather than the normal "booking confirmed" view; coordinate the exact parameter with whatever the plugin still expects at implementation time (it currently reads `must_action`, but confirm against the live code, not this note). Also: the owner wants the **email's visual design** to match the original plugin's own email templates (`src/Engine/EmailEngine.php`/`EmailLayoutEngine.php` from the audit trail, `AUDIT.md` section 4) rather than `ResendMailProvider`'s current generic styling — reuse that layout/copy, not a new design. Tests: a pay-at-hotel booking triggers exactly one confirmation email; a PokPay booking confirmed via webhook triggers exactly one; a PokPay booking confirmed via the poll sweep triggers exactly one (not zero, not two); the email's cancellation link, when visited, reaches the plugin's cancellation-review UI without any other query parameters being added by hand.

**Review (2026-07-31) — changes requested, not Done.** PokPay's email trigger (`pokpay-payment.service.ts`) is correctly implemented — `emailBookingId` is only set right before the genuine `duplicate: false` return, not on either duplicate-detection branch, so webhook/poll redelivery correctly sends nothing extra; verified by reading the code path directly. The email design match against the legacy plugin is genuine, not approximated — checked `EmailLayoutEngine.php` directly: the new HTML is a near line-for-line port (`background:#f4f1ea`, `color:#141414` headings, the exact "Booking Summary" table styling with `#f7f4ec`/`#d8d2c4`, the "Need Help?" block with `#faf7f0`/`#ddd6c8`). The `must_action=cancel` cancellation-link marker was checked against the plugin's actual gating logic (`confirmation-page.php`'s `sanitize_key(...) !== ''` check) — correct, any non-empty value works. **One confirmed, real bug found and fixed.** The pay-at-hotel email trigger originally sat outside the `withIdempotency`-guarded transaction, so retrying `POST /bookings` with the same Idempotency-Key (e.g. after a network timeout — the entire reason idempotency keys exist) sent a second confirmation email even though the booking data was correctly not duplicated.

**Fix verified, Done (2026-07-31).** `payAtHotelConfirmationBookingId` is now declared outside the transaction, initialized `null`, and only set inside the `withIdempotency`-guarded `execute()` callback, immediately before the genuine first-time `CONFIRMED` transition — on a cached replay `execute()` never runs, so it correctly stays `null` and no email fires. Exactly mirrors the pattern already used correctly by Stripe's webhook and PokPay's service. New regression test posts the same pay-at-hotel booking request twice with the same Idempotency-Key and asserts the confirmation-email count only increments once (checked directly, not just via HTTP response shape) and that no duplicate `payments` row exists. Verified independently: full suite re-run, 27/27 files, 52/52 tests, lint, build, migration status all clean (fixed 2 files' formatting drift with `prettier --write`, unrelated to the fix itself). | Done | |
