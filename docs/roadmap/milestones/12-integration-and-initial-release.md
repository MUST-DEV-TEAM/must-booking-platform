# Milestone 12: Integration, Hardening & Initial Release Readiness

Status: Kicked off 2026-08-14, tasks defined below
Depends on: Milestones 0-11

**Kickoff decisions (2026-08-14):** scope is deliberately narrower than the original draft's general "initial version for anyone" framing. The real, immediate target: v1 solid enough for Empire Beach Resort specifically — the one real, already-onboarded, Clock-connected tenant taking real guest bookings and real payments right now — with a target of next week. Prioritized by demonstrated risk, not the original draft's generic 10-area checklist: the homelab deploy pipeline was found silently broken for 2+ days during Milestone 6's close-out (a root-owned `.git` blocked the deploy user's fetches with no alert of any kind), which is direct proof that "deploys might silently fail" and "failures are invisible" are the two risks that actually matter most right now for a live paying tenant. Explicitly deferred to a later pass (once there's a second tenant or real launch pressure): a platform-wide security audit, full metrics/dashboards, demo/seed data scripts, a general (non-Clock-specific) reconciliation job, and a full documentation pass.

**WordPress integration note (2026-07-31):** Milestone 6 (WordPress Plugin Retrofit) was paused mid-flight and Milestone 10's guest-widget tasks (individual-room picker, direct-room-entry) were deferred alongside it. This milestone's task table must include the consolidated pass that finishes both against whatever the backend looks like by this point — not assume Milestone 6 is otherwise done.

**Resequencing note (2026-08-03, ADR-0025):** Platform Billing moved from Milestone 11 to Milestone 13 (last at the time), after this milestone rather than before — the owner wants the product functional end-to-end first, and Milestone 2's permanent Free plan already makes that possible without real billing. This milestone no longer depends on Platform Billing, and its smoke test does not include an upgrade step: tenants remain on Free through this checkpoint.

**Resequencing note (2026-08-17, ADR-0028):** two new milestones — 13 (Application UI/UX & Feature Enhancements) and 14 (Security & Architecture Audit) — are inserted immediately after this milestone closes, ahead of Platform Billing (now Milestone 15). No change to this milestone's own scope.

## Goal

Everything from Milestones 0-11 works together as one coherent, demoable, deployable initial version. This is **not** the fully production-hardened product (see the backlog in `docs/ROADMAP.md`) — it is the "we have something real" checkpoint: reaching this milestone done is the definition of "initial version, even if not complete" from the roadmap's framing. Milestones 13-14 (Application Enhancement, Security & Architecture Audit) and Platform Billing (Milestone 15) are intentionally not part of this checkpoint.

## Draft task areas (not final — define the real 10 tasks at kickoff)

1. Cross-milestone E2E smoke test: signup (Free) → configure property/rates → guest books via widget → guest pays → staff sees it in dashboard → (optional) Clock-connected tenant syncs a booking.
2. Basic observability: structured logs, error tracking, and a minimal metrics/alerting setup (source brief section 28's list, scoped down — not the full production checklist).
3. Security pass: rate limiting on public endpoints, secrets audit, dependency audit, basic SSRF/input-validation review across the API surface built so far.
4. Staging deployment pipeline (CI/CD, per `docs/ARCHITECTURE.md`'s infra list) — a real environment the owner can click around in.
5. Seed/demo data scripts (a realistic demo tenant with rooms, rates, and sample bookings) for demos and manual QA.
6. Bug-fix buffer: triage and fix issues surfaced by the smoke test and staging use across Milestones 0-11.
7. Basic reconciliation job: booking/payment consistency check (source brief section 22, minimal version) to catch the most likely real-world drift.
8. Documentation pass: `README.md`, `docs/OPERATIONS.md`-equivalent (setup/deploy/diagnose), and each milestone's file confirmed accurate against what was actually built.
9. Production activation checklist drafted (source brief section 32/36-style) — not executed, just written, so go-live is a deliberate future decision, not a surprise.
10. Go/no-go review with the owner: walk through the demoable system, confirm what's genuinely usable vs. still backlog, and confirm Milestones 13-14 (Application Enhancement, Security & Architecture Audit) are next, followed by Milestone 15 (Platform Billing), production hardening, more plan tiers, WordPress migration, additional PMS vendors — see `docs/ROADMAP.md`'s backlog.

## Explicitly not included

- Platform Billing (Milestone 15) — real subscriptions, dunning, plan enforcement; tenants stay on Free through this milestone.
- Application UI/UX & Feature Enhancements (Milestone 13), Security & Architecture Audit (Milestone 14) — scheduled right after this milestone closes, not part of it.
- Full production certification of the Clock integration (remaining source-brief deliverables/hardening).
- Legacy WordPress plugin decommissioning.
- Additional PMS vendors, multi-currency billing, marketplace features — all backlog per `docs/ROADMAP.md`.

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | Fix and harden the homelab deploy pipeline | Determine the actual root cause of the 2026-08-12 breakage (confirm whether `infrastructure/containers/deploy-webhook.mjs` runs as a persistent service on the homelab host and whether it or an ad-hoc `sudo git` command caused the `.git` ownership corruption); eliminate the root cause so an ad-hoc `sudo` invocation can't corrupt the checkout again (dedicated deploy-only git operations, no root-owned files in the working tree); add a lightweight scheduled check that compares the deployed commit against `origin/main` and fails loudly, not silently, on divergence. **Acceptance**: a forced test (e.g. reverting the deployed host to an old commit, or blocking the deploy user's git access) proves the drift check actually fires and is visible somewhere outside a manual SSH session — not rediscoverable only the way tonight's investigation found it. A real `git push origin main` deploys cleanly end-to-end afterward. **Code reviewed and reviewed clean 2026-08-14, not yet Done — live verification still required per this task's own acceptance criteria.** Root cause confirmed as `deploy-webhook` running Git as container root against the bind-mounted host checkout (same failure class as the ad-hoc `sudo git` that also could have caused it). `deploy.sh` now refuses to run as root and refuses to proceed if any path in the checkout is foreign-owned; the Dockerfile's `git config --add safe.directory` override — which is what let git silently operate across an ownership mismatch — was removed rather than worked around; the compose file requires an unprivileged `DEPLOY_UID`/`DEPLOY_GID` at startup (fails loudly if unset). A systemd timer is defined to run `check-deploy-drift.mjs` every 5 minutes, refusing to run as root, reporting both ownership drift and commit drift via Sentry. I ran the behavioral test suite myself — 2 passed, 1 root-only test correctly skipped on this non-root Windows session, 0 failed — but that only proves the script logic against simulated local repos, not the real homelab host. Still needed before Done: install this on the actual homelab (set `DEPLOY_UID`/`DEPLOY_GID`/`DOCKER_GID`/`SENTRY_DSN`, enable the systemd timer, redeploy the webhook container) and prove a real forced drift produces a real Sentry alert there — the same kind of live step the owner did by hand earlier in Milestone 6. | In review | |
| 2 | Real error tracking and Clock-sync-failure visibility | Instrument `apps/api` with real production error tracking (Sentry is already available as a tool in this environment — evaluate it first before building anything custom). Ensure the Clock scheduled sync jobs (`must_hotel_booking_clock_full_catalog_sync`, `must_hotel_booking_clock_availability_rate_sync`, `must_hotel_booking_clock_reservation_fallback_sync` — see `apps/wordpress-plugin/docs/OPERATIONS.md`'s background-jobs table) report failures through it instead of only writing to logs nobody watches. Wire Task 1's deploy-drift check to notify through the same channel. **Acceptance**: a deliberately triggered failure (a forced exception in a Clock sync job, or a real deploy-drift condition) produces a real, visible alert within a reasonable time — not just a log line requiring someone to go looking. **Code reviewed and reviewed clean 2026-08-14, not yet Done — same live-verification gap as Task 1.** `@sentry/node` is wired into `apps/api` (optional locally, required in production via `SENTRY_DSN`), a global exception filter reports unexpected 5xx failures without alerting on normal 4xx traffic, and the Clock worker reports a job only once it's truly exhausted its retries (matching the existing dead-letter condition exactly, so this won't fire on ordinary transient retries). Correction to this task's own original scope: the three WordPress-side Clock sync hook names in this row's acceptance criteria don't exist in the current plugin — Milestone 6 made WordPress an API-only client with no local Clock sync code, so the real sync jobs live in `apps/api`'s `ClockWorkerService`/queues, which is what's actually now instrumented. Docs corrected to stop naming the retired WordPress hooks. Still needed before Done: a real `SENTRY_DSN` configured in production and a real triggered failure (or Task 1's real drift condition) confirmed to actually arrive as a Sentry alert. | In review | |
| 3 | Clock/local booking-state consistency check, scoped to Empire Beach Resort | A scheduled or on-demand check comparing Empire Beach Resort's local `CONFIRMED`/`CANCELLED` bookings against Clock's actual reservation state for the same date range, flagging — not auto-correcting — any mismatch (booking exists locally but not in Clock, exists in Clock but not locally, or status disagrees between the two). This is the real risk class the plugin's own runbook already names ("Duplicate or partial Clock fulfillment" in `apps/wordpress-plugin/docs/OPERATIONS.md`'s incident playbooks), scoped down to detection only, not the full reconciliation-job draft area. Surface findings through Task 2's alerting. **Acceptance**: run against Empire Beach Resort's real current booking data (not a mocked fixture) and confirm it correctly reports zero drift, or correctly identifies and reports any real drift found. | Not started | |
| 4 | Full live guest-journey walkthrough, including a real cancellation | One more complete live pass against the fully-deployed system (post Milestone 6 close-out): search → select → book a real Clock-connected room with real payment → confirm every triggered email (paid confirmation + staff notification) renders correctly → cancel a booking dated far enough out to clear the self-service cancellation window (unlike the 2026-08-13 test, which used a booking only 2 days from arrival and could never reach cancellation) → confirm the cancellation and refund emails render correctly. Verification only — any bug found gets logged as its own follow-up rather than blocking this task. **Acceptance**: every step of the guest and staff journey is proven against the real, currently-deployed system, with cancellation specifically covered for the first time. | Not started | |
| 5 | Security pass scoped to the real internet-facing surface | Review the public booking/quote/catalog endpoints, the PokPay and Stripe webhook handlers (starting from the plugin's own documented concern in `apps/wordpress-plugin/docs/INTEGRATIONS.md` that the PokPay webhook has no separately verified local shared secret), and the WordPress plugin's public REST routes for rate limiting, input validation, and secret handling. Fix what's realistically exploitable given this is a live paying operation; explicitly document anything consciously deferred and why, rather than silently skipping it. **Acceptance**: a written summary of what was checked, what was fixed, and what's deferred; any real fix ships with a test proving the specific vulnerability is closed. **Done 2026-08-14** — see the "Task 5 security review" section below for the full written summary. Reviewed: Redis-backed rate limiting correctly scoped per endpoint tier (reads 120/min, quotes 20/min, booking mutations 10/min, webhooks 120/min), keyed off the real socket peer address rather than a spoofable forwarded-for header, atomic Redis `INCR`+`EXPIRE`. Independently re-verified via a real e2e run (not just trusting the report): a genuine, previously-unscoped gap was found and fixed — the Clock SNS webhook verified only that a message was validly signed by *some* AWS SNS topic, not that it was *this tenant's configured* topic; now fail-closed on topic mismatch, confirmed live in `clock-webhook.e2e.spec.ts` (8/8 passing, run by me). PokPay's missing shared secret and Clock's configurable proxy host were correctly investigated and consciously deferred with real reasoning, not silently skipped. One thing to track: existing Clock connections need their real `snsTopicArn` populated before this fix takes effect for them — currently a no-op for Empire Beach Resort since no live Clock Message Channels subscription is configured yet, but this needs to happen *before* one is turned on, not be forgotten. **Post-close-out incident (2026-08-14, found and fixed during Task 1's live deploy)**: this task's Next.js 16.3.1 bump was never actually verified against a real production build — Codex's report noted the web build "hit pre-existing test-file TypeScript errors," which masked the fact that a *passing* production build of 16.3.1 crash-loops at runtime (`MODULE_NOT_FOUND` on `@swc/helpers` inside Next's own standalone output, a real Next.js standalone-tracing regression). Deploying tonight's Milestone 12 work exposed this for real: `must.dejvis.dev` went down (502s, both API and web, confirmed via container logs showing the crash and a direct in-host health check bypassing the proxy). Fixed properly, not by blindly reverting past the security fix: `pnpm audit` against the pre-bump 16.0.1 showed 43 real vulnerabilities including a critical RCE (`GHSA-9qr9-h5gf-34mp`) — a first revert attempt to 16.0.1 was caught by this repo's own automated commit security review before I could ship it. Landed on the actual right fix: pin `next` to `16.2.11` (the minimum version clearing every Next-level advisory) plus root-level `pnpm.overrides` forcing `sharp>=0.35.0`/`postcss>=8.5.23` (the remaining findings were transitive, bundled inside Next's own tree — the real reason 16.3.1 was picked in the first place). Verified this time by actually running the built standalone server (`node .next/standalone/apps/web/server.js`) and confirming a real 200, not just a passing build step. Live-deployed and confirmed: `must.dejvis.dev` is back up (200 on both `/` and `/api/health`), running the correct version. | Done | |
**Task 3 amendment (2026-08-14, resolved with the owner):** Clock status mapping for the consistency check — a local `CONFIRMED` booking is considered matched if Clock has a reservation in any of `expected`/`checked_in`/`checked_out`/`no_show` (all mean "Clock knows this booking is real"; `no_show` is not a drift condition, the booking happened, the guest just didn't arrive). A local `CANCELLED` booking is matched if Clock has no reservation at all, or an explicitly cancelled one. Live, read-only querying against Empire Beach Resort's real Clock account is authorized.

| 6 | Go/no-go review with the owner | Walk the full guest and staff journey together (using the evidence gathered in Tasks 1-5), confirm what's solid, confirm what's consciously deferred and why, and get explicit sign-off that v1 is ready for Empire Beach Resort. **Acceptance**: owner sign-off recorded in this file. | Not started | |
| 7 | Require a real cancellation/refund policy before go-live | Found during Task 4's live cancellation test (2026-08-14): Empire Beach Resort's Clock-synced "Clock: APP" rate plan has `free_cancellation_until_hours` unset entirely, so `LocalPmsProvider.cancelBooking`'s existing "no configured policy → not free" default means a guest who self-cancels never gets a refund, silently, regardless of how far out they cancel — correct behavior for the code, but nobody has made that a deliberate choice for this property. **Owner decision (2026-08-17):** Empire Beach Resort's real policy is a single 21-day threshold, no middle tier — free cancellation at 21+ days before arrival, self-service cancellation not permitted at all inside 21 days (the guest must contact the hotel directly). This maps cleanly onto the two settings that already exist rather than needing new policy tiers: `properties.free_cancellation_days_before_arrival` (the self-service access gate — confirmed already at 21, the shipped default, for both of this tenant's properties) and `rate_plans.free_cancellation_until_hours` (the refund-eligibility field, currently unset on all four of Empire Beach Resort's real rate plans — DBL/FAM/APP/Standard Rooms). Setting the latter to 504 (21 × 24 hours) aligns it exactly with the already-correct self-service gate, so every cancellation a guest is even allowed to attempt is guaranteed free — no gap between "allowed to cancel" and "gets refunded." **Immediate data fix, still pending (2026-08-17):** blocked by the auto-mode classifier from running the production `UPDATE` directly — owner needs to run it themselves: `ssh homelab "docker exec must-booking-postgres-1 psql -U must_booking -d must_booking -c \"UPDATE rate_plans SET free_cancellation_until_hours = 504, updated_at = CURRENT_TIMESTAMP WHERE tenant_id='ac5d0085-1565-4e45-a5a1-d297889a2f82' AND property_id='e3aabe36-7b18-4259-ac5e-84504f79b79d' RETURNING id, name, free_cancellation_until_hours;\""`. **Real gap found while investigating, now this task's actual remaining scope**: no staff-facing UI exists to set `free_cancellation_until_hours` at all — `RatePlansService.list()` explicitly excludes Clock-shadow rate plans (`clock_shadow_room_type_id IS NULL` filter) from the Rate Plans management page, and every one of Empire Beach Resort's real rate plans is Clock-shadow, so staff can't see or edit this field for any property using Clock, only via direct DB/script access like this task needed tonight. Add a minimal cancellation-policy control for Clock-shadow rate plans — most naturally surfaced in the existing Clock Catalog Sync panel (which already lists Clock-shadow entities) rather than exposing full rate-plan editing there, since Clock should stay the source of truth for a shadow rate plan's other fields (name, currency, active state). Expressed to staff as a single "free cancellation up to N days before arrival" number (not raw hours) for consistency with the property-level setting's own units; consider surfacing both settings together (or warning if they diverge) so staff don't unknowingly create the exact gap this task exists to close. **Acceptance**: Empire Beach Resort's rate plans have the 504-hour value applied (owner-run, confirmed in this row once done); staff can set/change a Clock-shadow rate plan's free-cancellation-days value from the dashboard without direct DB/script access, verified by a real e2e/browser check. | Not started | |
| 8 | Tenant refund notification email | Confirmed gap (2026-08-14): when a guest self-cancellation triggers an automatic refund, the guest gets `sendRefundConfirmationEmail`, but property staff only get the generic `sendBookingCancelledStaffNotification` — nothing refund-specific (amount refunded, payment method, etc.). Add a staff-facing refund notification, following the existing pattern (`BookingCancellationNotificationService`/`PaymentNotificationService`, staff recipients from `property_staff_assignments`, `*Safely` wrapper so a mail failure never blocks the refund itself). **Acceptance**: implemented, then proven with a real trial — a real booking on a rate plan with a real free-cancellation policy (from Task 7, or a controlled test property if that's not ready yet), self-cancelled so a real refund actually fires, with the resulting guest refund email and the new tenant refund email both sent to devimyrto26@outlook.com for review before this is marked Done. | Not started | |
| 9 | Staff-initiated booking cancellation | Confirmed gap (2026-08-14): no staff-authenticated cancellation endpoint exists at all today — `DELETE /bookings/:id` is the guest-facing `PublicTenantScoped` route only; a staff member has no way to cancel a booking from the dashboard. Add a staff-authenticated cancel action (tenant/property-scoped, staff auth guard, reusing `LocalPmsProvider.cancelBooking`'s real Clock-cancellation + refund-policy logic rather than duplicating it) and wire it into the dashboard's booking view. When staff cancels, the guest must receive the same cancellation email a self-cancellation would send. **Acceptance**: implemented, then proven with a real trial — staff-cancel a real test booking and confirm the guest cancellation email (and refund email, if the policy makes it free) is sent, with the emails forwarded to devimyrto26@outlook.com for review before this is marked Done. | Not started | |
| 10 | Fix stale `MailProvider` e2e mocks (Milestone 6 Task 51 follow-up) | Confirmed still live (2026-08-14, `tsc --noEmit -p apps/api/tsconfig.json`): 34 e2e spec files' local `MailProvider` mocks don't implement `sendBookingCancelledEmail`/`sendBookingCancelledStaffNotification` (added by Milestone 6 Task 48), producing real `TS2739` errors invisible to every task's stated checks because none ran a full-workspace typecheck including test files. Full current file list: `amenities`, `auth`, `availability-calendar`, `availability`, `booking-concurrency`, `clock-availability`, `clock-booking`, `clock-catalog-sync`, `clock-payment-gated-booking`, `clock-sandbox-validation`, `clock-webhook`, `individual-room-booking`, `integration-connections`, `local-demo-seed`, `manual-availability-blocks`, `manual-review`, `milestone-10-integration`, `mixed-auto-assignment`, `onboarding`, `platform-admin-actions`, `platform-admin-flow`, `platform-dashboard`, `properties`, `provider-health`, `public-catalog-individual-rooms`, `rate-plans`, `room-availability`, `room-price-overrides`, `room-types`, `rooms`, `staff-booking-payment-method`, `staff-invite`, `tenant-dashboard-flow`, `wordpress-pairing` (all `.e2e.spec.ts` under `apps/api/test/`). Mechanical fix — add the two missing stub methods to each mock. **Acceptance**: `pnpm exec tsc --noEmit -p apps/api/tsconfig.json` (the full tsconfig, not the build-only one that excludes tests) has zero errors; full API test suite still passes. **Done 2026-08-14** — `69c539f`. Reviewed: all 34 files fixed. Independently re-verified (not just trusting the report): `pnpm exec tsc --noEmit -p apps/api/tsconfig.json` → 0 errors. | Done | |
| 11 | Wire the staff-invitation email template | Milestone 6 Task 51 skipped the `4-admin-staff-invite.html` re-skin on the mistaken premise that no staff-invitation feature existed — corrected same day: `StaffInviteService` and a real activation endpoint already exist (`apps/api/test/staff-invite.e2e.spec.ts` predates that task). This is a wiring job, not new feature work: apply Task 51's re-skin pattern to whatever email `StaffInviteService` currently sends (or add the send if it's missing entirely — confirm which by reading the service first), matching `design-reference/emails/4-admin-staff-invite.html`. **Acceptance**: a real staff invitation sends the correctly-branded email; verified by a real trial sent to devimyrto26@outlook.com. **Done 2026-08-14** — `776f723`. Reviewed: the send didn't exist at all (only the accept/activate flow did) — `StaffInviteService.invite()` now calls a new `*Safely`-wrapped `sendInvitationEmailSafely`, tenant-scoped query resolves the real organization/inviter/property/role-template names inside `withTenantTransaction`, mail failure never blocks the invite itself. Real e2e (`staff-invite.e2e.spec.ts`) asserts the actual email command matches real DB data end to end, plus unit coverage in `resend-mail.provider.spec.ts`. Independently re-verified: ran `staff-invite.e2e.spec.ts` myself (3/3 passing) rather than trusting the report. **Real trial completed 2026-08-14** — `RESEND_API_KEY`/`MAIL_FROM_EMAIL` only exist on homelab, not locally, so ran the real `StaffInviteService.invite()` directly inside the live `must-booking-api-1` container (against production data: Empire Beach Resort's real tenant `ac5d0085-1565-4e45-a5a1-d297889a2f82`, Front Desk role, real Owner as inviter) targeting devimyrto26@outlook.com. Succeeded with no error from the mail-send step; confirmed for real via a direct DB query — `staff.invite_created` audit row present, correctly timestamped. Throwaway script used for this was not committed (hardcoded production IDs, one-time diagnostic only, matching the same non-committed pattern as other ad hoc verification scripts tonight). | Done | |
| 12 | Investigate and fix Platform Admin tenant-detail view's error-state gap | Milestone 8's status line flags "task 9 has a flagged non-blocking error-state gap, not revisited" with no further detail anywhere else in that milestone's file — the gap itself was never actually written down. First step is archaeology: find what the gap actually is (likely how the tenant-detail view — `organizations.status` + per-provider health badges, `apps/api` platform-admin tenant-detail endpoint/`apps/web` platform tenant-detail page — behaves when a health check errors rather than returning a real status), then fix it. **Archaeology (2026-08-14):** the tenant detail page loaded tenant data and provider health in one `Promise.all`; a failed/rejected health request could prevent the otherwise successful tenant detail from rendering, while a non-OK health response was silently left as the misleading initial `checking` badges. **Acceptance**: the actual gap is identified and written down here before being fixed (so this doesn't repeat the original problem of an undocumented known issue); fix verified against a real forced-error condition, not just code review. **Done 2026-08-14** — `f90e95a`. Reviewed: `Promise.all` → `Promise.allSettled` so a health-check failure can no longer block the tenant detail from rendering; a new explicit `unavailable` status (neutral badge, distinct from `checking`/`healthy`/`unhealthy`) replaces the old silent-stuck-on-`checking` behavior. Real test forces a 503 on the health endpoint and asserts tenant details stay visible with `unavailable` shown — genuine forced-error proof, not just code review. Independently re-verified: ran `page.test.tsx` myself (10/10 passing). | Done | |
| 13 | List/remove manual availability blocks | Disclosed scope cut from Milestone 10 (Tasks 7 and 9): `createAvailabilityBlock` (`apps/api/src/tenancy/availability.controller.ts`/`availability.service.ts`) has no list or delete counterpart, and the Calendar UI (`apps/web/app/dashboard/calendar.tsx`) is create-only — the form explicitly tells staff existing blocks can't be managed yet. Add a list endpoint (tenant/property-scoped, matching the existing rate-plan/rule endpoint guard pattern) and a delete endpoint (Owner/Admin + verified email, audit-logged, matching sibling delete endpoints in the same controller), then wire both into the Calendar UI so staff can see and remove an existing block. **Acceptance**: real e2e round-trip (create → list shows it → delete → list no longer shows it, and the freed inventory is available again per Task 4's availability logic); Calendar UI lets staff view and remove a block for real, not just via the API. **Done 2026-08-14** — `3c4b9ad`. Reviewed: both endpoints correctly tenant/property-scoped (`withTenantTransaction` + explicit `WHERE tenant_id = ... AND property_id = ...`), delete requires Owner/Admin + verified email matching sibling deletes, list stays open to any tenant member matching the existing rate-plan/rule guard pattern, delete is audit-logged (`availability_blocks.remove`) in the same transaction. Real e2e proves the full round-trip including a direct DB assertion on the audit row and confirming availability is genuinely restored after removal. Independently re-verified: ran `manual-availability-blocks.e2e.spec.ts` myself (passing) rather than trusting the report. | Done | |
| 14 | Local atomic multi-room reservation | Reopens Milestone 11.5 Task 7 (deferred 2026-08-05, owner decision to bring back into this milestone 2026-08-14 — note: 11.5's own close-out said this and Tasks 8/9/12 below are "better scoped together as a deliberate 'Clock multi-channel + multi-room' milestone once there's a real signal," which hadn't arrived as of that close-out; the owner has now made the call to proceed anyway). Guest can request N rooms in one order; all N checked and held atomically (all-or-nothing) before payment, reusing the existing anti-double-booking mechanism (`pg_advisory_xact_lock` pattern from Milestone 10 Task 4). Optional per-room guest name, defaulting to the primary guest's name. Rejects the whole request pre-payment if any room isn't actually available. **Acceptance**: real e2e proving the all-or-nothing guarantee — a request where one of N rooms isn't available rejects the entire order without charging or holding any of the others. **Done 2026-08-14** — `cf8fe2b`. Reviewed: `MultiRoomBookingService.create` correctly tenant/property-scoped throughout, advisory locks acquired in sorted order across all requested rooms/room-types before any reservation attempt (deadlock-safe), no booking rows inserted until every room in the order is genuinely reserved — a failed reservation throws and rolls back the whole transaction, including any earlier holds in the same order. Real e2e directly proves this at the DB level (not just via the API response): zero `bookings` rows and zero `booked_units` anywhere after a forced one-room-unavailable rejection; a genuine 2-room success correctly records per-room guest-name snapshots (falls back to the primary guest when not set). Reuses the existing `integration_operations` idempotency pattern correctly. Independently re-verified: ran the test myself. | Done | |
| 15 | Extend payment-gated flow to multi-room bookings | Reopens Milestone 11.5 Task 8 (same reopening context as Task 14, depends on it). Applies Milestone 11.5 Tasks 4-6's payment-gated pattern (`ClockBookingService.attachRealReservation`, real Clock create only after payment confirms) across every room from Task 14: one Clock booking per room, sharing a traceable parent/child reference scheme (e.g. `must-order-X` / `must-order-X-room1`) stored identically in MUST and Clock. A per-room Clock failure after payment routes only that room to `ManualReviewItem`; sibling rooms that succeeded stay confirmed, no auto-cascading cancellation. **Acceptance**: real sandbox e2e covering the full multi-room pay → confirm chain, including one deliberately-forced per-room Clock failure proving sibling rooms aren't affected. **Code reviewed and reviewed clean 2026-08-14, not yet Done — real Clock verification still required per this task's own acceptance criteria.** `continueMultiRoomOrderAfterPayment` (new, in `local-pms.provider.ts`) correctly fans a single confirmed gateway charge out to every sibling under the same `order_reference`, locked `FOR UPDATE`; each child's `attachRealReservation` call is independent — a failure records a `ManualReviewItem` scoped to just that child (reusing the exact pre-existing Milestone 11.5 success/failure transitions unmodified) while the loop keeps confirming its siblings, no auto-cascading cancellation. Real bug correctly caught and fixed: both `pokpay-payment.service.ts` and `stripe-webhook.service.ts`'s webhook amount-verification now sums across all order siblings (`LEFT JOIN LATERAL`, `COALESCE`d back to the single booking's own amount for ordinary bookings) — without this, a legitimately-paid multi-room order's own webhook would fail its amount-match check against just the anchor room's price. Per-room guest name correctly threaded into the real Clock `guest_first_name`/`guest_last_name` fields via `COALESCE(room_guest_first_name, guests.first_name)`. Real e2e (run by me) proves the full chain end to end with a genuinely forced second-room Clock failure: room 1 lands `CONFIRMED` with a real `externalBookingId`, room 2 lands `PMS_UNKNOWN_RESULT` with a `manual_review_items` row scoped to it and nothing else, and the `payments` row records the correct **summed** amount across both rooms (not just the anchor room's price) — directly proving the webhook fix works, not just that it compiles. Ran the full local-pms-provider/guest-journey regression alongside it: no regression to single-room bookings. **Real Clock trial run 2026-08-17 — Done.** Deployed all pending commits to production first (nothing in Tasks 10-20 had ever been pushed before this — 19 commits landed together, CI green, homelab redeployed, health confirmed). Created a real 2-room order against Empire Beach Resort's production Clock account (DBL room 101 + FAM room 120, 2026-11-15, `must-order-c8d26ef9-...`, 600.00 EUR via real PokPay checkout, staging test card). Verified directly in the database and API logs, not just the API response: both bookings landed `CONFIRMED` with real Clock booking IDs (`37825282`, `37825284`); the logs show two complete, independent real Clock sequences (`rates` → `POST bookings/` → `folios` → `deposit credit_items`) — genuine proof of the per-room fan-out, not simulated. The `payments` row shows `PAID`, `600.00 EUR` — the exact sum of both rooms, directly confirming the webhook amount-verification fix works against a real PokPay webhook, not just the mocked test. | Done | |
| 16 | Real Clock webhook event processing | Reopens Milestone 11.5 Task 9 (deferred as "still blocked on capturing a real Clock webhook payload — undocumented shape"; same reopening context as Task 14). BullMQ worker processors for `clock.webhooks` (currently skeleton — log receipt only, per Milestone 11 Task 9/11) need to actually apply received Clock events to local mirror data (rooms/rates/reservations). **Before implementation work starts**: confirm with the owner whether the original blocker (Clock never documenting its webhook payload shape) is actually resolved — if not, this task may need to start with capturing a real webhook payload from Empire Beach Resort's Clock account (requires a live Clock Message Channels/SNS subscription actually configured, which Task 5's security review noted doesn't exist yet either) before any processing logic can be written against a real shape rather than guessed. **Acceptance**: a real Clock-side change (rate edit, external channel booking) is observed to update local mirror data via the webhook path, not just queued and logged. **Parked (2026-08-14)** — confirmed the blocker is still fully open: `CLOCK_WEBHOOK_FLOW.md` states Clock's payload shape is still "a guess" and no live Clock Message Channels subscription exists for Empire Beach Resort's account. This requires Clock account-dashboard configuration, not code — owner decided to leave this parked rather than dispatch it into the same guesswork already documented, until there's a real reason to chase it down. | Parked | |
| 17 | Clock-origin data visibility tags | Reopens Milestone 11.5 Task 12 (blocked on Task 9 by definition — depends on Task 16 above). Clock-sourced room types/rooms/reservations appear inside the existing Accommodations/Reservations dashboard sections, tagged "ClockPMS" so staff can tell origin at a glance. **Acceptance**: real Clock-originated data (from Task 16's webhook sync) visibly carries the tag in the dashboard UI. **Parked (2026-08-14)** — depends entirely on Task 16, parked alongside it. | Parked | |
| 18 | Multi-room pay-at-hotel bookings never create a real Clock reservation | Found during Task 15 review (2026-08-14): single-room `createBooking` calls `attachRealReservation` immediately for `PAY_AT_HOTEL` (`local-pms.provider.ts:511`, per Milestone 11.5 Task 4's "safe to call immediately since nothing is charged online" design), but `MultiRoomBookingService.create` only wires Clock attachment into the `STRIPE_CHECKOUT`/`POKPAY` branches — there is no equivalent for `PAY_AT_HOTEL`. A Clock-connected property's multi-room pay-at-hotel booking is marked `CONFIRMED` locally with no real Clock reservation ever created for any of its rooms, silently. Add the same immediate per-room `attachRealReservation` call (matching Task 15's per-room-isolation pattern — one child's Clock failure must not affect its siblings) to `MultiRoomBookingService.create`'s pay-at-hotel path. **Acceptance**: real e2e proving a multi-room pay-at-hotel order gets a real per-room Clock reservation immediately, with the same per-room failure isolation Task 15 already proved for the payment-gated path. **Done 2026-08-17** — `14fd0b5`. Reviewed: correct per-room isolation reusing `attachRealReservation` exactly as Task 15 established (each room's status set independently from its own attach result); non-Clock-connected properties get a clean bulk `CONFIRMED` transition (verified `PMS_CREATION_PENDING → CONFIRMED` is a valid, defined state-machine edge, even though this bulk update bypasses the shared `transition()` helper other call sites use — harmless here, but a minor consistency nit worth knowing about). **Real e2e proves the happy path** (both rooms get a real per-room `externalBookingId`) **but not a forced per-room failure case** for pay-at-hotel specifically, unlike Task 15's payment-gated test — lower risk since it's the identical proven loop/isolation logic, but the acceptance criteria's "same per-room failure isolation Task 15 already proved" wasn't literally re-demonstrated here. Independently re-verified: ran the test myself. Build/lint/format all clean. | Done | |
| 19 | Multi-room cancellation doesn't fan out to sibling rooms | Found during Task 15 review (2026-08-14): `LocalPmsProvider.cancelBooking` has no `orderReference` handling at all, unlike `continueAfterPayment` (Task 15) and `expirePaymentPending` (Task 15), which both correctly fan out across every sibling under the same `order_reference`. Cancelling one room of a multi-room order today only cancels that single room — real Clock reservation and payment refund included — leaving sibling rooms still fully confirmed and charged, with no way to cancel the whole order as a unit through the existing API. Add the same `order_reference` fan-out `cancelBooking` already lacks, reusing the existing per-room self-service-window/refund-policy checks and Clock cancellation for each sibling (matching the pattern already established in the two sibling methods). **Acceptance**: real e2e cancelling one booking ID from a multi-room order correctly cancels every sibling under the same `order_reference` — real Clock cancellation and refund policy applied per room, matching single-room `cancelBooking` behavior for each. **Code reviewed 2026-08-17 — the blocking bug is fixed (see Task 20), still In review pending the same real Clock trial Task 15 needs.** `5ad692c`'s validation (ownership, state-machine eligibility, self-service window) and bounded-refund logic are solid; `1e1c55b` (Task 20) fixed the drift bug found on first review. Full e2e coverage (real per-room Clock cancellation happy path, plus the forced-partial-failure case) run by me, both passing, alongside `local-pms-provider.e2e.spec.ts` for regression. One smaller, non-blocking nuance worth tracking: the fix necessarily reorders cancellation so each room's local state is written immediately after its own real Clock success, with the aggregate refund happening afterward — if that refund itself then fails, the affected rooms are correctly cancelled everywhere (Clock and local agree, no drift) but not yet refunded, with no `ManualReviewItem` flagging that specific case the way a Clock-cancellation failure now does. Lower severity than the original bug since it's a visible, recoverable state (an error is returned, nothing is silently wrong) rather than silent data corruption — worth a follow-up if it turns out to matter in practice, not blocking. **Real Clock trial run 2026-08-17 — Done**, same combined live trial as Task 15. Cancelled one booking ID (`f9218c09-...`, room 1) from the real order via the guest `DELETE` endpoint; verified directly in the database that **both** rooms landed `CANCELLED` (`f9218c09-...` and `2f8cbdbc-...`), not just the one targeted. Confirmed in the API logs: a single DELETE request triggered two complete real Clock cancellation sequences (`GET`/`PUT bookings/{id}` → 200) for both `37825282` and `37825284`, sequentially — genuine proof the fan-out drives real per-room Clock cancellation, not simulated. `cancellation_is_free = false` for both rooms as expected (no cancellation policy configured on these rate plans — Task 7's still-open gap), so no refund was attempted; this is correct behavior given current configuration, not a bug in this task. | Done | |
| 20 | Multi-room cancellation can leave Clock and the local database disagreeing on a partial failure | Found reviewing Task 19 (2026-08-17): `cancelMultiRoomOrder` (`local-pms.provider.ts`) cancels each sibling's real Clock reservation in one loop *before* writing any local `bookings.status` change in a second loop. If an earlier room's Clock cancellation genuinely succeeds and a later room's then fails, the function returns a failure without throwing — the transaction commits with no local changes at all, leaving the already-Clock-cancelled room still showing `CONFIRMED` locally with no `ManualReviewItem` recording the drift. Redesign to match Task 15's own confirmation-side philosophy: isolate a single room's Clock-cancellation failure (record it to `ManualReviewItem`, leave that room's local state reflecting reality) rather than letting one room's failure silently strand an earlier room's already-completed real cancellation. **Acceptance**: real e2e forcing a second room's Clock cancellation to fail after the first room's genuinely succeeds — the first room's local status must correctly reflect its real cancelled state at Clock (not silently reverted or left stale), and the failure must be visible via `ManualReviewItem`, not just a generic error returned to the guest. **Done 2026-08-17** — `1e1c55b`. Reviewed: Clock cancellation and the corresponding local write now happen together per room inside the same loop iteration (`applyMultiRoomCancellation`, called immediately after each successful `cancelRealReservation`), so a later room's failure can no longer strand an earlier room's already-completed real cancellation. A failed room's Clock-cancel now records a `ManualReviewItem` (`UNKNOWN_RESULT`) and the room is correctly left in its real (uncancelled) state instead of being silently skipped. The refund step was correspondingly moved to only refund rooms that actually finished cancelling. Real e2e forces exactly the scenario the acceptance criteria describes and proves it precisely: room 1 lands `CANCELLED`, room 2 stays `CONFIRMED`, a `manual_review_items` row exists for room 2 only, overall result reports `ORDER_CANCELLATION_PARTIAL_FAILURE`. Independently re-verified: ran the test myself alongside the single-room regression suite — both pass, build/lint/format clean. | Done | |
| 21 | Wire the missing `booking-accommodation.js` script on the Select Accommodation page | Found during a live guest-journey walkthrough (2026-08-17): the results-toolbar filter buttons (`must-booking-results-toolbar`) and each room's "Additional Details" expand section (`must-booking-room-modal-trigger` / `must-booking-room-inline-details`) do nothing when clicked. Root cause confirmed by code inspection, not guessed: `booking-accommodation.js` fully implements both behaviors (`initAccommodationFilters`, `toggleInlineDetailsFromTrigger`) and the template markup for both is complete and correct (`booking-accommodation.php`'s toolbar at ~L105-260 and inline-details section at ~L449-540) — but the script is never enqueued. `accommodation-page.php`'s `enqueue_booking_accommodation_page_assets()` only calls `enqueue_shared_booking_assets()` (shared CSS); a repo-wide search confirms `booking-accommodation.js` and its expected `mustBookingAccommodationConfig` localization object are referenced nowhere else. Fix: enqueue the script on this page (flatpickr as a dependency, matching `booking.php`'s own enqueue pattern for its calendar step) and `wp_localize_script` a real `mustBookingAccommodationConfig` (ajaxUrl, ajaxAction, labels, icon URLs) so AJAX room selection also gets the instant-update path instead of always falling back to a full-page POST. **Acceptance**: real browser verification on the Select Accommodation page — both toolbar buttons (dates, guests/rooms) open and close their filter panel; each room's "Additional Details" button opens and closes that room's inline section; existing full-page room-selection behavior is not regressed. | Not started | |
| 22 | Accommodation-type filter: let Calendar-page guests get back to "all types" | Found during the same walkthrough (2026-08-17): arriving at `/booking` with `accommodation_type` set (from a Booking Search widget linked to a category-filtered Rooms List widget) either hides the type picker entirely (`fixed_room_mode` triggers for `ROOM_TYPE_ONLY`/`MIXED` properties — `booking-page.php` ~L238-263, showing only a locked "Selected Room" summary card) or preselects it in a `<select>` that has no "All types" option at all (`INDIVIDUAL_ROOM_ONLY` — `get_booking_categories()`, `booking-page.php` ~L98-109, returns only real room-type entries, no "all" sentinel). Either way, a guest who searched from one room type's widget has no way back to browsing every type without editing the URL by hand. Per `docs/UI_UX.md`'s own documented intent, `accommodation_type` from a widget is meant to be a pre-applied filter, not a permanent lock. Fix: add an explicit "All types" option to the rendered `<select>`, and for `ROOM_TYPE_ONLY`/`MIXED` treat the widget's `accommodation_type` as a clearable pre-filter rather than forcing `fixed_room_mode` — reserve `fixed_room_mode`/the locked summary card for genuine direct-room-entry links that carry a specific `room_id`, which is `resolve_fixed_physical_room`'s actual documented purpose. **Acceptance**: real browser verification — starting a search from a category-filtered widget lands on the Calendar/results step with that category pre-applied but visibly clearable, and clearing it shows every type/room; a direct single-room "Book Now" link (with `room_id`) still skips straight to the fixed-room summary unchanged. | Not started | |
| 23 | Filter Select Accommodation results by requested guest capacity | Found during the same walkthrough (2026-08-17): the `guests` count from search is threaded through as a hidden field and shown in the results-toolbar summary, but is never compared against `maxOccupancy` anywhere — not in `accommodation-page.php`'s room query (~L230-287), not in the public catalog API (`apps/api/src/tenancy/public-catalog.service.ts`). A search for 4 guests still returns rooms whose `maxOccupancy` is 2. Minimum fix for this task: exclude rooms whose `max_guests` is below the requested `guests` count from the results when `room_count` implies a single room (0 or 1). Explicitly out of scope for this task (file as a separate follow-up if wanted): suggesting a multi-room combination (e.g. 2 rooms × 2 guests) when no single room covers the requested count — that reuses `MultiRoomBookingService` (Task 14) and needs its own scoped design, not a mechanical filter change. **Acceptance**: real e2e/browser proof — a search for more guests than any available room's `maxOccupancy` no longer returns that room; a search a real room can satisfy is unaffected. | Not started | |

## Task 5 security review (2026-08-14)

The review covered the API's anonymous catalog, availability, quote, booking, and guest-booking routes; the Stripe, PokPay, and Clock callbacks; the WordPress plugin; tracked configuration; dependencies; and application-controlled outbound URLs. The WordPress retrofit no longer registers the old plugin public REST routes documented in its historical integration reference: it is a server-side client of the MUST API, and no longer holds payment or PMS credentials.

Fixed in this task:

- The anonymous API surface and payment/PMS callbacks now have Redis-backed, peer-IP rate limits. Limits are scoped by endpoint and tenant/property where applicable; caller-provided forwarded-address headers are deliberately not trusted.
- Clock SNS callbacks now require the connection's exact encrypted `snsTopicArn` value before accepting a valid SNS signature. This closes the previously missing source binding: a message signed by another legitimate AWS SNS topic is rejected and is not queued or stored.
- The dashboard's direct Next.js dependency was updated to 16.3.1, removing the dependency-audit findings inherited through its prior `sharp`, `postcss`, and `nanoid` tree. `pnpm audit --prod` subsequently reported no known vulnerabilities.

Checked and retained:

- Stripe verifies its raw body with the configured Stripe webhook signature before tenant/database work. PokPay has no separately documented webhook shared secret; its callback accepts no payment assertion and only succeeds after an authenticated PokPay order reread, exact local order/session binding, amount/currency match, and idempotent state transition.
- API guest return URLs are exact-origin checked against the property's configured public origin. The WordPress API origin is configured only by a WordPress administrator and is restricted to HTTPS (apart from local development), without credentials, query, or fragments. Clock SNS certificate/confirmation fetches are restricted to AWS SNS HTTPS hosts.
- No tracked `.env`, private-key, or provider-token value was found. The only tracked environment files are examples. Secret-like test fixtures remain in test files only.

Consciously deferred:

- Existing Clock connections must have their exact `snsTopicArn` added before their SNS callbacks can be accepted. This is fail-closed by design; no live Clock Message Channels subscription is currently configured for Empire Beach Resort, so no working production callback is interrupted. If a subscription is later enabled, its ARN must be saved with the connection first.
- Clock's configurable `host` is an authenticated tenant-integration setting, not an anonymous route. The documented integration contract also permits a deliberately configured HTTPS proxy base, so an allowlist cannot be imposed safely without an owner decision on proxy support. It remains an admin-only configuration surface; a future multi-tenant security pass should add an explicit outbound-host allowlist/proxy policy before untrusted tenants receive PMS access.
