# Milestone 21: Clock PMS Certification Fixes

Status: Scoped 2026-09-10
Runs ad hoc, parallel to the main 0-15 sequence — same pattern as Milestone 20 (Staff Dashboard Restructure). Not blocked by Milestone 13/14/15 and doesn't block them either.

## Goal

Fix the concrete issues Clock's integration team raised on a call 2026-09-10, reviewed against Clock's own certification requirements. Scope and priority order set by the owner on the same date.

## Context

Investigated against the current `apps/api/src/integrations/clock/clock-booking.service.ts` and related files before this file was written — see task notes below for exact file:line references. One item from the call needed no fix: `lock_version` handling was verified correct as already implemented (fresh `GET` immediately before every `PUT`, never cached) — no task for it here.

## Sequencing

Owner's stated priority (2026-09-10): folio close and payload minimalism first (small, low-risk); duplicate-guest handling and occupancy next; document types is explicitly low priority; rates management is deliberately last, tackled only once everything else here is done, and gets its own detailed task breakdown at that point rather than being fully specified now (per the "leave this for last" instruction).

## Phase A — Small, low-risk fixes

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | Close the deposit folio in Clock after posting a deposit | `ClockBookingService.postDeposit()` (`clock-booking.service.ts:535-633`) opens/reuses an open deposit folio and posts a `credit_item` to it, but never closes the folio — confirmed by Clock as required. **Exact contract, pulled from Clock's Postman collection ("Clock PMS+ API Docs", request `folio_close`)**: `POST https://sky-eu1.clock-software.com/base_api/{subscription_id}/{account_id}/folios/{folio_id}/close`. Body is optional: `{ "document_type_id": <int> }` — closes the folio as a specific fiscal document type (invoice/credit invoice/etc, see Task 7); omit it to close without generating one. Requires the API credential to have the "Folio: Close" right granted, and possibly "Folio: Close folio with outstanding balance" depending on whether MUST ever closes a folio with a remaining balance. Add the close call immediately after the credit item posts successfully — Task 7 (low priority, done later) adds real `document_type_id` selection on top of this; this task can ship closing with no `document_type_id`. Decide and document in the PR: if the close call fails after the credit item already posted, does the booking/payment still count as successful locally (recommended — money already moved, don't roll back a real payment over a follow-up API call failing) with the close failure logged/retried, or does it fail the whole operation? Must not touch `ClockFolioHydrationService`'s read-only visibility path (`clock-folio-hydration.service.ts`) — that stays read-only into MUST's own records; this task only adds one outbound write. | **Blocked — code deployed and live-verified 2026-09-10, but Clock's account doesn't grant the required right yet** | |

**2026-09-10 live verification result**: implemented directly (not dispatched to Codex — Claude implemented per owner request), deployed to `booking.must.al`, and verified against a real booking on the demo tenant (Clock booking `38262037`, folio `76431942`, credit item `64454785`, €700 PokPay deposit). The deposit posted correctly and the booking confirmed — but the close call failed: `manual_review_items` recorded "Deposit was posted to Clock but the folio could not be closed: The User doesn't have right: 'Folio: Close'". This is exactly the failure-handling path the acceptance criteria specified (payment not rolled back, ManualReviewItem + notification recorded, booking stays CONFIRMED) — it worked as designed. The blocker is external: Clock's API user for this account does not have the "Folio: Close" permission granted. **Needs Clock's team (or whoever administers the account) to grant that right before this can be marked Done** — no further code change expected once it's granted; re-run the same test to confirm.
| 2 | Send only changed fields on booking UPDATE PUT | `updateBooking()` (`clock-booking.service.ts:774-824`) always sends both `arrival` and `departure` on every update PUT, falling back to the existing unchanged value when only one date actually changed — per the owner's email quoting Clock's guidance to send only changed fields on booking updates (no doc link was given for this one; it's from the email text). Change the PUT body construction to include only the fields present in `command` (e.g. `startsOn` → `arrival` only if `command.startsOn` is set, same for `endsOn`/`departure`); `lock_version` is always included regardless, since it's required for the concurrency check, not a "changed field". Same principle applies to `cancelRealReservation`'s PUT (`clock-booking.service.ts:864-881`) if it ever grows beyond `status`+`lock_version`. **Verification**: an update that only changes the arrival date sends a PUT body without a `departure` key at all (not the old departure value); existing update/cancel tests updated and passing. | **Done — implemented and deployed 2026-09-10, verified by code inspection only** | |

**2026-09-10 note**: implemented directly (Claude, per owner request), deployed to `booking.must.al`, container rebuilt and healthy. Could not live-test against a real Clock call the way Task 1 was: no existing test exercises `updateBooking`'s real PUT body (the one spec touching it mocks `ClockBookingService` entirely), and — a separate real finding — **no current HTTP endpoint calls `updateBooking` with date changes at all**. `PATCH /bookings/:id` (`booking.controller.ts:118-142`) only ever sends `expectedVersion`/`total`; the `startsOn`/`endsOn` branch is unreachable from any real product surface today. Change itself is small and low-risk (conditionally include two JSON keys) and confirmed correct by inspection + clean typecheck. Worth a follow-up: is "staff can change a confirmed booking's dates" a real gap worth its own task, or intentionally out of scope (cancel+rebook instead)?

## Phase B — Duplicate guest profiles

`ADR-0030-guest-matching-phone-signal-and-merge.md` drafted and accepted 2026-09-10 — supersedes `ADR-0015-guest-matching-rule.md`'s explicit decision that phone is never a matching signal. Read it in full before picking up Task 3 or 4.

**Design agreed with the owner 2026-09-10, scope extended same day**: exact match on **both** email and phone auto-attaches to the existing guest (this already happens today via the email-only rule and doesn't need new merge machinery — it's the existing "reuse" path, just now double-confirmed). A match on **only one** of email/phone against a *different* existing guest than the other field would suggest is a suspected duplicate — never auto-merged, always flagged for a human to review and decide. Match on **neither** is a genuinely different guest.

**Extended scope**: the owner specifically asked that this apply to Clock too, not just MUST's own database — otherwise Clock keeps creating its own duplicate guest profile even after we stop creating ours. Clock's API fully supports this (confirmed against its real Postman docs, 2026-09-10): `GET /guests/search?free_text_search=...` to look a guest up, and `main_booking_guest: "<guest id>"` on `POST /bookings/` to attach to an existing Clock guest instead of letting Clock auto-create one from the inline `guest_*` fields. See ADR-0030 point 2 for the exact mechanics — this is Task 4, a genuinely separate piece of work from Task 3's local-only matching (different API, different failure modes), so it's its own task rather than folded into Task 3.

Merge mechanics (for the manual-merge action, once staff confirms two profiles are the same person):
- Staff picks which of the two profiles is canonical (kept) — not automatic, since automated canonical-selection can guess wrong (e.g. which spelling of a name is correct). This is a human-in-the-loop review, so let the human pick.
- **Moved**: every record with a foreign key to the losing guest's id (bookings, payments, any other guest-linked table) is reassigned to the canonical guest's id.
- **Merged**: canonical keeps its own field values; any field canonical has blank that the losing profile has populated gets backfilled (e.g. canonical has no phone, losing profile does → canonical gets it). No overwriting of a field canonical already has a value for.
- **Not destroyed**: the losing guest record is never hard-deleted — mark it (e.g. `mergedIntoGuestId` pointer + excluded from active guest search/list) so booking/payment audit history stays traceable back through it. Hard-deleting a guest with real booking/payment history is a bad idea in a system that has to account for money.
- A "not a duplicate, dismiss" action clears the suspected-duplicate flag without merging, for the case staff determine it's genuinely two different people (e.g. shared family phone number).

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 3 | Extend guest matching to consider phone; flag suspected duplicates — **every property, not just Clock-connected ones** | `ClockBookingService.resolveGuest()` (`clock-booking.service.ts:1206-1256`) and `LocalPmsProvider.resolveGuest()` (`local-pms.provider.ts:1475-1524`) are two separate implementations of the identical email-only lookup (per `ADR-0015`, which was the general local-matching rule, not Clock-specific). Extend **both** per ADR-0030 point 1: also check phone against existing tenant guests; when email and phone point at two different existing guest records (or one matches while the other belongs to someone else), still create/attach as today's email-first rule would, but persist a suspected-duplicate flag linking the two candidate guest records. The Clock side of this is already implemented and reviewed (schema migration `20260910100000_guest_suspected_duplicate` adding `suspected_duplicate_of_guest_id` with a tenant-scoped FK + not-self check, plus `clock-booking-guest-matching.spec.ts`) — apply the identical logic to `LocalPmsProvider.resolveGuest`. Prefer extracting one shared helper both providers call over duplicating the block a second time, since the two implementations already drifting apart once is exactly how this gap happened — not mandatory per ADR-0030's consequences, but the better outcome. No merge happens automatically. **Verification**: same four cases proven for `LocalPmsProvider` as already proven for Clock (booking with matching email+phone still attaches to the single existing guest; booking with matching phone but a new/different email creates a new guest record *and* raises a flag pointing at the phone-matched guest, provably in the database; existing Clock-side tests still pass unchanged). | **Done — reviewed 2026-09-10** | |
| 4 | Look up and attach the existing Clock guest instead of letting Clock auto-create one | Per ADR-0030 point 2 (**corrected 2026-09-10 after first review — read the ADR's correction note before touching this**: attach on **email match alone**, not an AND with phone; the original text required both, which defeated the point for most returning guests). Two follow-ups on top of the already-landed `attachRealReservation` implementation: **(a)** loosen `clockGuestForBooking`'s combining logic from "email and phone must match the same family_id" to "email match is sufficient, regardless of phone" — the two `exactClockGuestMatch` calls and their tests stay, only the combining rule changes. **(b)** `createBooking`'s own direct `POST /bookings/` (`clock-booking.service.ts:255-272`, the pay-at-hotel/immediate path — a **separate** code path from `attachRealReservation`'s payment-gated one) still unconditionally sends inline `guest_e_mail` fields with no Clock lookup at all; apply the same `clockGuestForBooking` call there too, so both booking-creation paths behave identically. Watch the rate-limiter budget — this adds up to 2 more live Clock calls per booking on top of what Task 10 is already trying to reduce. **Verification**: unit tests updated for the loosened rule (email-only match now attaches); a real pay-at-hotel booking for a returning guest (same email, any/no phone) attaches to the same Clock `family_id` instead of creating a new one, provable via `GET /guests/search`; a genuinely new guest still gets a fresh Clock profile on both paths. | **Done — reviewed 2026-09-10, including a follow-up removing an unused phone search** | |
| 5 | Staff-facing suspected-duplicate review + merge action | Build a review surface (dashboard, likely under Guests) listing flagged suspected-duplicate pairs, each with both profiles' details (name, email, phone, booking count) side by side. Two actions: **Merge** (staff picks canonical, backend executes the move/merge/tombstone mechanics from ADR-0030 point 4 inside one transaction) and **Not a duplicate** (clears the flag, no data change). **Verification**: merging reassigns all of the losing guest's bookings/payments to the canonical guest (provable by querying before/after); the losing guest no longer appears in guest search/lists; dismissing a flag leaves both profiles untouched and removes it from the queue. | **Done — reviewed 2026-09-10** (typecheck clean, unit tests passing, thorough manual trace of the real-DB e2e test; the e2e test itself couldn't be executed live — unrelated infra issue, `ClockWebhookHealthService.onModuleInit`'s BullMQ/Redis setup hangs during any real `AppModule` boot in this local environment, blocking every e2e spec, not specific to this task) | |

## Phase C — Adults / children occupancy

Split the single `guestCount` into `adults`/`children` end to end — data model, API, both booking entry points, and the Clock payload.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 6 | Add `adults`/`children` to the booking data model and API | `Booking.guestCount` (`schema.prisma:548`) is the only occupancy field today; `CreateBookingCommand.guestCount` (`packages/domain-contracts/src/index.ts:249`) is the only command field. Add `adults: Int` (default 1) and `children: Int` (default 0) columns via an additive Prisma migration; keep `guestCount` for now as a computed/derived value (`adults + children`) rather than deleting it outright, since other code may still read it — note in the PR everywhere that still reads `guestCount` directly and whether it needed updating. Extend `CreateBookingCommand` (and `UpdateBookingCommand` if occupancy changes are ever meant to be editable — confirm with Claude before adding update support, since `updateBooking` today rejects most field changes per Task 2's neighboring code) with `adults`/`children`. `validGuestCount()` (`clock-booking.service.ts:1215-1217`) becomes validation on the two new fields (each a non-negative integer, adults ≥ 1). | **Done — reviewed 2026-09-10** | |
| 7 | Send adults/children to Clock; capture them at both booking entry points | `createBooking`/`attachRealReservation` (`clock-booking.service.ts:244-256`, `407-420`) send no occupancy breakdown to Clock at all today — add it once Clock's docs confirm the exact field name(s) for adults/children on booking create (check the API docs before implementing; don't guess the field name). Capture adults/children as real inputs, not just a total: (a) the guest-facing booking widget lives in `apps/wordpress-plugin` (`assets/js/booking-page.js` and friends) — add adult/children fields there; (b) the staff walk-in form (`apps/web/app/dashboard/walk-in-booking.tsx:21`) has no party-size field of any kind today — add one. **Verification**: a real booking created through each entry point carries adults/children through to the Clock payload, checked against a real sandbox booking (not just a unit-test payload assertion). | **Done — reviewed 2026-09-10, including a follow-up fix** (typecheck clean, full unit suite passing — 245 tests — plus real WordPress PHP checkout-to-booking test coverage). One review round caught a real gap: `QuoteService.validate()`'s occupancy-tamper check was skippable by simply omitting adults/children on the create request, letting a booking's recorded/Clock-sent occupancy silently diverge from what was actually quoted and charged (price itself stayed protected by the existing total-amount check). Fixed to be unconditional, same as every other locked quote field, with a test proving both the rejection and the no-false-positive case. **Not yet live-verified**: no real sandbox booking was made through either entry point during implementation to confirm Clock actually receives and stores the adults/children fields as expected — worth a real check on `booking.must.al` before fully trusting this against Clock's live behavior. | |

## Phase D — Fiscal document types on folio close (low priority)

**Correction (2026-09-10): this is not about guest identity documents (passport/ID card).** Once Clock's own Postman collection was pulled directly (see Phase A Task 1), the linked doc turned out to be `document_types - VIEW`: `GET https://sky-eu1.clock-software.com/base_api/{subscription_id}/{account_id}/document_types`, which returns the account's configured **fiscal/billing document types** — e.g. Invoice / Credit Invoice / Debit Invoice, each with its own `id`, print template, and fiscalization requirement. It exists purely to supply the optional `document_type_id` on the `folio_close` call from Task 1. The original phrasing ("if hotel has different document types we can synchronize the different things") means: different properties have different configured invoice types in Clock, and closing a folio should be able to specify which one to use — not a guest-identity feature. There is nothing to add to the guest/identity data model for this item.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 8 | Let folio close specify the right fiscal document type | Building on Task 1 (which can ship closing folios with no `document_type_id`): fetch the tenant's/property's configured document types from Clock (`GET .../document_types`) and let the close call pass the correct `document_type_id`. Needs a decision with the owner at dispatch time: is there always exactly one relevant document type per property (in which case just look it up and use it automatically), or does staff need to choose (in which case this needs a small settings UI, e.g. "which document type closes a deposit folio")? Default to automatic if a property has exactly one configured type; only build a picker if properties commonly have more than one. Low priority — do not block other phases on this. | Not started | |

## Phase E — Rate management (last)

This is the largest item raised — today MUST has a fully local rate-plan CRUD (`apps/web/app/dashboard/[tenantId]/rate-management.tsx`, `apps/api/src/tenancy/rate-plans.service.ts`) with zero connection to Clock, and the Clock integration only ever *reads* rates (`ClockAvailabilityService`, `rateIdForRoomType()` at `clock-booking.service.ts:1065-1085`) — no create/edit/delete/assign toward Clock exists, and bookings only work cleanly today when a property has exactly one Clock rate plan.

**Meal plans/board types are explicitly out of scope for this phase** (owner's instruction, 2026-09-10) — every task below deals only with room-only rate selection and pricing.

**Kickoff done 2026-09-10.** Clock's team raised this after certification testing only had ≤10 rates per room type; their real question was "what happens with 50 rates, and what stops a room getting quoted at €0/night." Investigated against Clock's own public Postman docs (the same verified workspace used throughout this milestone) plus a real, permanent hand-built test rate plan (`RateTest-Summer2027`, 4 room types, 5 rate variants each with deliberately varied `wbe`/occupancy caps — see `docs/CLOCK_ENDPOINT_MATRIX.md` if a rate schema reference is needed) and a live diagnostic script run against `booking.must.al`'s real Clock connection (`GET /rates/` + `GET /products` for room type DBL/41994, three occupancy scenarios). Findings:

1. **Clock's own `/products` endpoint enforces `max_adults`/`max_children` server-side** — an over-capacity request comes back `available:false` with a descriptive error (`"Maximum adults allowed - 5"` etc.), *but only when* `product_search[adult_count]`/`[children_count]` are actually present on the request. Clock does not reject occupancy silently or return a wrong price; it just doesn't check at all if those fields are omitted.
2. **Clock's `/products` does *not* filter out `wbe:false` rates.** A rate explicitly marked "don't publish to the booking engine" still comes back `available:true` with a real bookable price, identical in shape to a `wbe:true` rate for the same room type. Confirmed directly: `DBL - Summer no WBE` (`wbe:false`) returned `available:true, price:11000` for a 1-adult stay, indistinguishable from the legitimate `wbe:true` `DBL - Summer` rate at the same price. **This is the actual mechanism behind Clock's "what if we picked a €0 or wrong rate" warning** — nothing in Clock's API stops an integrator from quoting a non-public rate; that filtering is entirely the integrator's job.
3. **A second, unrelated bug found by the same test**: `ClockAvailabilityService.getQuote`'s offer selection (`clock-availability.service.ts:223-228`) is `Object.values(roomType.rates).flat().find(available && no-errors)`. `roomType.rates` is a plain object keyed by Clock's numeric rate id as a string — and JavaScript enumerates integer-index object keys in **ascending numeric order regardless of insertion order**, not API response order. In all three test scenarios the code picked whichever valid rate happened to have the numerically lowest id, not the first one Clock returned, not the cheapest, not any considered business rule. In this test it happened to land on the property's original real rate (`784160`, lowest id) every time — accidentally correct here only because that rate happens to have the lowest id and no occupancy cap. A newly created promotional rate with a lower numeric id than the intended primary rate would silently win instead. This needs fixing regardless of the `wbe` fix below.
4. `QuoteInput.adults`/`.children` (`quote.service.ts:19-20`) are optional at the type level; `getQuote` only forwards them to Clock `if (query.adultCount !== undefined)`. Every *current* real call site (`quote.service.ts:84-85,112-113`) does pass them today, so this isn't an active incident, but it's a silent footgun: Clock's occupancy enforcement (finding 1) only fires because today's callers happen to always populate them, not because anything guarantees it.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 9 | Kick off rate management scope | Work out with the owner what "create/edit/delete/assign rates" needs to cover, grounded in Clock's real docs/behavior rather than guesses, and break it into concrete tasks. | **Done — kicked off 2026-09-10**, split into Tasks 12-13 below plus the open questions logged at the end of this phase. | |
| 12 | Safe, deterministic rate selection for Clock-quoted stays | In `ClockAvailabilityService`: **(a)** `ratesForRoomType()` (`clock-availability.service.ts:481-501`) must drop any rate with `wbe: false` before it ever reaches `/products`, `/rates_availability`, or `/rate_prices` — extend its response type to include `wbe` (already present on the real payload, confirmed) and filter on it; also distinguish (for the error message in (d)) "this room type has zero Clock rates at all" from "it has rates, but none are published to the booking engine." **(b)** Replace every `Object.values(...).flat().find(...)` offer-selection spot (`getQuote` at `clock-availability.service.ts:223-228`, and the per-night loop in `nightlyShapeFromAvailability`) with an explicit selection that never depends on object key iteration order: collect every `available && no-errors` offer, and when more than one qualifies, pick the **lowest price** as the *default fallback only* — factor this into one pure, exported `selectBestOffer(offers, rankOrder?)` function so Task 13 can later hand it a staff-defined rank list (see below) without another rewrite of the selection logic itself. **(c)** `getQuote`/`getQuoteWithNightlyRates`/`nightlyShapeFromAvailability` must always send `product_search[adult_count]`/`[children_count]` (and `/rates_availability`'s `adults`/`children`) to Clock — default to `adultCount ?? 1`/`childrenCount ?? 0` inside `ClockAvailabilityService` itself rather than trusting every caller to populate `QuoteInput.adults`/`.children`, so Clock's server-side occupancy enforcement (confirmed real, see kickoff notes above) can never silently not run. **(d)** If wbe-filtering leaves zero rates for a room type, return a distinct, clearly-worded configuration error (not the generic "no rate configured" message) so a Clock account with no public rate on a room type surfaces immediately instead of looking like normal sold-out. **Verification**: unit tests proving (i) a `wbe:false` rate is never returned as a quote even when it's the cheapest/only-available option, (ii) selection is stable and price-based when multiple valid `wbe:true` offers exist regardless of their numeric rate ids, (iii) occupancy is enforced even when a caller omits adults/children, (iv) the existing `clock-availability.service.spec.ts` suite still passes. Live-verify against the real `RateTest-Summer2027` test data on `booking.must.al` before marking Done — that data was built specifically for this (don't delete it). | **Done — implemented and deployed 2026-09-10, live-verified against real Clock data** | |
| 13 | Staff-facing Clock rate ranking (per room type) | **Design agreed with the owner 2026-09-10**: not just read+map — staff can define an explicit **ranked priority list of `wbe:true` rates per room type** (Clock rates are inherently room-type-scoped, so a ranking never crosses room types). Rank 1 wins whenever it's valid for a given booking's dates/occupancy (per Clock's own `available`/`errors` on `/products`); if rank 1 doesn't apply, fall through to rank 2, and so on. This is deliberately *not* "always cheapest" (Task 12's fallback) — a ranked rate wins even if a lower-ranked one happens to be cheaper, since the whole point is protecting against exactly the failure Clock's team warned about (a stray low/€0 rate winning by accident). A new rate that appears in Clock but hasn't been ranked yet is never silently included or silently excluded: it sorts after every ranked rate (so it can't jump ahead of a deliberate ranking) **and** surfaces a "new unranked rate — review" flag on the page, so it doesn't sit invisible forever. Extend the shared Rates & Pricing surface (`apps/web/app/dashboard/[tenantId]/rate-management.tsx` is local-only CRUD today) so a Clock-connected property instead shows, per room type, its real live Clock rates (name, price, occupancy caps, pulled via `GET /rates/` — `wbe:false` ones never shown, they're filtered at the source by Task 12) with a drag-to-reorder rank list. New table needed (tenant/property-scoped, FK to the local room type, `external_rate_id`, `rank`, unique per room type+rate) — read-only from Clock's side, MUST only stores the ranking, never the rate data itself. `ClockAvailabilityService.getQuote`/`getQuoteWithNightlyRates` consult this ranking (via the `rankOrder` parameter Task 12's `selectBestOffer` already accepts) before falling back to cheapest. Local properties keep their existing full CRUD unchanged. **Non-goals**: no rate creation/edit/delete against Clock (the account's API user doesn't have that right today — see open questions below); no meal-plan/board-type handling (explicitly out of scope this phase). **Verification**: a Clock property's Rates & Pricing page shows real, live Clock rate data with a working rank editor; a booking that matches multiple valid rates picks the highest-ranked applicable one, provably overriding a cheaper lower-ranked one; an unranked new rate never wins over a ranked one and shows the review flag; a room type with no ranking set at all still quotes correctly via Task 12's cheapest fallback; a local property's existing rate CRUD is unaffected. | **Done — implemented and deployed 2026-09-10, live-verified against real Clock data** | |

**2026-09-10 note (Task 13)**: implemented directly (Claude, per owner request). New table `clock_rate_rankings` (tenant/property/room-type-scoped, `external_rate_id` + `rank`, unique per room-type+rate and per room-type+rank, `ON DELETE CASCADE` FK to `room_types`, RLS enabled following the exact `clock_catalog_mappings` template) via migration `20260910130000_clock_rate_rankings` — applied cleanly against a real local Postgres as a dry run before touching production. New `ClockRateRankingService` (get/set the stored order — MUST never stores Clock's rate data itself, only which ids should win and in what order) and `ClockAvailabilityService.ratesForRoomTypeDetailed()` (live `wbe:true` rates with name/occupancy caps, for display). New `ClockRateRankingController` at `tenants/:tenantId/properties/:propertyId/room-types/:roomTypeId/clock-rate-ranking` (`settings.manage`, owner/admin only) combines both for `GET`, and on `PUT` validates every submitted id against Clock's real current rates (rejects unknown ids and duplicates) before replacing the ranking in one transaction. `ClockAvailabilityService.getQuote`/`getQuoteWithNightlyRates` now fetch the ranking and pass it into `selectBestOffer` (the `rankOrder` parameter Task 12 already left in place) — a ranked rate wins over a cheaper unranked one, proven directly with a new test. Frontend: `rate-management.tsx` now checks `/pms-connection-status` and, for a Clock-connected property, replaces the local rate-plan CRUD with one card per room type listing its live Clock rates and letting staff reorder them — **up/down buttons rather than drag-and-drop** (a deliberate simplification: functionally identical for what is typically a handful of rates per room type, without taking on a drag-and-drop library dependency); unranked rates are visibly marked "not yet ranked" and always sort after ranked ones, matching the design's fail-loud-not-silent requirement. Local properties are unaffected — same CRUD as before. **Verified**: typecheck clean on both `apps/api` and `apps/web`; new unit tests (5 for `ClockRateRankingService`, 6 for `ClockRateRankingController`, 2 more on `ClockAvailabilityService` covering `ratesForRoomTypeDetailed` and the rank-override-beats-cheapest case, 1 interactive frontend test covering the reorder-then-save round trip) all pass; full existing suites still pass (API: 273 tests/48 files; web: 137 tests/38 files). **Deployed and live-verified 2026-09-10**: pushed to `main` (`7287301`), migration applied cleanly against production (and, as a dry run first, against a real local Postgres) before the container rebuild/restart, same manual `deploy.sh`-equivalent steps as Task 12. Ran a real end-to-end check through the actual services (not raw HTTP) against the real DBL room type: the live-rates endpoint correctly returned only the 5 `wbe:true` rates (the "no WBE" test rate excluded, as expected); a quote with no ranking set returned the cheapest rate (€110.00, Task 12's default); after setting "DBL - Summer 3" as rank #1, the same quote returned €325.00 — the ranked rate winning despite being far from cheapest; after clearing the ranking again, the quote reverted to €110.00. Ranking was cleared back to empty afterward so the live test property keeps its default cheapest-wins behavior for any further manual testing — the test rate *data* itself was left untouched, per the "don't delete them" instruction.

**2026-09-10 note (Task 12)**: implemented directly (Claude, per owner request) in `clock-availability.service.ts` — `ratesForRoomType()` now drops `wbe:false` rates at the source (before any `/products`/`/rates_availability` call), a new `selectBestOffer()` pure function replaces every `Object.values(...).find(...)` spot with deterministic cheapest-valid-offer selection and already accepts an optional `rankOrder` for Task 13 to plug into later, `getQuote`/`getQuoteWithNightlyRates`/`nightlyShapeFromAvailability` now always send `adult_count`/`children_count` to Clock (defaulting to 1/0) instead of only when the caller happens to supply them, and a distinct config error fires when a room type's rates exist but none are `wbe:true`. 4 new unit tests added proving each of these directly (wbe:false never quoted even as the only/cheapest offer, distinct error when every rate is wbe:false, cheapest-wins is stable regardless of numeric rate id, occupancy defaults always sent) — full existing `clock-availability.service.spec.ts` suite (15 tests) and the whole non-e2e unit suite (259 tests across 46 files) pass, typecheck clean.

**Deployed and live-verified 2026-09-10**: pushed to `main` (`dae9d16`), manually pulled/rebuilt/restarted on `booking.must.al` (the deploy-webhook service isn't running on this host yet — see [[reference_new_host_ssh_access]] — so this was done by hand, following `infrastructure/containers/deploy.sh`'s own steps: `git reset --hard origin/main`, `docker compose build`, `prisma migrate deploy` (no pending migrations), `db:set-app-password`, `up -d`; API booted clean). Ran a real `getQuote` call for the real DBL room type against three scenarios: **1 adult** → `€110.00` (the cheapest `wbe:true` rate — the `wbe:false` "DBL - Summer no WBE" rate at the identical €110 price never even reaches the decision now); **7 adults** → `€250.00` (correctly excludes every rate whose `max_adults` is below 7, picks cheapest of what's left); **occupancy omitted entirely** → still `€110.00`, proving the adults=1/children=0 default reaches Clock and its enforcement still runs. Previously (pre-fix) the same setup always returned the property's original "Normal" rate regardless of scenario, purely because its Clock id happened to be numerically lowest — that accidental behavior is gone.

## Phase H — Real booking creation never got Task 12's rate-selection fix (found live, 2026-09-10, URGENT)

**Currently live and affecting real guests on the real Must Hotel account** — not a test-only issue. Found while live-verifying Task 7 with a real PokPay-paid test booking: the booking was charged successfully (`payments` row, `PAID`, €110.00) but got permanently stuck at `PMS_CREATION_PENDING` with no `external_booking_id` and, critically, **no manual-review item and no staff-visible flag of any kind**.

Root cause: Task 12 fixed rate selection only in `ClockAvailabilityService` (the *quoting* path). The *actual real-booking-creation* path — `ClockBookingService.rateIdForRoomType()` (`clock-booking.service.ts:1575-1595`), called by both `createBooking` (line ~217) and `attachRealReservation` (line ~462) — was never touched. It still does:

```ts
if (matches.length !== 1)
  return this.failure('clock_configuration', matches.length === 0
    ? 'This room type has no rate configured in Clock yet.'
    : 'This room type has multiple Clock rates — automatic rate selection is not supported yet.');
```

DBL, FAM, APP, and Standard Rooms all now have more than one `wbe:true` rate (the `RateTest-Summer2027` setup, deliberately built for Task 12/13 testing) — so **every real booking attempt against any of these room types on the live property hits this failure right now.** For a pay-at-hotel booking the error surfaces to staff immediately (annoying, at least visible). For a paid booking (PokPay/Stripe) it does not: `attachRealReservation` returns this failure *before* any of its later success/failure branches that record a manual-review item, so the caller (`LocalPmsProvider.continueAfterPayment` → `PokPayPaymentService.processAuthoritativeOrder`) just returns the failed `Result` — the payment-confirmation transaction has already committed (payment recorded, status advanced to `PMS_CREATION_PENDING`) by the time the failure is returned, so nothing rolls back and nothing alerts anyone. Confirmed via the real timeline: PokPay's webhook got a `400` (from the thrown exception in `PokPayWebhookController`), retried, and the retry hit the "already-processed" duplicate-payment short-circuit in `processAuthoritativeOrder` — which returns `{ok:true, duplicate:true}` **without ever retrying `continueAfterPayment`/`attachRealReservation` again.** A booking that reaches this state is stuck there permanently unless someone manually intervenes; nothing in the system will ever retry it or tell anyone it needs attention.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 14 | Fix real Clock booking creation to use safe, consistent rate selection — and never fail silently | **(a)** Replace `rateIdForRoomType()`'s logic so it no longer hard-fails whenever a room type has more than one rate. It must select **the same rate the guest was actually quoted/charged for**, not just any valid one — reuse Task 12's `wbe`-filtering + `selectBestOffer` (and Task 13's ranking, via `ClockRateRankingService`) rather than reimplementing selection a third time; this needs the real stay dates + occupancy, which both call sites already have (`attachRealReservation` has the booking row's `startsOn`/`endsOn`/`adults`/`children`; `createBooking` has `command`'s). Decide with Claude where this shared logic should live before implementing — likely exporting a method from `ClockAvailabilityService` (or a small shared helper both services call) rather than duplicating `selectBestOffer` into `ClockBookingService`. **(b)** Any failure in `attachRealReservation`/`createBooking` that happens *after* the local booking/payment has already been committed must create a manual-review item + staff notification before returning — audit every early `return this.failure(...)` in both methods (not just the ones after the Clock POST call) to confirm none of them can leave a paid booking silently stranded the way this one did. **(c)** Fix the stuck-forever gap this incident also exposed: `PokPayPaymentService.processAuthoritativeOrder`'s duplicate-payment short-circuit (`booking.status !== PAYMENT_PENDING` → check for an existing payment row → return `{duplicate:true}`) never retries `continueAfterPayment` for a booking still sitting in `PMS_CREATION_PENDING` (as opposed to already `CONFIRMED`) — decide with Claude whether that path should retry the Clock attach when the booking isn't confirmed yet, or whether relying solely on (b)'s manual-review item for staff-initiated retry is preferred. **Verification**: a real booking against a room type with multiple Clock rates now succeeds and reaches Clock; unit tests cover the selection now matching what `getQuote` would have picked for the same dates/occupancy/ranking, and cover that every failure path after commit produces a manual-review item; live-verify with a real new paid test booking against the real `RateTest-Summer2027` room types on `booking.must.al` — Claude will hand you a PokPay staging checkout link if you can't complete the payment yourself, since the current test property has `pay_at_hotel`/`stripe` disabled and only `pokpay` enabled. | **Done — implemented by Codex, reviewed and deployed by Claude 2026-09-10, live-verified with a real paid booking** | |

**2026-09-10 note**: implemented by Codex — `ClockAvailabilityService` now exposes `selectRateForStay()` (returns both the winning rate id and total; `getQuote()` itself now just calls this and takes `.total`), and `ClockBookingService.rateIdForRoomType()` delegates to it instead of the old "exactly one rate or fail" check. Every early-return failure branch in `attachRealReservation`/`createBooking` now calls a new `recordPostCommitFailure()` (manual-review item + notification, verified `notify:false` on some calls is correct de-duplication since `transition()` already notifies for `PMS_UNKNOWN_RESULT`, not a dropped alert). `PokPayPaymentService`'s duplicate-webhook short-circuit now retries `continueAfterPayment` when a paid booking is still stuck at `PMS_CREATION_PENDING`, closing the exact stuck-forever gap the incident exposed; `attachRealReservation`'s own `externalBookingId` idempotency guard makes this safe to retry. Reviewed by Claude: typecheck clean, full suite passes (278 tests/50 files, including 4 new tests directly covering this fix), tenant scoping and state-machine transitions all correct. One residual, non-blocking note: rate selection now happens live at attach time rather than being pinned from quote time, so a rate/price change in the gap between quote and payment completing could in principle select a different rate than what the guest saw — the owner's read: this is a low-probability edge case in practice, since real price changes tend to happen during off-hours, not active booking windows. Deployed to `booking.must.al` (no schema migration needed, pure logic change) same day. **Live-verified with a real new paid PokPay-staging test booking against DBL** (which has 5 real rates): booking reached `CONFIRMED` with a real Clock `external_booking_id` (`38274362`); the real Clock record confirmed `adults:2, children:0` (also finally live-verifying Task 7), `rate_id:803405` ("DBL - Summer," correctly selected from among DBL's multiple rates), `total_booking_value: €110.00` matching the charge, `balance: €0`. The only manual-review item on the booking is the already-known Task 1 blocker (missing "Folio: Close" right) — nothing new, nothing silent.

### Open questions for Clock (send with the pending message)

1. **Missing right**: `POST /rate_plans/` returns `403` — `pms_api_rate_plans_create` isn't granted on this account's API user. Worth bundling into the pending message even though Task 13 doesn't currently need rate *creation* — useful to have for testing, and to know whether Clock expects integrators to ever create rates via API at all or only through their dashboard.
2. **`wbe` — is it really the one and only signal for "not for the booking engine"?** Confirmed it gates nothing server-side on `/products`/`/rates_availability` on its own. Is there any other field (`stop_from_sale`, a rate category value, something on `rate_plans` itself) that Clock's *own* WBE also checks before showing a rate, that we should be replicating?
3. **Rate Category / "Best Available Rate (BAR)"**: the dashboard rejected saving multiple rates under the same room type all marked "BAR" (`422 Room type unique category has already been taken`). Is BAR meant to be unique per room type by design (one designated "primary" rate), and does an external integrator's rate selection have any reason to prefer whichever rate is marked BAR over a plain price comparison?
4. **When multiple `wbe:true` rates are simultaneously valid for the same room type/dates/occupancy** (confirmed possible and easy to reach — this test setup does it on purpose), what does Clock's *own* WBE actually pick? Cheapest, `sort_order` (present but null/undocumented in every real example seen), rate creation order, something else? We're defaulting to cheapest (Task 12) absent a documented answer — want to confirm we're matching Clock's own real behavior rather than inventing a different rule.
5. **Overlapping seasonal date windows on different rates for the same room type** (e.g. a "Summer" rate and a "particular week in Summer" rate both marked `wbe:true` and both covering the same date) — same question as #4, specifically for the `rate_restriction.active_from_date`/`active_to_date`/`rate_prices` season mechanism rather than plain concurrent rates.
6. **Occupancy enforcement is conditional on the request supplying `adult_count`/`children_count`.** Confirmed real (see kickoff notes). Is there any Clock-side setting to make this mandatory/always-enforced regardless of what the requester sends, as a second layer of protection beyond what we control client-side?

## Phase F — Clock call efficiency (found live, 2026-09-10)

Not one of Clock's 7 original items — found while diagnosing a real "Too many Clock requests right now" error the owner hit repeatedly booking 4+ rooms one at a time through the Quick Booking (walk-in) screen. Investigated 2026-09-10: that screen only ever books one room per search-then-create cycle (no multi-room UI wired in there, unlike the separate `MultiRoomBookingService`), and each single-room cycle already makes `nights + 3` live, uncached Clock calls — most of that from `ClockAvailabilityService.getQuoteWithNightlyRates()` (`clock-availability.service.ts:231-298`), which calls Clock's `/products` endpoint once for the stay total and then loops and calls it **again for every individual night**, purely to show a nightly price breakdown in the UI. Against Clock's ~4 req/s limit (`clock-rate-limiter.ts:10`), one room's cycle is already near the ceiling; a second or third room started shortly after reliably tips it over.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 10 | Stop looping a live Clock call per night for the nightly price breakdown | `getQuoteWithNightlyRates()` (`clock-availability.service.ts:231-298`) makes 1 call for the stay total (`getQuote`) then N more calls (one per night) just to break that total down per night for display. First check what Clock's `/products` response for the full-range request actually contains — if it already carries a nightly breakdown (or enough data to derive one, e.g. a per-night rate array), drop the loop entirely and compute the breakdown from the single total call's response instead of N extra live calls. If Clock's API genuinely has no way to get a per-night breakdown from one call, that's a real constraint to document rather than route around with a hack — bring it back to Claude/the owner before building a workaround. **Do not touch** the separate re-price-at-booking-creation call (`staff-booking.controller.ts:70` → `QuoteService.price` → `getQuote`) — that one is a deliberate anti-stale-price safety revalidation (same principle as the WordPress plugin's ADR-0002, final live quote revalidation before charging), not redundant waste; removing it would reintroduce a staleness risk, not fix one. **Verification**: searching a 7-night stay for one room now makes measurably fewer live Clock calls than nights+1 (ideally 1-2 total, not 8); the nightly breakdown shown to staff is unchanged/still correct; booking 4+ rooms back-to-back in Quick Booking no longer trips the rate limiter under normal use. | **Done — implemented directly by Claude 2026-09-10** (typecheck clean, full unit suite passing including new coverage for the fast path, the rounding/reconciliation math, and the fallback). `/products` confirmed to only return a single stay-total price (per the existing, already-CONFIRMED_BY_DOCS type); `/rates_availability`'s already-documented per-date `price` field (CONFIRMED_IN_SANDBOX for its `free`/room-count fields, just not previously read for price) is used for the nightly *shape* only, scaled to the real `/products` total — never trusted as itself authoritative, with an automatic fallback to the old per-night-`/products` method if any night is missing from the single call. **Not yet live-verified**: no real sandbox call was made to confirm `/rates_availability`'s price matches `/products`' price night-for-night on a property with real per-night rate variation (weekday/weekend, seasonal) — worth a real booking.must.al check before fully trusting this on a complex rate setup. | |

## Phase G — Refund synchronization

Manual refunds are money movements in MUST's guest-payment ledger and need a matching correction in Clock for Clock-connected properties. Clock's documented deposit-refund workflow posts a negative payment to the original deposit folio even when it is closed; it does not reopen that folio or create a detached new one. Clock then requires a Deposit Adjustment to issue the correction document, but its public Base API does not expose that action.

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 11 | Sync successful manual refunds to Clock | After `PaymentRefundService.manualRefund()` succeeds for a booking with a real Clock reservation, find the original `deposit=true` folio by MUST's original payment reference and post the refund as a negative `credit_item` there, with the gateway refund ID in a stable MUST reference. Do **not** reopen the closed folio and do **not** create a new/detached folio; Clock's own deposit documentation requires the negative payment on the original deposit. The public Base API exposes no Deposit Adjustment/correction-document action, so create a tenant/property-scoped manual-review item and staff notification directing staff to issue Clock's Deposit Adjustment after a successful post. A Clock-sync failure must likewise never block or roll back the actual refund; it must be flagged for manual posting. **Verification**: unit coverage proves the signed `credit_item` payload, original-folio lookup, idempotent replay, and non-blocking manual-review paths; a real Clock-connected manual refund visibly adds the negative payment to the original deposit folio without reopening it. | Not started | |
