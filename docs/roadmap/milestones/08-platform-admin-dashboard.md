# Milestone 8: Platform Admin Dashboard

Status: Reopened (2026-08-02) — tasks 1-12 Done (task 5 satisfied by task 1's own tests; task 7's flagged non-blocking test-coverage gap closed by task 8; task 9 has a flagged non-blocking error-state gap, not revisited), but the milestone itself is not closing yet. Reopened same-day after the owner reviewed the live `/platform` screen: it doesn't reflect the Figma reference the kickoff note called for, and the dashboard-home page's own nav never got updated to link to `/platform/tenants` after task 8 added it (`platformNavigation` in `apps/web/app/platform/page.tsx` is still a hardcoded single-item array). Prior task reviews verified CSS values traced to real, active design tokens — that check is not the same as verifying the actual layout/visual language matches the Figma file, which never happened. Brainstorming additional tasks (real header/nav, full page set, actual Figma-grounded visual pass) before writing new task rows.
Depends on: Milestone 7 (Auth Pages — login, `/platform` placeholder route, component library — Done); ADR-0020 (Platform Admin vs. Tenant dashboard routing); ADR-0021 (Platform Admin cross-tenant data access)

**Split out 2026-07-31** from what was originally planned as part of Milestone 11 (Platform Billing) — see ADR-0020's amendment. Originally the Platform Admin dashboard was folded into the billing milestone because both need cross-tenant data access; splitting it into its own, earlier milestone was a deliberate resequencing decision, not a technical requirement — per ADR-0021, Platform Admin's actual backend needs (`organizations`, `users`/memberships read access; two allowlisted write actions) don't depend on Milestone 9 (Tenant Dashboard) or Milestone 11 (Platform Billing) existing first.

## Goal

MUST's own internal team (not tenant staff) gets a working `/platform` dashboard: visibility into tenants/accounts across the whole platform, whether the shared Stripe/PokPay connections are healthy, and the two allowlisted administrative actions ADR-0021 scoped — suspending/reactivating a tenant, and triggering a password-reset link for a locked-out tenant user. Done means: a `PLATFORM_ADMIN` account can log in (Milestone 7), land on `/platform`, see platform-wide stats and provider health at a glance, search a cross-tenant tenant list, open a tenant to see its status and enabled providers, and perform both allowlisted actions — all audit-logged and visible in an activity feed, with no cross-tenant write bypass anywhere.

## Draft task areas (original, before kickoff — superseded by "Tasks" below)

1. RLS read carve-out (ADR-0021 decision 1): add the `current_setting('app.role', true) = 'platform_admin'` `OR` clause to the RLS policies on tables Platform Admin needs to see — at minimum `organizations`, `users`/memberships; the full table list is a kickoff decision scoped against the real dashboard screens below, not assumed here. Application middleware sets `app.role` per request once a session resolves to `PLATFORM_ADMIN`, the same way `app.tenant_id` is already set today (Milestone 1 Task 4's mechanism).
2. `organizations.status` field (ADR-0021's Consequences flagged this doesn't exist yet) — the concrete state a tenant is suspended/reactivated into. Migration + the state-transition logic itself.
3. Two allowlisted platform endpoints, each `@Roles(Role.PlatformAdmin)`-gated, each its own dedicated route (ADR-0021 decision 3 — no generic pass-through): `POST /platform/tenants/:tenantId/suspend` (+ reactivate), `POST /platform/tenants/:tenantId/users/:userId/reset-password`. Both reuse Milestone 1 Task 4's existing per-request `SET LOCAL app.tenant_id` write path scoped to the one named tenant — no bypass-RLS role or connection (ADR-0021 decision 2, and the history behind it: Milestone 1 Task 2 already removed a superuser/bypassrls runtime connection once, this must not reintroduce it).
4. Audit log actor-shape (ADR-0021 Consequences): extend Milestone 1 Task 10's audit log to distinguish "platform admin acting on tenant X" from a tenant's own self-service actions on the same log — every allowlisted write, and any read surfacing sensitive tenant data, gets logged with the platform admin as actor.
5. Tests: RLS carve-out gives `platform_admin` role cross-tenant SELECT and confirms it grants **no** write access anywhere not explicitly allowlisted (ADR-0021 Consequences, explicit negative-case test) — companion to the existing "no session vars ⇒ zero rows" tests.
6. `/platform` dashboard shell: tenant list view (basic identity/status), replacing Milestone 7's placeholder landing page.
7. Tenant detail view: enough to see the tenant's basic state and reach the suspend/reactivate and password-reset actions.
8. Suspend/reactivate action UI, wired to task 3's endpoint, audit-visible.
9. Password-reset-trigger action UI, wired to task 3's endpoint.
10. E2E test: `PLATFORM_ADMIN` account logs in, lands on `/platform`, sees cross-tenant tenant list, suspends a tenant, reactivates it, triggers a password reset for a tenant user — each step audit-logged with the platform admin as actor; a tenant-membership account attempting any `/platform` route or endpoint is rejected.

## Explicitly not included

- Subscription/billing state, dunning, plan enforcement — that's Milestone 11 (Platform Billing), unaffected by this split except that it no longer also needs to build the dashboard shell.
- A capability/template system for platform staff (ADR-0020 decision 4) — `PLATFORM_ADMIN` is the only platform role for now; a second role is new schema/guard work at that time, not pre-built here.
- Materialized/aggregate cross-tenant reporting (ADR-0021 decision 5) — stays as live RLS-carve-out `SELECT`s until an actual performance problem exists.
- Impersonation / "view as this tenant" — explicitly out of scope per ADR-0020's decision 2; a platform account can never also hold a tenant membership, and there is no "act as" mechanism.
- Any new write/action beyond suspend/reactivate/password-reset-trigger — deliberately not reopening ADR-0021 decision 3's "no generic pass-through" boundary without a proven need.
- **Per-tenant provider health.** Confirmed by reading the actual provider code (`apps/api/src/payments/{stripe,pokpay}-payment.provider.ts`): both Stripe and PokPay are single shared platform-level connections today (`STRIPE_SECRET_KEY`, `POKPAY_KEY_ID`/`POKPAY_MERCHANT_ID` env vars) — there is no per-tenant credential, so there is exactly one real health signal per provider, not N. A tenant "having PokPay" only ever means their `pokpayEnabled` boolean is set; the health badge shown next to it is the one shared platform-wide signal, not something computed per tenant. Don't build per-tenant health scoring (e.g. per-tenant success-rate) now — no proven need yet, and it's a bigger, separate feature if one emerges.

## Kickoff resolved (2026-08-01, brainstormed with the owner)

Design brainstorm covered two things beyond the original draft above: provider connection health monitoring, and three additions to round out the dashboard home. See tasks 6, 7, 9 below for what that resolved into. Also resolved: Milestone 9's Figma file ("01 — Admin Dashboard", 22 sections, referenced in that milestone's own kickoff notes) is fair to draw on for this milestone's *visual language and structural patterns* (stat-card styling, list-with-search layout, detail-panel conventions, activity-feed styling) even though it's nominally the tenant dashboard's file — pull real tokens/patterns via Figma MCP the way Milestone 7 Task 1 did (after its first, ungrounded attempt was rejected), not invented values. Don't assume any specific section of that file maps 1:1 onto a platform-admin screen; these are new screens, not a reskin.

## Tasks

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | RLS read carve-out (ADR-0021 decision 1) | `current_setting('app.role', true) = 'platform_admin'` `OR` clause added to RLS policies on `organizations` and `users`/`tenant_memberships` (the tables the screens below actually need). Application middleware sets `app.role` per request once a session resolves to `PLATFORM_ADMIN`, the same mechanism Milestone 1 Task 4 already uses for `app.tenant_id`. No other table gets the carve-out without a specific screen in this milestone needing it.

**Submitted and reviewed (2026-08-01)**: verified the migration directly, not just the report. It adds three separate `FOR SELECT`-only policies (`organizations_platform_admin_read`, `users_platform_admin_read`, `tenant_memberships_platform_admin_read`), each purely additive alongside the existing `FOR ALL` tenant-isolation policies from Milestone 1 — since Postgres combines multiple permissive policies with OR per command, and the new policies never touch INSERT/UPDATE/DELETE, platform-admin write denial is guaranteed by construction, not just by the tests happening not to find a hole. `properties` correctly received no carve-out at all (confirmed no new policy references it). `withPlatformAdminTransaction` (`tenant-database.service.ts`) mirrors `withTenantTransaction`'s existing transaction-local `set_config(..., true)` pattern exactly — matches Milestone 1 Task 4's real mechanism (explicit per-operation invocation, not NestJS middleware, despite this row's "middleware" phrasing being imprecise) rather than inventing something new. Test coverage is genuinely rigorous, not superficial: real cross-tenant reads (two distinct tenants) proving the SELECT carve-out, an explicit assertion that `properties` returns empty, and real `UPDATE` statements attempted against all four tables under platform-admin context with every affected-row count asserted at 0. Independently reran: full API suite 28 files/59 tests (up from 57, confirms nothing else broke), full `pnpm -w build`/`lint`/`format:check` all clean, migration applies cleanly. No issues found.

**Note for task 3, not a defect here**: this task only built the DB-level primitive (RLS policies + `withPlatformAdminTransaction`). The actual `@Roles(Role.PlatformAdmin)` route guard that decides *when* to invoke it — resolving a session to `PLATFORM_ADMIN` and rejecting anyone else — doesn't exist yet and is correctly task 3's job, not scope creep missed here. | Done | |
| 2 | `organizations.status` field | Migration adding the field (ADR-0021 Consequences flagged this gap). Concrete state values covering active/suspended at minimum. State-transition logic (what's allowed from what) lives with task 3's endpoints, not scattered elsewhere.

**Submitted and reviewed (2026-08-01)**: `OrganizationStatus` enum (`ACTIVE`/`SUSPENDED`), non-null `status` column defaulting to `ACTIVE`, idempotent migration (`CREATE TYPE` wrapped in a duplicate-object-tolerant `DO` block, `ADD COLUMN IF NOT EXISTS`) — verified by running `prisma migrate deploy` twice myself, second run correctly reported no pending migrations. Test hardcodes the DB connection string directly rather than reading an env var; checked whether that was a deviation from convention before treating it as one — it isn't, all 24 e2e spec files in this suite do the same (confirmed via search), including the file Task 1 just extended. No issue. Independently reran: full API suite 29 files/60 tests (up from 59, confirms nothing broke), full `pnpm -w build`/`lint`/`format:check` clean. The disclosed Windows `prisma generate` EPERM (locked query-engine DLL) didn't affect anything real — build succeeded using the already-generated client, matching other Windows-only friction seen elsewhere this project. | Done | |
| 3 | Two allowlisted platform write endpoints | `POST /platform/tenants/:tenantId/suspend` (+`/reactivate`), `POST /platform/tenants/:tenantId/users/:userId/reset-password` — each its own dedicated route, each `@Roles(Role.PlatformAdmin)`-gated, no generic pass-through (ADR-0021 decision 3). Both reuse the existing per-request `SET LOCAL app.tenant_id` write path scoped to the one named tenant — no bypass-RLS role/connection reintroduced (ADR-0021 decision 2; Milestone 1 Task 2 already removed one of these once).

**Submitted and reviewed (2026-08-01), reviewed carefully given the security stakes.** Traced the full guard chain, not just the endpoints: `TenantContextGuard` now explicitly no-ops for routes with no `@TenantScoped()` and `Role.PlatformAdmin` in their required roles (`if (!scope && requiredRoles?.includes(Role.PlatformAdmin)) return true`), deferring entirely to `RolesGuard`, which does its own independent session→user→`isPlatformAdmin` resolution for that branch — correct, since a platform account can never hold a tenant membership (ADR-0020), so a tenant-membership check would never apply here anyway. Both guards are registered as global `APP_GUARD`s (`app.module.ts`), so this can't be accidentally skipped by a future controller forgetting to apply it.

The write path is the important part: `transitionTenant` reads the org via task 1's `withPlatformAdminTransaction` (cross-tenant, read-only) to check current status, then performs the actual `UPDATE` via `withTenantTransaction({ tenantId })` — meaning the write is authorized by the *ordinary* tenant-isolation RLS policy (`app.tenant_id` set to the one named tenant), not any platform-admin bypass. Confirmed directly: no RLS write policy anywhere references `app.role` (verified in task 1's review) — so this endpoint has no special write privilege at the database layer at all; it works because the application layer legitimately sets tenant context to the one target tenant, the same way that tenant's own admin would. Genuine idempotency, not just claimed: the `UPDATE` restates `AND "status" = ${from}` in its `WHERE` clause and checks `RETURNING`, so a concurrent double-suspend has its second request affect 0 rows and correctly 409 — real optimistic-concurrency protection against a double-click or race, verified by reading the SQL, not assumed. `triggerPasswordReset` verifies the target user is actually a member of the named tenant (via `requirePlatformVisibleTenantUser`) before doing anything, closing off a platform admin resetting an arbitrary unrelated user's password by guessing a userId — then reuses `AuthService`'s existing token-issue-and-email logic (cleanly extracted into `triggerPasswordReset(user)`, called by both the self-service and platform-admin paths) rather than duplicating it.

Test coverage is real, not superficial: genuine HTTP login as an actual platform-admin account, real suspend→reactivate→reset-password sequence with the mail provider mock asserted to have received exactly one call with the right `userId`/`to`, and a second test logging in as a real tenant-membership account and confirming all three routes 403. Minor gap, not blocking: no dedicated test for calling suspend twice in a row (the WHERE-clause race protection is correct on inspection, just not exercised by a test) — noting for whoever next touches this, not requesting a follow-up now. Independently reran: full API suite 30 files/62 tests (up from 60, confirms nothing broke — separately reran the two platform-admin spec files together to be sure the guard refactor didn't regress the pre-existing session-routing test), full `pnpm -w build`/`lint`/`format:check` clean. No issues found. | Done | |
| 4 | Audit log actor-shape | Extends Milestone 1 Task 10's audit log so "platform admin acting on tenant X" is distinguishable from a tenant's own self-service actions in the same log. Every task 3 write, and any read surfacing sensitive cross-tenant data, gets logged with the platform admin as actor.

**Submitted and reviewed (2026-08-01)**: `AuditActorType` (`TENANT_USER`/`PLATFORM_ADMIN`) added via the same idempotent migration pattern as task 2, existing rows correctly default to `TENANT_USER`. Verified the atomicity claim directly, not just trusted it: in both `transitionTenant` and `triggerPasswordReset`, the audit-log insert runs inside the *same* `withTenantTransaction` callback as the actual status `UPDATE`/membership check — genuine same-transaction atomicity, and `transitionTenant` correctly skips the audit write entirely when the `UPDATE` affects 0 rows (a race loser doesn't get a misleading "suspended" log entry for something that didn't happen). `actorUserId` is threaded from `request.platformContext.userId`, which `RolesGuard` derives server-side from the session — not client-suppliable. Test verification queries `audit_logs` directly after all three actions and asserts the exact rows (action, actor, actor_type, tenant, target) rather than just checking HTTP responses. Independently reran with the established stable pool config: full API suite 30 files/62 tests passed cleanly, confirming the disclosed P1001 connection error was genuine concurrency flakiness in Codex's own run, not a real issue (matches the pattern seen elsewhere in this project). Full `pnpm -w build`/`lint`/`format:check` clean, migration re-applies idempotently. No issues found.

**Note for task 7, not a gap here**: `audit_logs` itself has no platform-admin RLS read carve-out yet (task 1 deliberately scoped the carve-out to just `organizations`/`users`/`tenant_memberships`) — task 7's cross-tenant activity feed will need to add one when it gets there. | Done | |
| 5 | RLS carve-out tests, including the negative case | Confirms `platform_admin` role gets cross-tenant `SELECT` on the carved-out tables and, explicitly, **no** write access anywhere not allowlisted by task 3 (ADR-0021 Consequences) — companion to the existing "no session vars ⇒ zero rows" tests.

**Already satisfied by task 1 (2026-08-01), no separate dispatch needed.** Task 1's own submission included both `tenant-rls.e2e.spec.ts` tests this row asks for — "allows platform admins to read only the carved-out tables across tenants" (cross-tenant SELECT, two distinct tenants, explicit empty-`properties` assertion) and "does not grant platform admins write access anywhere" (real `UPDATE` attempts against all four tables, every affected-row count asserted 0). Already reviewed in task 1's row above; marking Done here by reference rather than asking Codex to redo work already delivered. | Done | |
| 6 | Provider health monitoring — backend | New `ProviderHealthService`, same `Queue`/`Worker` BullMQ pattern as the existing `PaymentExpiryService` (`apps/api/src/payments/payment-expiry.service.ts`) — no new infra dependency. Repeatable job (e.g. every 5 minutes) pings each provider with the cheapest safe read available: Stripe via `GET /v1/balance`; PokPay via the same auth call `authenticatedRequest` already uses to fetch an access token (successful token fetch = healthy), since no dedicated status endpoint exists in what's integrated. Result cached in Redis per provider (`{ ok, checkedAt, error? }`), no DB migration. New `GET /platform/provider-health` endpoint (`PLATFORM_ADMIN`-gated) returns current cached status for both providers; returns an explicit "checking" state, not an error, if no check has run yet.

**Submitted and reviewed (2026-08-01)**: verified the PokPay reuse claim directly rather than trusting the description — `authenticate()` was genuinely extracted out of `authenticatedRequest` (which now calls it internally), and `checkHealth()` calls that same extracted method, so it's the identical `POST /auth/sdk/login` call, not a new one. `healthConfiguration()` still enforces the staging-only restriction (`POKPAY_TEST_MODE_REQUIRED`) exactly like the real `configuration()` does — no accidental loosening for the health-check path, it just skips requiring `webhookUrl` since a health check has no webhook to speak of. Stripe's `checkHealth()` uses `balance.retrieve()` exactly as scoped, no side effects. `ProviderHealthService` correctly closes its worker/queue/Redis connections in `onModuleDestroy` (verified this doesn't leak across the many app instantiations other e2e specs create — full suite completed without hangs). Test coverage is honest about its own limits: credentials are deliberately deleted from `process.env` in the E2E test rather than mocking network calls, so "unhealthy" here means "correctly detected as unconfigured," not "verified against a live provider" — matches the report's own disclosure, not an inflated claim. Independently reran: full API suite 31 files/66 tests (up from 62, confirms nothing broke), full `pnpm -w build`/`lint`/`format:check` clean. No issues found. | Done | |
| 7 | `/platform` dashboard shell + home | Replaces Milestone 7's placeholder. Reuses `packages/ui`'s `AppShell`/`SidebarNavigation` (Milestone 7 Task 1), styled per the kickoff note above (real Figma-derived patterns, not invented). Dashboard home has three parts: (a) stats strip — tenant count, property count, breakdown by plan, signups this week, from the RLS-carved-out `organizations` table; (b) system health panel — Stripe/PokPay badges from task 6's endpoint; (c) recent activity feed — latest platform-admin audit log entries from task 4, so accountability is visible without a separate screen.

**Submitted and reviewed (2026-08-01).** New `audit_logs_platform_admin_read` migration correctly mirrors task 1's surgical pattern (additive `FOR SELECT`-only policy, no write access granted) — actually simpler than task 1's, since it relies on the existing `audit_logs_tenant_isolation` policy for the tenant-match half instead of redundantly repeating it. Genuinely verified every CSS value in `platform.module.css` traces to a real, active entry in `packages/ui/src/tokens.ts` (checked directly against the token registry, not assumed) — no invented colors. Considered whether logging every dashboard *view* (`platform.dashboard.viewed`) into the same feed it's displayed on was a bug (routine views could bury real actions) before flagging it — checked the frontend's own panel copy, "Platform-admin actions **and reads**," which shows this is deliberate, matching ADR-0021's accountability-first framing, not an oversight. Property counts deliberately avoid extending the carve-out to `properties` (per task 1's "no carve-out without a specific need" framing) by summing per-tenant counts via `withTenantTransaction` in a loop instead — correct and conservative, though it's an N+1 pattern worth revisiting if the tenant count ever grows past what's practical for one dashboard load; not a problem at today's scale.

Independently reran: full API suite 32 files/68 tests (up from 66), full web suite 14 files/25 tests (unchanged, expected — no web test file was added for this task), full `pnpm -w build`/`lint`/`format:check` clean, migration re-applies idempotently.

**Gap, not blocking**: no `page.test.tsx` for `/platform` — every other page in this app has at least a component-level smoke test, and the old placeholder never had one either so this isn't a regression, but this is the pattern-setting first real dashboard screen for tasks 8-11 and a real data-fetching page (two parallel API calls, loading/error/success states) is exactly the kind of thing worth a basic render test. Wasn't explicitly asked for in this task's dispatch, so not holding up Done status for it — flagging for a quick follow-up, ideally before task 8 repeats the same gap. | Done | |
| 8 | Tenant list | Search/filter by organization name or owner email. Basic identity + `organizations.status` columns. Links into tenant detail. | Done | |

**Submitted and reviewed (2026-08-02).** New `GET /platform/tenants` reuses task 1's `withPlatformAdminTransaction` carve-out (no new RLS surface needed). Query is a `LEFT JOIN LATERAL` finding each org's earliest-created `OWNER` membership's email, `ILIKE` search against org name and owner email, optional status filter via the `(${status} IS NULL OR ...)` idiom — all values passed through Prisma tagged templates, verified parameterized, no injection risk despite the controller reading `request.query` manually with a loosely-typed annotation rather than `@Query()` (noted as type-safety looseness only, not a security gap — the raw query object never reaches SQL unparameterized). The list read is itself audit-logged (`platform.tenants.listed`, with `search`/`status` in `details`), consistent with task 7's dashboard-view-logging precedent.

Frontend (`apps/web/app/platform/tenants/page.tsx`) exports a presentational `TenantListView` separately from the data-fetching default export — deliberate separation that lets `page.test.tsx` render loading/populated states directly without mocking `fetch`, closing the test-coverage gap flagged in task 7's review. Table markup is genuinely accessible (`scope="col"`/`scope="row"`, visually-hidden "Open" column header, `<caption>`), search is debounced 180ms via `window.setTimeout` with cleanup on re-render. Every CSS value in `tenant-list.module.css` traces to a real, active entry in `packages/ui/src/tokens.ts` (checked directly, not assumed) — `--must-color-border`, `--must-space-8`/`--must-space-12`, `--must-color-text-primary`/`-secondary`, `--must-color-ink` all confirmed active.

Independently reran: full API suite 32 files/69 tests (up from 68), full web suite 15 files/27 tests (up from 14/25 — new `page.test.tsx`), full `pnpm -w build`/`lint`/`format:check` clean. No issues found.
| 9 | Tenant detail view | Tenant's basic state (including `organizations.status`); each provider the tenant has enabled (`stripeEnabled`/`pokpayEnabled`/`payAtHotelEnabled`) shown next to the same shared health badge from task 7's panel — enabled-state + the one real signal, no fabricated per-tenant health (see "Explicitly not included"). Reaches into tasks 10/11's actions. | Done | |

**Submitted and reviewed (2026-08-02).** The dispatch resolved one ambiguity the acceptance criteria left open before handing this off: the provider-enabled flags live on `Property`, not `Organization` (a tenant can have multiple properties), so I specified OR-aggregation across the tenant's properties plus an enabled-property count — verified the service does exactly that (`BOOL_OR`/`COUNT(*) FILTER` in a single aggregate query against `properties`, scoped via `withTenantTransaction({ tenantId })`, no new RLS policy, matching task 7's precedent for per-tenant reads). `GET /platform/tenants/:tenantId` correctly checks the org exists (platform-admin carve-out) before auditing the read (`platform.tenant.viewed`, `tenantId` set) — a 404 doesn't produce a misleading audit entry, consistent with task 3's "no audit write on a no-op" pattern. Confirmed the `stripeEnabled: true` assertion in the new E2E test is genuine, not a default: the seed `INSERT` was extended to add `stripe_enabled` explicitly for this test's property.

Frontend correctly reuses task 8's presentational/data-fetching split (`TenantDetailView` exported separately, tested via `renderToStaticMarkup` without mocking `fetch`), disabled placeholder buttons for suspend/reactivate/reset-password are genuinely inert (not wired to anything yet, appropriately deferred to tasks 10/11), provider badges show both the enabled-state (per-tenant, real) and the shared health badge (platform-wide, from task 6/7 — correctly not fabricating per-tenant health). Every new CSS value in `tenant-detail.module.css` traces to a real, active token (`--must-space-16`/`-24`, `--must-color-ink`, `--must-color-border`, checked directly).

**Gap, not blocking**: unlike task 8's list page (which sets a visible `error` state on a fetch failure), this page's `catch` block on a genuine network error just falls through to a blank page — `notFound` stays `false`, `tenant` stays `null`, and `TenantDetailView` renders `null` for that combination. Only the explicit 404 path is user-visible; a transient network failure is silent. Not required by this task's acceptance criteria (only loading/not-found states were asked for), and low-impact given `AuthRouteGuard` already handles the auth-failure case separately, but worth a quick follow-up to match task 8's precedent.

Independently reran: full API suite 32 files/70 tests (up from 69), full web suite 16 files/30 tests (up from 27), full `pnpm -w build`/`lint`/`format:check` clean. No blocking issues found.
| 10 | Suspend/reactivate action UI | Wired to task 3's endpoint, audit-visible (appears in task 7's activity feed after use). | Done | |

**Submitted and reviewed (2026-08-02).** The action button correctly derives which endpoint to call from the tenant's *current* status (`suspend` if `ACTIVE`, `reactivate` if `SUSPENDED`), and on success re-fetches the tenant detail from the server rather than optimistically flipping the badge locally — meaning the displayed status is always what the backend actually persisted, not a client guess. The 409 race case (task 3's `WHERE status = from ... RETURNING` protection) is handled correctly on both ends: the UI surfaces a visible "changed elsewhere" message *and* re-fetches to show the real current state, rather than either silently failing or leaving a stale badge. Verified this isn't just a claimed behavior — traced the actual `onTransition` handler in `page.tsx`: it throws a tagged `Error` with `.status` set from the response, caught in `TenantDetailView` and branched on `=== 409` specifically, not a generic catch-all.

Interaction test genuinely exercises the DOM via `createRoot`/`act` (not just static markup like the other page tests) — clicks the real button, asserts the pending-state text appears while the mocked transition promise is unresolved, then asserts the 409 error text appears after a rejected retry. E2E test hits the real suspend/reactivate endpoints and asserts the re-fetched detail shows `SUSPENDED`, then reactivates at the end to avoid leaving the shared fixture tenant in a suspended state for no reason (good hygiene, not required but appreciated). Confirmed the disclosed non-blocking `act()` console warning is real but harmless — a timing gap in the test's own `finally`-block state update, not a defect in the component under test.

Independently reran: full API suite 32 files/71 tests (up from 70), full web suite 16 files/31 tests (up from 30), full `pnpm -w build`/`lint`/`format:check` clean. No issues found.
| 11 | Password-reset-trigger action UI | Wired to task 3's endpoint, audit-visible. | Done | |

**Submitted and reviewed (2026-08-02).** This task needed a `userId` the detail response didn't have yet; the dispatch resolved that ahead of time by directing an additive response-shape change (`ownerUserId` alongside the existing `ownerEmail`, same `LEFT JOIN LATERAL` owner lookup, one more selected column) rather than improvising a narrower one-off — verified `getTenant` does exactly that and nothing more invasive. The reset button is correctly disabled (with an explanatory message, not just a dead click) when `ownerUserId` is null — a real edge case for a tenant with no OWNER membership yet, not hand-waved. Since the endpoint is fire-and-forget (202, queues an email), the UI correctly does *not* try to re-fetch tenant detail afterward — there's nothing to re-fetch — and instead shows a success confirmation in place, distinct from task 10's re-fetch-based pattern for a stateful action. Failure path shows a visible error, matching task 10's precedent.

Verified the E2E test's mail-assertion traces correctly: `sendPasswordResetEmailSafely` in `auth.service.ts` passes `{ userId: user.id, to: user.email, ... }` to the mail provider, and the test's `toMatchObject({ userId: ownerId, to: ownerEmail })` checks exactly that shape via a real mock array populated by the overridden `MAIL_PROVIDER`, not a stub that always "passes." Interaction test covers the full real flow: submits the correct owner ID, shows pending state, shows the success confirmation, and re-renders with a null owner to confirm the second button goes genuinely `disabled`.

Independently reran: full API suite 32 files/72 tests (up from 71), full web suite 16 files/32 tests (up from 31), full `pnpm -w build`/`lint`/`format:check` clean. No issues found.
| 12 | E2E test | `PLATFORM_ADMIN` logs in, lands on `/platform`, sees the stats strip and system health panel, searches the tenant list, opens a tenant, suspends it, reactivates it, triggers a password reset for a tenant user — each step audit-logged with the platform admin as actor and visible in the activity feed; a tenant-membership account attempting any `/platform` route or endpoint is rejected. | Done | |

**Submitted and reviewed (2026-08-02), milestone-closing task.** New `platform-admin-flow.e2e.spec.ts` uses fully isolated fixtures (its own organization/property/users, not shared with `platform-dashboard.e2e.spec.ts`) with an `afterAll` that cleans up every inserted row — deliberate, avoids any risk of polluting other spec files' aggregate assertions even though none currently assert exact counts. The one part of the acceptance criteria worth calling out specifically: "visible in the activity feed" is verified by calling `GET /platform/dashboard` *again* after suspending and asserting the suspend action appears in `activity` — i.e. checked through the real endpoint the UI actually reads, not by querying `audit_logs` directly, which is what I asked for in the dispatch and what makes this a genuine end-to-end check rather than a shortcut. The negative-case test reuses a real tenant-membership session (the seeded owner's own login) and checks 403 across every route exercised by the positive flow — same route list, not a reduced subset.

Independently reran: full API suite 33 files/74 tests (up from 32/72 — the new file's 2 tests), full `pnpm -w build`/`lint`/`format:check` clean (web suite unaffected, no web files touched by this task). No issues found.
