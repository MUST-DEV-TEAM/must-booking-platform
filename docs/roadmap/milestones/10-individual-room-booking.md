# Milestone 10: Individual Room Booking

Status: In progress — kickoff complete (2026-08-03), 10 tasks defined, task 1 not yet dispatched.
Depends on: Milestone 4 (booking domain/inventory model); Milestone 9 (Tenant Admin Dashboard, for the staff-facing manual-blocking/booking-mode UI's follow-up tasks); Milestone 6 (guest widget, gets follow-up work here); ADR-0022 (booking-mode model)

## Goal

Per [ADR-0022](../../decisions/ADR-0022-individual-room-booking-model.md): today, availability is tracked as an anonymous count per room type (Milestone 4's model) — a guest booking "Deluxe" gets whichever unit is free, and nobody, guest or system, distinguishes between them. Some tenants need the opposite: guests pick (or are assigned) a *specific* physical room, tracked individually so the same room is never double-booked, sometimes with its own price. This milestone adds that as a per-property choice — Room-Type-Only, Individual-Room-Only, or Mixed (guest may pick a specific room or let the system auto-assign one, only among same-priced rooms) — without breaking the existing pooled model tenants already use. Done means: a property can be configured into any of the three modes, guests can complete a booking correctly under each, and staff can manually block availability targeting any combination of "all," a room type, or specific individual rooms.

## Draft task areas (not final — define the real tasks at kickoff; task count is whatever the real scope needs, not fixed at 10)

1. `Property.bookingMode` setting (`ROOM_TYPE_ONLY` / `INDIVIDUAL_ROOM_ONLY` / `MIXED`), chosen once for the whole property, changeable later but never mixed per room type at a given time (ADR-0022).
2. Room-level availability tracking: extend beyond Milestone 4's anonymous per-room-type count so a specific `Room` can be marked unavailable for a date range, without breaking the existing pooled-count model for `ROOM_TYPE_ONLY` properties.
3. Optional per-room price override: a `RateRule` (or sibling concept) can price a specific `Room` differently from its room type's base rate, for properties where individual rooms are priced independently.
4. `createBooking` accepts an optional specific `roomId` (in addition to today's required `roomTypeId`), validated against the property's booking mode and that room's real-time availability.
5. Guest-facing catalog/availability API changes: expose individual rooms (with their own IDs, even where display names repeat within a type) when the property's mode requires or allows room-level choice.
6. "Same price" invariant for Mixed mode's auto-assign path: a guest may only "let the system decide" among rooms of a type that share an identical price; enforce this server-side, don't just assume the data satisfies it.
7. Manual blocking: staff can block availability targeting any combination of "all," one or more room types, and/or one or more specific rooms in a single action (per the owner's kickoff answer) — new schema + staff-facing endpoint.
8. Guest widget follow-up (Milestone 6): the accommodation step lists individual rooms (not just types) for `INDIVIDUAL_ROOM_ONLY`/`MIXED` properties, with a "let the system choose" option where the same-price invariant allows it.
9. Direct-room-entry flow: a guest can land on one specific room's page and see availability/book against only that room, skipping the type-selection step entirely (the original plugin's `$fixed_room_mode` concept, currently unwired).
10. E2E test: a property configured under each of the three modes completes a real guest booking correctly, and staff can exercise every manual-blocking target combination (all / type / specific room(s), combined) without breaking the pooled-count model still used by `ROOM_TYPE_ONLY` properties.

## Explicitly not included

- Per-room rich content (galleries, floor plans) beyond what's needed to distinguish rooms in the picker — a possible future polish item, not core to the booking-mode capability.
- Booking multiple specific rooms in a single guest transaction (multi-room individual booking) — out of scope unless an actual tenant needs it.
- Complex per-room seasonal/date-bounded pricing beyond a flat override — reuses whatever `RateRule` already supports; no new pricing-rule engine.
- Retrofitting the WordPress Admin back-office removed in Milestone 6 Task 5 — manual blocking and booking-mode configuration are staff-facing, but ship in Milestone 9's Tenant Admin Dashboard, not by resurrecting the deleted plugin admin screens.

## Sequencing note (2026-07-31, updated)

Milestone 6 (WordPress plugin) is paused — backend milestones take priority for now, with one consolidated guest-widget integration pass planned before Milestone 12 (Integration & Initial Release Readiness — see Milestone 6's Status line; renumbered from 13 by ADR-0025). This milestone's own draft tasks 8-9 above are guest-widget work by nature. At this milestone's actual kickoff, split those two out explicitly as **deferred** in the task table (not silently dropped, and not counted against this milestone's close-out) — the backend capability (tasks 1-7, 10) can be built and tested standalone against real API calls, same as Milestone 5's PokPay/email follow-ups are being done without touching WordPress at all. Tasks 8-9 get picked up together with Milestone 6's own remaining items in that later integration pass.

**Order flip (2026-07-31):** this milestone now runs *after* Milestone 9 (Tenant Admin Dashboard), reversing the original plan. Milestone 9 already ships against today's room-type-only pooled model and carries its own explicit deferred-follow-up tasks for this milestone's manual-blocking/booking-mode UI — so starting this milestone after Milestone 9 doesn't block anything; it just means those two follow-up tasks in Milestone 9 get implemented once this milestone's backend (tasks 1-7, 10) actually exists.

## Tasks

Draft tasks 8-9 (guest-widget individual-room listing and direct-room-entry) are **deferred** per the sequencing note above — picked up in Milestone 6's later consolidated integration pass, not counted against this milestone's close-out. Tasks 1-7 and 10 below cover the backend capability, tested standalone against real API calls. Two new tasks (8-9 below, renumbered) wire the resulting capability into the two Milestone 9 screens that already carry an explicit deferred-follow-up note for this milestone (Settings' booking-mode setting, Calendar's manual-blocking controls).

| # | Task | Acceptance criteria | Status | PR |
| --- | --- | --- | --- | --- |
| 1 | `Property.bookingMode` schema + settings API | Add a `bookingMode` enum column to `Property` (`ROOM_TYPE_ONLY` / `INDIVIDUAL_ROOM_ONLY` / `MIXED`, per ADR-0022), defaulting existing and new properties to `ROOM_TYPE_ONLY` so no tenant's behavior changes until they opt in. A PATCH endpoint (Owner/Admin only, matching the existing property-settings authorization pattern) lets a tenant change it. Non-goal: changing `bookingMode` does not retroactively touch existing bookings or inventory data — it only affects future availability/booking behavior. Tests: default value on both existing (via migration backfill) and newly created properties; PATCH round-trips; non-Owner/Admin roles rejected; tenant scoping. | Done | |

**Reviewed clean after one direct fix (2026-08-03).** Migration is properly idempotent (`CREATE TYPE ... EXCEPTION WHEN duplicate_object`, `ADD COLUMN IF NOT EXISTS`, backfill-then-`NOT NULL`), verified it applies cleanly against the local database (`prisma migrate status` reports up to date). The PATCH endpoint reuses the *existing* `properties.update()` raw-SQL partial-update pattern exactly (`CASE WHEN ... THEN ... ELSE column END`) with a proper `::"PropertyBookingMode"` cast, and rides the *already-existing* `@Roles(Owner, Admin)` + `@RequiresCapability('settings.manage')` guard chain on that same endpoint rather than adding a new one — no new attack surface. `properties.create()` correctly relies on the column's DB-level default rather than setting it explicitly, matching "new properties default to ROOM_TYPE_ONLY."

Independently reran the full API suite myself (Codex's own report disclosed the default parallel run couldn't complete — a Vitest/tinypool worker IPC crash, and their single-worker retry ran out of their command's time limit before finishing). Running single-fork here reproduced the same IPC crash inconsistently but also surfaced a real, genuine regression Codex's scoped `properties.e2e.spec.ts`-only run had no way to catch: `auth.e2e.spec.ts`'s `toEqual` deep-equality assertion on the full signup response body was never updated for the new `bookingMode` field now present in `auth.service.ts`'s response, so every signup started failing that test. Fixed directly (added `bookingMode: 'ROOM_TYPE_ONLY'` to the expected `property` object) — small and obviously correct, not sent back. After the fix, a clean single-fork run showed only the known pre-existing `audit_logs`-table pollution flake (same one documented on task 28's row last milestone), and confirmed the same known flake reproduces on the *default* parallel pool too, independent of this task's code. `pnpm -w build` and `pnpm -w lint` both clean; Prettier clean on every touched `.ts`/`.tsx` file (`.prisma`/`.sql` aren't Prettier-covered file types in this repo, not a gap).
| 2 | Room-level availability tracking | Extend beyond Milestone 4's anonymous per-room-type pooled count so a specific `Room` can be marked unavailable for a date range, without changing behavior for `ROOM_TYPE_ONLY` properties, which must continue to use only the existing pooled `inventory_units` count — individual-room-level tracking only activates for `INDIVIDUAL_ROOM_ONLY`/`MIXED` properties. Tests: a room-level block correctly excludes that specific room from individual-room availability; a sibling `ROOM_TYPE_ONLY` property's pooled count is provably unaffected by any of this new tracking; tenant/property scoping. | Done | |

**Reviewed clean, no issues found (2026-08-03).** New `room_availability` table follows the exact established RLS pattern (`ENABLE`/`FORCE ROW LEVEL SECURITY`, the same `app_current_tenant_id()`/`app_current_property_id()` policy shape used by 12 other tables), proper composite tenant/property-scoped FKs to both `properties` and `rooms`, idempotent migration (`IF NOT EXISTS`/`DROP POLICY IF EXISTS`). Verified the architectural isolation claim directly, not just from the test: `getAvailability` (the pooled query) only ever joins `inventory_units` — the diff never touches it, so there's no path for `room_availability` data to leak into pooled results regardless of what the test checks. The test itself is genuinely rigorous: it directly inserts a false `room_availability` row for the pooled property via raw SQL (bypassing the API's own `ROOM_TYPE_ONLY` rejection) and confirms the pooled `/availability` endpoint still reports correctly — proving isolation at the query level, not just "the feature works when used correctly." Also covers the `ROOM_TYPE_ONLY` → 400 rejection, `MIXED` mode also being permitted (not just `INDIVIDUAL_ROOM_ONLY`), cross-tenant 403, and cross-property room-id-mismatch 404. Endpoint guards mirror the existing pooled-inventory endpoints exactly (`calendar.view` capability for reads, Owner/Admin + verified-email for writes) — no new attack surface introduced.

Independently reran: single-fork full API suite 34/38 files passed (`room-availability.e2e.spec.ts` included), the 4 failures are the same already-known flakes (the `audit_logs`-pollution pattern across 3 platform-admin files, plus the pre-existing `local-pms-provider.e2e.spec.ts` timing flake confirmed unrelated to any of this session's work via the earlier clean-`main` stash test). `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 3 | Optional per-room price override | A `RateRule` (or sibling concept, your call at implementation) can price a specific `Room` differently from its room type's base rate. Tests: a booking against a room with a price override is charged the override amount, not the room-type base rate; a booking against a room without an override falls back correctly to the room-type base rate. | Done | |

**Reviewed clean, no issues found (2026-08-03).** New `room_price_overrides` table: flat one-row-per-(rate-plan, room) override, same established RLS pattern, `CHECK (amount >= 0)`, composite FKs to both `rate_plans` and `rooms`. Traced the pricing query itself (not just the test) since this is money-handling code: the new `LEFT JOIN room_price_overrides ... AND room_override.room_id = (input.roomId ?? null)::uuid` correctly never matches when no `roomId` is given (SQL `= NULL` is never true), so room-type-only quotes are provably unaffected; when a `roomId` is given, `COALESCE(room_override.amount, resolved.amount)` picks the override over the resolved `RateRule` price per night, and since the override is a flat amount, the same row applies uniformly across every generated night — correct given the schema has no per-night dimension. The pre-existing `pricedNights` completeness check (every requested night must resolve *some* price or the quote is rejected) is preserved unchanged under the new logic. `requireRoomForType` correctly rejects a `roomId` that doesn't belong to the requested `roomTypeId`. The quote-signing payload and its equality check both gained `roomId`, so a signed quote for one room can't be silently reused for a different one — this also lays the groundwork Task 4 needs to bind `createBooking(roomId)` to the room it was actually quoted for.

Endpoint guards mirror the existing rate-plan/rule endpoints exactly (list = any tenant member via `@TenantScoped` only, matching the pre-existing `list()`/`listRules()` pattern; set/delete = Owner/Admin + verified email). The e2e test is well-targeted: two rooms of the same type, one overridden to €125.50/night (→ €251.00 for 2 nights) and its sibling left at the €100.00 base rate (→ €200.00) — proving the override doesn't leak across rooms of the same type, which is exactly the failure mode worth catching here. Cross-tenant PATCH correctly rejected (403). The DELETE endpoint isn't separately covered by a round-trip test, but its logic is a straightforward tenant-scoped delete + audit log matching several already-established, untested-in-isolation delete endpoints elsewhere in this controller — not worth blocking on.

Independently reran: single-fork full API suite 35/39 files passed (`room-price-overrides.e2e.spec.ts` included), same 4 known pre-existing flakes as tasks 1-2 (audit-log pollution × 3, `local-pms-provider` timing). `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 4 | `createBooking` accepts an optional specific `roomId` | In addition to today's required `roomTypeId`, accept an optional `roomId`, validated against the property's `bookingMode` (reject `roomId` for `ROOM_TYPE_ONLY`; require it for `INDIVIDUAL_ROOM_ONLY`; optional for `MIXED`, triggering task 6's auto-assign path when omitted) and that room's real-time availability (reject if already booked or blocked for the requested date range — genuine double-booking prevention, not just an advisory check). Tests: each `bookingMode`'s validation branch (accept/reject/optional); concurrent-booking-attempt double-booking prevention for the same specific room, mirroring Milestone 4's existing pooled-count concurrency test pattern. | Done | |

**Reviewed clean, no issues found, most correctness-sensitive task so far (2026-08-03).** Traced the full booking-creation path by hand, not just the test. Locking: `lockRoom` acquires `pg_advisory_xact_lock(hashtextextended("tenantId:propertyId:roomId", 0))` before any validation runs, at the exact same serialization point as the pre-existing `lockBookedUnits` — same idiom, same key shape, just keyed by room instead of room type. Availability: `roomIsAvailable` is a live query (no separate reservation ledger), checking both Task 2's `room_availability` blocking table and any overlapping booking in an "active-ish" status — and since the new booking's own row is inserted in `DRAFT` status *before* this check runs, and `DRAFT` isn't in the blocking-status list, it correctly never blocks against itself. Verified this isn't just theoretically sound: the test fires two genuinely concurrent (`Promise.all`) booking attempts for the same room/dates and asserts exactly one `CONFIRMED` and one `AVAILABILITY_FAILED` — real proof, not just a code-reading argument.

One observation, not a Task 4 regression: the blocking-status list (`PAYMENT_PENDING`, `PAYMENT_NOT_REQUIRED`, `PMS_CREATION_PENDING`, `PMS_CONFIRMATION_PENDING`, `CONFIRMED`, `PAYMENT_FAILED`, `PMS_UNKNOWN_RESULT`, `PMS_REJECTED`, `MANUAL_REVIEW`) includes some failure/uncertain states that the *existing* pooled model's cancel-path never explicitly releases inventory from either (only `PAYMENT_PENDING`/`PMS_CONFIRMATION_PENDING`/`CONFIRMED` are released on cancel) — this task faithfully mirrors that pre-existing ambient behavior rather than diverging from it, and continuing to block on `PMS_UNKNOWN_RESULT`/`MANUAL_REVIEW` is arguably the conservative, correct choice (avoids a double-booking risk while a booking's true state is still uncertain). Not something to fix here since it's not new or worse than what already exists.

`validateRoomSelection` correctly checks the room belongs to the requested `roomTypeId` (not just the tenant/property), quote-validation now includes `roomId` in the existing signed-payload equality check (verified via the test's `QUOTE_MISMATCH` case: quoted for one room, booking attempted for a different one), and cancellation/expiry correctly skip pooled-inventory release for room-based bookings (`if (!row.roomId) ...`) since the room path never touched that counter in the first place. The test's coverage is comprehensive: all three `bookingMode` branches, quote/room mismatch, Task 2's manual-block integration (blocked room → `AVAILABILITY_FAILED`), the full guest-facing HTTP path (not just calling the provider directly), the concurrency proof, and a direct DB check confirming exactly one `CONFIRMED` row exists for the contested room.

Independently reran: single-fork full API suite 36/40 files passed (`individual-room-booking.e2e.spec.ts` included), same 4 known pre-existing flakes as tasks 1-3. `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 5 | Guest-facing catalog/availability API — individual room exposure | Expose individual rooms (their own IDs, even where display names repeat within a type, plus real-time availability) when the property's `bookingMode` requires or allows room-level choice. API-only in this task — no WordPress UI consumes it yet (task 8/9 of the original draft, deferred to Milestone 6's later pass); test against real API calls directly. Tests: a `ROOM_TYPE_ONLY` property's catalog response is provably unchanged (no per-room breakdown leaks through); `INDIVIDUAL_ROOM_ONLY`/`MIXED` properties' catalog includes individual room IDs and their availability. | Done | |

**Sent back (2026-08-03): the guest-catalog feature itself is correct, but one out-of-scope schema change needs reverting.** The actual catalog endpoint is well done — `ROOM_TYPE_ONLY` correctly short-circuits before parsing dates or touching `rooms` at all (verified, matches the isolation bar from tasks 2/5's own acceptance criteria), and the individual-room availability query correctly reuses the exact same blocking-status list Task 4 established, not a divergent one.

The problem: the migration drops `rooms`' existing `@@unique([tenantId, propertyId, name])` constraint entirely (`DROP CONSTRAINT rooms_tenant_property_name_key`), and the new e2e test explicitly relies on this by creating two rooms named "Classic Room" in the same room type. My own acceptance-criteria wording ("even where display names repeat within a type") was meant as a defensive instruction — don't assume names are unique when building the *API response* — not an instruction to remove the DB guarantee. `rooms.service.ts`'s `create()`/`update()` already have explicit application-level handling for this exact constraint (`ConflictException('A room with this name already exists for this property.')`), which is now dead code with no test protecting it — I checked, and no existing test (`rooms.e2e.spec.ts` or otherwise) covers that rejection path, which is exactly why the full suite run didn't flag this. Staff can now silently create duplicate-named rooms in the same property, an unrelated, out-of-scope, unintended product-behavior change.

The actual feature doesn't need this: exposing rooms by `id` in the catalog response works identically whether names are unique or not, since the response already keys on `id`, not `name`. Follow-up dispatched to Codex: restore the constraint, and change the test's two same-type rooms to distinct names (e.g. "Classic Room A"/"Classic Room B") — the availability-per-room assertions don't depend on the names matching.

**Follow-up verified and Done (2026-08-03).** Confirmed directly against the local database, not just the migration ledger — `SELECT conname FROM pg_constraint WHERE conname = 'rooms_tenant_property_name_key'` returns the constraint, so it's genuinely back on the table, not just removed from the migration folder while still dropped underneath. The catalog test's fixtures now use distinct names, and a new regression test in `rooms.e2e.spec.ts` asserts a duplicate room name returns 409 — closing the exact gap this was sent back for. Independently reran: single-fork full API suite 37/41 files passed (both `public-catalog-individual-rooms.e2e.spec.ts` and the updated `rooms.e2e.spec.ts` included), same 4 known pre-existing flakes as every prior task this milestone. `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 6 | Same-price invariant for Mixed mode's auto-assign path | When a guest books under `MIXED` mode without specifying a `roomId` (task 4), the system may only auto-pick a room among rooms of that type sharing an identical effective price (base rate, or task 3's override) — enforced server-side per ADR-0022, never assumed from the data. Tests: auto-assign correctly picks among same-priced candidates; a room with a different price override (task 3) is excluded from the auto-assign candidate pool even though it's the same room type; auto-assign fails clearly (not silently mis-prices) if every same-type room has a distinct override and none match. | Done | |

**Reviewed clean, no issues found (2026-08-03).** The invariant is tied directly to the guest-facing guarantee rather than an abstract "candidates match each other" reading: since a no-`roomId` `MIXED` quote is priced at the room type's base rate (no override join possible without a `roomId`, per Task 3), `reserveSamePricedRoom` re-prices each candidate room individually via `QuoteService.price` and only accepts an *exact* match against `command.total` — the guest can never be auto-assigned into a room that would silently charge them something different than what they were quoted, which is the actual property ADR-0022 cares about.

Traced the concurrency-safety of iterating multiple candidates in one transaction, since this was a real question worth checking: `reserveRoom` acquires a `pg_advisory_xact_lock` per candidate tried, held for the rest of the transaction (these aren't released between candidates). This *could* be a deadlock risk if concurrent transactions acquired candidate locks in different orders — but the candidate query is `ORDER BY created_at`, deterministic and identical across every concurrent transaction for the same room type, so all transactions acquire locks in the same total order, which rules out a deadlock cycle by construction. Not something the submission called out explicitly, but verified sound rather than assumed.

Test coverage matches the invariant's real edge cases, not just the happy path: a price-mismatched room earlier in creation order is correctly skipped (not accidentally picked), a second booking correctly falls through to the next available same-priced candidate once the first is taken, and an all-mismatched room type fails clearly with `AUTO_ASSIGNMENT_UNAVAILABLE` rather than silently picking a wrong price. The Task 4 test update is a genuine re-proof, not a superficial patch: it blocks one candidate via Task 2's manual-block endpoint first, forcing auto-assign to skip it and pick the next same-priced room, confirming the auto-assign path also respects manual blocks.

Independently reran: single-fork full API suite 38/42 files passed (`mixed-auto-assignment.e2e.spec.ts` and the updated `individual-room-booking.e2e.spec.ts` both included), same 4 known pre-existing flakes as every prior task. `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 7 | Manual blocking — schema + staff-facing endpoint | New schema plus a staff-facing endpoint letting staff block availability targeting any combination of "all," one or more room types, and/or one or more specific rooms, combined freely in a single blocking action (per ADR-0022's explicit resolution of this — not a re-ask). Tests: each target type individually (all / type / room) and meaningfully combined (e.g. block one room type plus one specific room from a different type in the same action); a block correctly restricts subsequent availability and booking-attempt checks for its exact target and date range, and does not affect anything outside that scope; tenant/property scoping. | Done | |

**Reviewed clean, no issues found (2026-08-03).** Schema is a sensible header-plus-two-junction-tables design (`availability_blocks` + `availability_block_room_types` + `availability_block_rooms`), same RLS pattern as every other table this milestone, `CHECK (ends_on > starts_on)`, `ON DELETE CASCADE` from the junction tables to their parent block. Doesn't duplicate or conflict with Task 2's `room_availability` or the pooled `inventory_units` counter — it's a genuinely new layer that both existing mechanisms now additionally consult.

Verified the block check was wired into every path that needed it, not just the new endpoint's own read: the pooled `getAvailability` GET query (returns 0 via a `CASE` when `blocks_all` or a room-type target overlaps), pooled *reservation* (`adjustBookedUnits` now checks `roomTypeIsBlocked` only on the reserve direction, correctly not on release — you can always release inventory regardless of a later block), Task 4's room-level `roomIsAvailable` (extended with a third `NOT EXISTS` covering all three target kinds via OR), and Task 5's public catalog room-availability query — the same block-matching logic appears in all of these, not a divergent one-off. Room-specific block targets are correctly rejected for `ROOM_TYPE_ONLY` properties (mirrors Task 2's own rule), while room-type/`all` targets are allowed on pooled properties, matching that pooled properties still need blocking, just not at room granularity.

The test is genuinely thorough: each target kind individually, `all` reaching into a type untouched by the room-type block, a combined action (room-type + a different room together) proving they compose, an exact date-boundary check (the day immediately after a block ends is available), integration proof through an actual `createBooking` attempt (not just the availability read), the public catalog endpoint, and both cross-tenant and cross-property rejection. Also directly exercises the pooled path via `AvailabilityService.reserveBookedUnits` returning `false` when blocked, not just the GET-availability read.

One disclosed, reasonable scope cut: block creation only, no list/removal endpoint yet. Not required by this task's acceptance criteria; worth deciding at Task 9's dispatch whether the Calendar UI needs it or is create-only for now.

Independently reran: single-fork full API suite 39/43 files passed (`manual-availability-blocks.e2e.spec.ts` included), same 4 known pre-existing flakes as every prior task. `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 8 | Wire the booking-mode setting into Settings (Milestone 9 Task 8's deferred follow-up) | Add the per-property `bookingMode` setting (A/B/C, task 1) to the existing Settings page built in Milestone 9, Owner/Admin only, matching that page's existing identity/rules save pattern (including its loading/error/retry handling). Tests: the setting round-trips through the real UI and its effect is independently verified against task 4's actual enforcement (not just that the PATCH request fired). | Done | |

**Reviewed clean, no issues found (2026-08-03).** `settings.tsx`'s new booking-mode section matches the existing identity/rules pattern exactly (same partial-update `save()` call, only sent when changed, same saving/toast handling). The real proof this task asked for is genuinely real: `booking-mode-settings.e2e.ts` is a Playwright browser test — attempts a room-specific booking before any UI interaction (rejected, `ROOM_ID_NOT_ALLOWED`), navigates to the actual rendered Settings page, selects "Individual-Room-Only" from the real dropdown, clicks the real save button, waits for the real PATCH response, then repeats the exact same booking attempt and confirms it now succeeds (`CONFIRMED`) — proving the control's real effect through the browser, not a mocked assertion that a request fired.

`e2e/support.ts`'s shared helper changes are legitimate, not scope creep: `signup()`'s removed address/timezone fields match the current signup form exactly (confirmed against `signup/page.test.tsx`'s own assertion that those fields don't exist), and `deleteTenant()`'s new cleanup statements for this milestone's new tables are correctly FK-ordered (children before the parents their `RESTRICT` FKs reference) — necessary since tasks 1-7 added several new `RESTRICT`-constrained tables the E2E cleanup didn't know about yet.

Independently reran, not just trusted the report: full web unit suite (32/32 files), and — since this task's whole point is real browser-verified behavior — actually ran the full Playwright suite myself (`pnpm e2e`, builds a production Next.js bundle and boots real API/mail-sink servers): all 5 tests passed, including this task's own real end-to-end proof. `pnpm -w build`/`lint` clean, Prettier clean on every touched file.
| 9 | Wire manual-blocking controls into Calendar (Milestone 9 Task 4's deferred follow-up) | Add manual-blocking controls (target All / room type / specific room(s), combinable per task 7) to the existing Calendar page built in Milestone 9, consuming task 7's endpoint. Tests: creating a block through the real UI correctly restricts what task 7's endpoint (and therefore subsequent guest/staff availability checks) reports as available. | Done | |

**Reviewed clean, no issues found (2026-08-03).** Completed the `bookingMode`/`canManageAvailability` prop-wiring that was already sitting half-done in `dashboard-shell.tsx` from before this task was dispatched. Controls are gated to Owner/Admin (`canManageAvailability`), and specific-room targets only appear for `INDIVIDUAL_ROOM_ONLY`/`MIXED` properties (`canTargetRooms`), matching Task 7's own `ROOM_TYPE_ONLY` rejection rule exactly — extra room-list API calls are only fetched when both conditions hold, avoiding unnecessary requests for Staff or pooled properties.

Traced the date-range handling carefully since it's an easy place to get subtly wrong: `dateToIsoDay` uses local-timezone getters (`getFullYear`/`getMonth`/`getDate`), not `toISOString()`, correctly avoiding the classic UTC-shift bug for users in negative offsets; DayPicker's inclusive range selection is correctly converted to the backend's exclusive-`endsOn` convention by adding one day to the selected end date. The `refresh` counter cleanly forces a real refetch after block creation (bypassing the SSR-initial-data shortcut only after the first render) rather than duplicating fetch logic or reloading the page.

React DayPicker adopted per the owner's decision, scoped to exactly the block's date-range input — the operational month grid stays fully custom as already decided. Create-only UI is a reasonable, disclosed scope cut matching Task 7's own gap (no list/removal endpoint exists yet); the form explicitly tells staff existing blocks can't be managed yet, not a silent omission.

The e2e test is genuinely convincing: sets up a real Mixed property with pooled inventory, clicks through the actual rendered DayPicker (`[data-day=...]`), selects both a room type and a specific room together, confirms the real POST response contains both target arrays, and — the important part — confirms the calendar grid's own remaining-units count actually drops from 2 to 0 after the block, proving the real effect rather than just that a request fired.

Independently reran, not just trusted the report: full web unit suite (32/32 files, 74 tests) and the full Playwright suite (`pnpm e2e`, 6/6 tests including this task's own). `pnpm -w build`/`lint` clean, Prettier clean on every touched file. (Note: Codex's own report flagged a stale lint blocker from a temp migration script of mine that I'd already deleted before this task was dispatched — confirmed gone from the working tree, not a real issue.)
| 10 | E2E test: all three modes + full manual-blocking matrix | A property configured under each of the three `bookingMode`s completes a real guest booking correctly end-to-end, and staff can exercise every manual-blocking target combination (all / type / specific room(s), combined) without breaking the pooled-count model still used by `ROOM_TYPE_ONLY` properties. This is the milestone's own closing proof, not a substitute for each task's own tests above. | Not started | |
