# HTTP listener port for the local API.
APP_PORT=3000

# Local PostgreSQL service started by `docker compose up -d`.
DATABASE_URL=postgresql://must_booking:must_booking_dev@localhost:5432/must_booking?schema=public

# Local Redis service started by `docker compose up -d`.
REDIS_URL=redis://localhost:6379
